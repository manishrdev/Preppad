// DSA patterns, LLD/HLD, and GenAI content for the 14-week roadmap.
// Question format: [level, question, answer, subject]  level 1=Junior 2=Mid 3=Senior 4=Lead
// "subject" is the pattern/stage name shown as a tag instead of the plain topic name.
// Note format: { title, points: [], code?, diagram? }

export const dsa = {
  questions: [
    // Arrays & Hashing
    [1, 'What is the core idea behind the Arrays & Hashing pattern?', 'Trade O(n) extra space for O(1) average lookup so you avoid nested loops. Store what you have seen so far in a hash map keyed by value (or a derived signature), then check membership or a complement in O(1) instead of scanning the rest of the array.', 'Arrays & Hashing'],
    [1, 'Walk through the approach for the classic Two Sum problem.', 'Scan once, and for each number check whether its complement (target - number) is already in a hash map of value to index. If not, store the current number and index and keep going. Single pass, O(n) time, O(n) space, versus O(n^2) for the brute-force nested loop.', 'Arrays & Hashing'],
    [2, 'How would you solve Group Anagrams?', 'Build a signature per word (the sorted characters, or a 26-length character count) and use it as a hash map key, appending each original word to the list for that key. Words sharing a signature are anagrams. O(n * k log k) with sorting, or O(n * k) with counting, where k is word length.', 'Arrays & Hashing'],
    [2, 'How do you find duplicates in an array in O(n) time versus O(1) extra space?', 'O(n) time / O(n) space: add each element to a hash set, a duplicate is one already present. O(n log n) time / O(1) extra space: sort first, then compare neighbors. If values are bounded to the array length you can also mark visited indices in place or use Floyd\'s cycle detection when the array represents a function.', 'Arrays & Hashing'],
    [3, 'Explain Product of Array Except Self without using division.', 'Build a prefix-product array where prefix[i] is the product of everything before i, and a suffix-product array where suffix[i] is the product of everything after i. The answer at i is prefix[i] * suffix[i]. You can compute this in one pass in each direction, reusing the output array so it runs in O(n) time and O(1) extra space excluding the output.', 'Arrays & Hashing'],

    // Two Pointers
    [1, 'Explain the two-pointer pattern and when it applies.', 'Use two indices that move toward each other or in step across a sorted or otherwise structured sequence, instead of checking every pair with nested loops. It turns many O(n^2) problems into O(n) because each pointer only moves forward, never backtracking.', 'Two Pointers'],
    [1, 'How do you solve Two Sum when the input array is already sorted?', 'Start one pointer at the left end and one at the right end. If the sum is too small, move the left pointer right to increase it; if too large, move the right pointer left to decrease it; if it matches, you are done. O(n) time, O(1) space, relying on the sorted order.', 'Two Pointers'],
    [2, 'Explain the approach for Container With Most Water.', 'Start pointers at both ends of the array; the water held is limited by the shorter wall times the distance between pointers. Move the pointer at the shorter wall inward, since keeping the taller wall can only help, never hurt, on the next comparison. O(n) time, O(1) space.', 'Two Pointers'],
    [2, 'How do you solve 3Sum in O(n^2)?', 'Sort the array, then fix each index i and run a two-pointer search for a pair summing to -nums[i] in the remaining subarray. Skip over duplicate values for i, and for the left/right pointers once a valid triplet is found, to avoid duplicate triplets in the output.', 'Two Pointers'],
    [3, 'Explain Trapping Rain Water using two pointers.', 'Maintain leftMax and rightMax as you move two pointers inward from both ends. At each step, move the pointer on the side with the smaller max, since the water trapped there is limited by that smaller max regardless of what is further ahead on the other side. O(n) time, O(1) space, versus O(n) extra arrays for the simpler prefix/suffix-max version.', 'Two Pointers'],

    // Sliding Window
    [1, 'Explain the sliding window pattern.', 'Maintain a contiguous window over the array or string with a left and right boundary, expanding the right edge to include more elements and contracting the left edge to drop elements, instead of recomputing a range from scratch each time. It turns many O(n^2) substring/subarray problems into O(n).', 'Sliding Window'],
    [1, 'How do you find the maximum sum subarray of a fixed size k?', 'Compute the sum of the first k elements, then slide the window one step at a time by subtracting the element leaving the window and adding the element entering it, tracking the running maximum. O(n) time instead of recomputing each window sum from scratch.', 'Sliding Window'],
    [2, 'How do you find the length of the longest substring without repeating characters?', 'Use a variable-size window with a set or map tracking the last seen index of each character. Expand the right pointer; whenever you hit a character already in the window, move the left pointer just past its previous occurrence. Track the max window size seen. O(n) time.', 'Sliding Window'],
    [2, 'Explain the approach for Minimum Window Substring.', 'Expand the right pointer, tracking counts of characters you still need, until the window contains everything required. Then contract the left pointer as far as possible while the window stays valid, recording the smallest valid window found, before continuing to expand again. O(n + m) time with a need/have count map.', 'Sliding Window'],
    [3, 'How do you decide between a fixed and a variable-size sliding window, and when does the pattern not apply?', 'Use a fixed window when the problem gives you an explicit window size. Use a variable window when the constraint is a condition to satisfy, such as longest or shortest substring meeting some property. Sliding window does not help when order does not matter, when you need arbitrary (non-contiguous) subsets, or when adding one element can invalidate a window in a non-monotonic way that breaks the expand/contract logic.', 'Sliding Window'],

    // Binary Search
    [1, 'What is the precondition for binary search, and how do you frame it as a search space rather than just a sorted array?', 'Binary search needs a monotonic predicate over the search space: everything on one side is false, everything on the other is true (or the space is sorted). That space does not have to be an array index; it can be a value range you are searching over, which is the basis of the "binary search on the answer" pattern.', 'Binary Search'],
    [2, 'How do you find the first and last position of a target in a sorted array?', 'Run two slightly different binary searches: one that keeps moving left when it finds the target to locate the first occurrence (lower bound), and one that keeps moving right to locate the last occurrence (upper bound). Both are O(log n), giving O(log n) total instead of an O(n) linear scan.', 'Binary Search'],
    [2, 'How do you search in a rotated sorted array?', 'At each step, compare the middle element to the endpoints to determine which half is properly sorted. If the target falls within the sorted half\'s range, binary search that half; otherwise search the other half. Still O(log n) despite the rotation.', 'Binary Search'],
    [3, 'Explain "binary search on the answer" with an example like minimum ship capacity to ship packages within D days.', 'Binary search over the possible answer values (capacities) instead of array indices. Write a feasibility check, such as "can this capacity ship everything within D days," which is monotonic: if a capacity works, every larger capacity also works. Binary search the smallest capacity for which the check passes, giving O(n log(range)) instead of trying every capacity.', 'Binary Search'],
    [3, 'How would you find the median of two sorted arrays in O(log(min(m, n)))?', 'Binary search a partition point in the smaller array; the partition point in the larger array is determined by the combined left-half size. Adjust the partition until the max of the left elements is less than or equal to the min of the right elements on both arrays, which means you have found the correct split, and the median comes from the boundary elements.', 'Binary Search'],

    // Stack
    [1, 'When is a stack the right structure, and what is a classic example?', 'A stack gives last-in-first-out ordering, which fits problems about matching pairs, undo history, DFS, and parsing nested structure. The classic example is Valid Parentheses: push opening brackets, and on a closing bracket, check the top of the stack matches before popping.', 'Stack'],
    [1, 'Walk through solving Valid Parentheses with a stack.', 'Push every opening bracket onto a stack. On a closing bracket, the stack must be non-empty and its top must be the matching opening bracket; pop it. If any check fails, or the stack is not empty at the end, the string is invalid. O(n) time and space.', 'Stack'],
    [2, 'What is a monotonic stack, and what is it used for?', 'A stack kept strictly increasing or strictly decreasing by popping elements that would break the order before pushing the new one. It is used to find the next greater or next smaller element for every position in O(n) total, since each element is pushed and popped at most once.', 'Stack'],
    [2, 'How do you solve Daily Temperatures?', 'Keep a monotonic decreasing stack of indices (by temperature). For each new day, pop indices off the stack whose temperature is lower than today\'s, and record today\'s index minus the popped index as the wait; then push today\'s index. O(n) time since each index is pushed and popped once.', 'Stack'],
    [3, 'How would you evaluate Reverse Polish Notation, or design a basic calculator, using a stack?', 'For RPN, push operands; when an operator is read, pop the last two operands, apply the operator, and push the result back, finishing with one value left on the stack. For a calculator with parentheses and precedence, use two stacks (one for values, one for operators) or recursive descent, applying operators from the operator stack whenever precedence or a closing parenthesis requires it.', 'Stack'],

    // Linked List
    [1, 'How do you reverse a singly linked list iteratively?', 'Walk the list with three pointers: previous, current, and next. At each node, save next, point current\'s next pointer back to previous, then advance previous and current forward. O(n) time, O(1) space.', 'Linked List'],
    [2, 'How do you detect a cycle in a linked list, and find where it begins?', 'Use Floyd\'s tortoise and hare: a slow pointer moves one step and a fast pointer moves two; if they meet, a cycle exists. To find the start of the cycle, reset one pointer to the head and advance both pointers one step at a time; they meet exactly at the cycle\'s start. O(n) time, O(1) space.', 'Linked List'],
    [2, 'How do you merge two sorted linked lists?', 'Create a dummy head node and a tail pointer. Repeatedly compare the current heads of both lists, splice the smaller one onto the tail, and advance that list\'s pointer. Attach whatever remains once one list is exhausted. O(n + m) time, O(1) extra space (excluding the merged list itself).', 'Linked List'],
    [3, 'How do you find the middle of a linked list in a single pass?', 'Use a slow pointer that advances one node at a time and a fast pointer that advances two nodes at a time. When the fast pointer reaches the end, the slow pointer is at the middle. One pass, O(1) extra space, versus counting the length first and walking again.', 'Linked List'],
    [3, 'How would you reorder a linked list into L0, Ln, L1, Ln-1, L2, Ln-2, ...?', 'Find the middle with slow/fast pointers, reverse the second half in place, then merge the two halves by alternating nodes from the first half and the reversed second half. O(n) time, O(1) extra space.', 'Linked List'],

    // Trees
    [1, 'Explain the three DFS traversal orders and BFS for binary trees, and when you would use each.', 'Preorder (node, left, right), inorder (left, node, right), and postorder (left, right, node) are all depth-first, done recursively or with an explicit stack. Inorder on a binary search tree yields sorted order. Level-order (BFS) uses a queue and processes the tree level by level, useful for problems about tree width, level sums, or shortest structural distance.', 'Trees'],
    [2, 'How do you check whether a binary tree is height-balanced?', 'Compute height bottom-up: for each node, get the height of the left and right subtrees; if either is already "unbalanced" (use a sentinel like -1) or their difference exceeds 1, propagate that unbalanced signal up instead of recomputing. This checks balance in a single O(n) pass instead of O(n^2) from recomputing height at every node.', 'Trees'],
    [2, 'How do you find the lowest common ancestor of two nodes in a binary tree?', 'Recurse from the root: if the current node is either target, return it. Otherwise recurse into both children; if both sides return a non-null result, the current node is the LCA, otherwise propagate whichever side is non-null. O(n) time, single pass.', 'Trees'],
    [3, 'How would you serialize and deserialize a binary tree?', 'Serialize with a preorder DFS, writing a marker (like "#") for null children, joined by a delimiter into a string. Deserialize by splitting the string back into tokens and consuming them in order with the same recursive preorder structure, building null nodes on the marker. This preserves the exact tree shape, unlike just storing sorted values.', 'Trees'],
    [3, 'How do you validate whether a binary tree is a valid binary search tree?', 'Recurse with a valid (low, high) range for each node, checking the node\'s value falls strictly within it, and narrowing the range for each child (the range is not just compared to the immediate parent). Equivalently, do an inorder traversal and check the sequence is strictly increasing. Comparing only to the immediate parent, without carrying the bound down, is the classic bug.', 'Trees'],

    // Heap
    [1, 'What is a heap used for, and what are the complexities of its core operations?', 'A heap (priority queue) keeps the minimum or maximum element quickly accessible. Push and pop are O(log n), peek at the top is O(1). It is used for top-K problems, scheduling by priority, merging sorted streams, and algorithms like Dijkstra\'s shortest path.', 'Heap'],
    [2, 'How do you find the Kth largest element in an array?', 'Maintain a min-heap of size K; push each element, and if the heap exceeds size K, pop the smallest. After processing everything, the heap\'s minimum is the Kth largest. O(n log k) time. Quickselect gives average O(n) if you do not need the heap\'s other properties.', 'Heap'],
    [2, 'How do you merge K sorted lists efficiently?', 'Put the head of each list into a min-heap keyed by value. Repeatedly pop the minimum, append it to the result, and push that list\'s next node if it has one. O(N log K) time where N is the total number of nodes and K is the number of lists.', 'Heap'],
    [3, 'How would you design a structure that supports adding numbers and finding the running median in O(log n) per add?', 'Keep two heaps: a max-heap for the lower half of numbers seen so far and a min-heap for the upper half, rebalancing after each insert so their sizes differ by at most one. The median is either the top of the larger heap, or the average of both tops when they are equal size.', 'Heap'],
    [3, 'How do you find the Top K Frequent Elements?', 'Count frequencies with a hash map, then use a min-heap of size K keyed by frequency to keep the K most frequent, giving O(n log k). If K is close to n or you want O(n) average, bucket sort by frequency instead, since frequency is bounded by n.', 'Heap'],

    // Graphs (BFS/DFS)
    [1, 'BFS versus DFS on graphs: which guarantees the shortest path in an unweighted graph, and why?', 'BFS explores the graph layer by layer using a queue, so the first time it reaches a node is guaranteed to be via the fewest edges, giving the shortest path in an unweighted graph. DFS explores as deep as possible before backtracking and does not guarantee shortest paths, but it is a natural fit for reachability, cycle detection, connected components, and topological sort.', 'Graphs (BFS/DFS)'],
    [2, 'How do you solve Number of Islands?', 'Scan the grid; whenever you hit an unvisited land cell, run BFS or DFS flood fill from it to mark every connected land cell as visited, and count that as one island. Repeat for the rest of the grid. O(rows * cols) time since every cell is visited once.', 'Graphs (BFS/DFS)'],
    [2, 'How do you detect a cycle in a directed graph?', 'Run DFS tracking three states per node: unvisited, in the current recursion stack (visiting), and fully processed. If DFS reaches a node that is currently in the recursion stack, that back edge means a cycle. This differs from cycle detection in an undirected graph, where you just track visited versus the immediate parent.', 'Graphs (BFS/DFS)'],
    [2, 'What are the two standard approaches to topological sort, and when is it valid?', 'Kahn\'s algorithm does BFS driven by in-degree counts, repeatedly removing nodes with zero remaining in-degree. The DFS approach does a postorder traversal and reverses the result. Topological sort is only valid on a directed acyclic graph; if either method cannot place every node, a cycle exists.', 'Graphs (BFS/DFS)'],
    [3, 'Explain Dijkstra\'s algorithm and how it differs from BFS.', 'Dijkstra\'s finds shortest paths from a single source on a graph with non-negative edge weights, using a min-heap of (distance, node) and relaxing each edge when a shorter path is found, always expanding the currently-closest unvisited node next. BFS is the special case where every edge weight is 1, so a plain queue suffices instead of a priority queue.', 'Graphs (BFS/DFS)'],
    [3, 'How does Course Schedule reduce to a graph problem, and how do you determine whether all courses can be finished?', 'Model each course as a node and each prerequisite as a directed edge. All courses can be finished exactly when this graph has no cycle, so the problem reduces to cycle detection in a directed graph; if it is acyclic, a valid course order is any topological sort of it.', 'Graphs (BFS/DFS)'],
    [4, 'How would you compute connected components or shortest paths on a graph too large to fit on a single machine?', 'Partition the graph across workers (by min-cut or consistent hashing on node id) and use a bulk-synchronous message-passing model, such as Pregel or GraphX, where BFS-style level propagation happens as a sequence of supersteps exchanging messages between partitions. For approximate answers at extreme scale, probabilistic structures like HyperLogLog can estimate component sizes without exact traversal.', 'Graphs (BFS/DFS)'],

    // Backtracking
    [1, 'What is backtracking, and how does it differ from plain brute force?', 'Backtracking explores choices depth-first via recursion, building a partial solution incrementally, and abandons ("backtracks" out of) a branch as soon as it violates a constraint, instead of exploring it to completion. This prunes large parts of the search space that brute force would enumerate in full.', 'Backtracking'],
    [2, 'How do you generate all subsets of a set?', 'Recurse through the elements, and at each one branch into two calls: include it in the current subset, or exclude it, adding the current subset to the result at each leaf. This produces all 2^n subsets. An iterative alternative is to loop a bitmask from 0 to 2^n - 1 and include element i whenever bit i is set.', 'Backtracking'],
    [2, 'Explain the backtracking approach to the N-Queens problem.', 'Place queens one row at a time; for each row, try every column, and before placing, check that column and both diagonals are not already attacked by a previously placed queen. If a placement is valid, recurse to the next row; if no column works, backtrack to the previous row and try its next option.', 'Backtracking'],
    [3, 'How do you solve Word Search in a grid?', 'Try starting the search from every cell matching the first letter, then do DFS in the four directions matching the next letter of the word, marking cells visited so the same cell is not reused within one path, and unmarking them (backtracking) once that path is exhausted so other starting points can reuse those cells.', 'Backtracking'],
    [3, 'How do you prune a backtracking search so it stays fast on larger inputs?', 'Order choices to try the most constrained option first so invalid branches fail fast, precompute or incrementally maintain feasibility checks (such as bitmasks for row/column/diagonal usage) so each check is O(1) instead of O(n), memoize repeated states when they recur, and terminate early once one valid solution, or the best one, is found if the problem does not need every solution.', 'Backtracking'],

    // Dynamic Programming
    [1, 'How do you recognize a DP problem, and what are the two implementation styles?', 'Look for overlapping subproblems (the same smaller inputs recur) and optimal substructure (the optimal answer is built from optimal answers to subproblems). Top-down memoization writes the natural recursion and caches results by input; bottom-up tabulation fills a table iteratively from the smallest subproblems up, usually with better constant factors and no recursion depth limit.', 'Dynamic Programming'],
    [2, 'Walk through a Fibonacci-style DP like Climbing Stairs.', 'dp[i] = dp[i - 1] + dp[i - 2], since you can reach step i from one step back or two steps back, with base cases dp[0] = 1 and dp[1] = 1. Since only the last two values are needed, this reduces from O(n) space to O(1) space with two rolling variables.', 'Dynamic Programming'],
    [2, 'Explain the state and transition for 0/1 Knapsack.', 'dp[i][w] is the best value using the first i items with capacity w. dp[i][w] = dp[i-1][w] if the ith item does not fit, otherwise the max of skipping it (dp[i-1][w]) or taking it (value[i] + dp[i-1][w - weight[i]]). This can be compressed to a 1D array by iterating capacity backward when updating.', 'Dynamic Programming'],
    [2, 'How do you compute the Longest Common Subsequence of two strings?', 'dp[i][j] is the LCS length of the first i characters of one string and the first j of the other. If the characters match, dp[i][j] = dp[i-1][j-1] + 1; otherwise dp[i][j] = max(dp[i-1][j], dp[i][j-1]). O(m * n) time and space, reducible to O(min(m, n)) space by keeping only two rows.', 'Dynamic Programming'],
    [3, 'How do you solve Coin Change for the minimum number of coins to make an amount?', 'dp[amount] is the minimum coins needed for that amount. For each amount from 1 upward, dp[amount] = min over every coin c <= amount of dp[amount - c] + 1, with dp[0] = 0 and unreachable amounts left at infinity. If dp[target] is still infinity at the end, the amount cannot be made.', 'Dynamic Programming'],
    [3, 'How do you find the Longest Increasing Subsequence in O(n log n)?', 'Maintain a "tails" array where tails[k] is the smallest possible tail value of an increasing subsequence of length k + 1. For each number, binary search tails for the first entry greater than or equal to it and replace it there, or append if it is larger than everything in tails. The final length of tails is the LIS length; this is faster than the O(n^2) DP that compares every pair.', 'Dynamic Programming'],
    [4, 'How do you approach a DP problem with multiple constraints, like a knapsack with an extra resource limit or a weighted edit distance?', 'Add one dimension to the state per constraint (for example dp[i][weight][extraResource]) and check the resulting state space is small enough to be tractable; if it is too large, look for ways to drop a dimension, such as a constraint that is monotonic and can be handled with a pointer instead of a full axis, or offline processing that fixes one dimension in advance. When the exact state space is still too large, consider whether an approximation or a greedy relaxation is acceptable for the problem\'s actual requirements.', 'Dynamic Programming'],

    // Greedy
    [1, 'What makes a problem solvable greedily, and how do you justify that a greedy choice is correct?', 'A problem is greedy-friendly when it has the greedy-choice property (a locally optimal choice leads to a globally optimal solution) plus optimal substructure. You justify it with an exchange argument (show any optimal solution can be transformed to match the greedy choice without getting worse) or by checking the structure matches a known greedy-correct pattern such as interval scheduling or a matroid.', 'Greedy'],
    [2, 'How do you solve the meeting rooms problem: how many rooms are needed for a set of intervals?', 'Sort intervals by start time and use a min-heap of end times. For each meeting, if the earliest-ending room (heap top) already ended by the time this meeting starts, reuse that room (pop and push the new end time); otherwise open a new room (push without popping). The maximum heap size reached is the number of rooms needed.', 'Greedy'],
    [2, 'How do you solve Jump Game (can you reach the last index)?', 'Scan left to right, tracking the farthest index reachable so far. At each index, if it exceeds the current farthest reach, you cannot get there, so return false; otherwise update the farthest reach using that index\'s jump length. This is greedy because the farthest reachable point never needs to be reconsidered once computed.', 'Greedy'],
    [3, 'How do you solve Gas Station: find the starting index to complete a circuit?', 'If the total gas across all stations is less than the total cost, no solution exists. Otherwise, track a running tank total while scanning once; whenever it goes negative, reset the candidate start to the next station and reset the running total to zero. The final candidate start is guaranteed to be the answer when a solution exists.', 'Greedy'],
    [3, 'When does a greedy approach fail and you need DP instead? Give an example.', 'Greedy fails when a locally optimal choice can block a better global solution. The classic example is coin change with a non-canonical coin system, like denominations 1, 3, 4 for amount 6: greedy picks 4 + 1 + 1 (three coins), but the optimal answer is 3 + 3 (two coins). Whenever the greedy-choice property cannot be proven, fall back to DP, which explores all the choices instead of committing early.', 'Greedy'],
  ],
  notes: [
    { title: 'DSA Pattern Cheat-Sheet', points: [
      'Arrays & Hashing: need O(1) lookup/complement/dedup -> hash map or set',
      'Two Pointers: sorted array, pair/triplet sums, palindromes -> two indices moving inward',
      'Sliding Window: contiguous subarray/substring with a constraint -> expand/contract a window',
      'Binary Search: sorted data, or a monotonic yes/no predicate over a range -> halve the space',
      'Stack: matching/nesting, undo, next-greater-element -> LIFO or monotonic stack',
      'Backtracking: build a solution incrementally, prune on constraint violation',
      'DP: overlapping subproblems + optimal substructure -> memoize or tabulate',
      'Greedy: locally optimal choice provably leads to a globally optimal one',
    ] },
    { title: 'Complexity by Pattern', points: [
      'Two pointers / sliding window: O(n) time, O(1) space',
      'Binary search: O(log n) time',
      'Hash map lookups: O(1) average, O(n) worst case (bad hash / collisions)',
      'Heap push/pop: O(log n), peek O(1)',
      'BFS/DFS on a graph: O(V + E)',
      'DP: usually O(states * transitions); watch the state space size before coding',
    ] },
    { title: 'Graph Algorithms at a Glance', diagram: { type: 'flow', items: ['Pick the tool', 'Unweighted shortest path -> BFS', 'Weighted, non-negative -> Dijkstra', 'Cycle / topo order -> DFS', 'Connectivity -> Union-Find'] }, points: [
      'BFS: shortest path when every edge weight is equal',
      'DFS: cycle detection, topological sort, connected components',
      'Dijkstra: single-source shortest path, non-negative weights, min-heap',
      'Union-Find: fast connectivity queries and cycle detection in undirected graphs',
    ], code: 'int[] parent;\nint find(int x){\n  return parent[x]==x ? x : (parent[x]=find(parent[x]));\n}\nvoid union(int a,int b){\n  parent[find(a)] = find(b);\n}' },
  ],
};

export const lld = {
  questions: [
    // SOLID
    [1, 'Explain the Single Responsibility Principle with a concrete refactor.', 'A class should have only one reason to change. A class that both calculates an invoice and formats it for printing has two reasons to change: pricing logic and presentation format. Splitting it into an Invoice class and a separate InvoicePrinter lets each change independently without risking the other.', 'SOLID'],
    [1, 'Explain the Open/Closed Principle.', 'Code should be open for extension but closed for modification: you should be able to add new behavior without editing existing, tested code. A common way to achieve this is polymorphism, such as a Strategy interface, so adding a new payment type means adding a new class rather than editing an existing switch statement.', 'SOLID'],
    [2, 'Explain the Liskov Substitution Principle and give a violation.', 'Subtypes must be usable anywhere their base type is expected without surprising callers. The classic violation is Square extending Rectangle: overriding setWidth to also change height breaks the assumption that setting width alone leaves height unchanged, which code written against Rectangle relies on.', 'SOLID'],
    [2, 'Explain Interface Segregation.', 'Many small, client-specific interfaces are better than one large general-purpose interface, since a fat interface forces implementing classes to provide methods they do not need or support meaningfully, such as a Bird interface with fly() that a Penguin class is forced to implement or throw from.', 'SOLID'],
    [2, 'Explain Dependency Inversion and how a DI container relates to it.', 'High-level modules should depend on abstractions, not on concrete low-level implementations, and the concrete implementation should be supplied from outside rather than constructed internally. A dependency injection container, such as Spring, wires in the concrete implementation at startup, so the high-level class only ever references an interface.', 'SOLID'],

    // Design patterns
    [1, 'When would you use Singleton, and what are its downsides?', 'Singleton fits a single shared resource, such as an application-wide configuration or a connection pool registry, where only one instance should exist. Downsides: it introduces global state that hurts testability, hides dependencies from a class\'s constructor signature, and needs careful handling under concurrency. A DI-managed singleton scope is usually preferable to a hand-rolled one.', 'Design patterns'],
    [2, 'Factory Method versus Abstract Factory: what is the difference?', 'Factory Method is a single method that creates one product, with subclasses deciding which concrete type to instantiate. Abstract Factory is a family of related factory methods that together produce a suite of related objects, such as a UI theme factory producing a matching Button and Checkbox for either a light or dark theme.', 'Design patterns'],
    [2, 'When would you reach for the Builder pattern?', 'When constructing an object needs many optional parameters or a multi-step assembly process, since a constructor with a dozen optional parameters (telescoping constructors) is unreadable and error-prone. A builder exposes fluent, named setter-style methods and a final build() call, making the construction self-documenting.', 'Design patterns'],
    [2, 'Explain the Strategy pattern with an example.', 'Strategy encapsulates a family of interchangeable algorithms behind a common interface and lets the caller inject the one it needs at runtime, instead of branching on type inside one method. For example, a pricing engine could take a DiscountStrategy interface, with different implementations for seasonal, loyalty, or bulk discounts, chosen or swapped without touching the pricing engine itself.', 'Design patterns'],
    [2, 'Explain the Observer pattern and where you have seen it in a framework.', 'A subject keeps a list of observers and notifies all of them when its state changes, without needing to know their concrete types, decoupling the source of an event from its consumers. Spring\'s ApplicationEventPublisher and standard UI event listeners are both Observer in practice.', 'Design patterns'],
    [3, 'Decorator versus inheritance: when do you pick Decorator?', 'Decorator wraps an object at runtime to add behavior, and decorators can be composed in any combination without a new subclass for every combination, unlike inheritance, which is fixed at compile time and multiplies classes as combinations grow. Java\'s IO streams (wrapping an InputStream with a BufferedInputStream, then a GZIPInputStream) are a textbook example.', 'Design patterns'],
    [3, 'Adapter versus Facade: how do they differ?', 'Adapter converts one interface into another interface a client already expects, typically to integrate a legacy or third-party API without changing either side. Facade provides a simplified, unified interface over a complex subsystem with many parts, without necessarily changing any of the underlying interfaces, just hiding their complexity behind one entry point.', 'Design patterns'],
    [3, 'Explain the Proxy pattern and where Spring uses it.', 'A proxy stands in for a real object and controls access to it, adding behavior such as lazy initialization, access control, caching, or logging, transparently to the caller. Spring AOP builds JDK dynamic proxies (for interfaces) or CGLIB proxies (for classes) to implement things like @Transactional and @Cacheable without the target class knowing about it.', 'Design patterns'],
    [2, 'Explain the Template Method pattern.', 'A base class defines the fixed skeleton of an algorithm in a method, deferring specific steps to abstract or overridable methods that subclasses implement. The overall sequence of steps stays fixed and controlled by the base class, while individual steps vary by subclass, useful when several algorithms share the same shape but differ in details.', 'Design patterns'],

    // Practice problems
    [2, 'Design a Parking Lot system. What are the core classes?', 'ParkingLot managing Levels, each Level managing Spots typed by vehicle size (motorcycle, compact, large); a Vehicle class hierarchy; a Ticket issued on entry and settled on exit; and a spot-assignment strategy (nearest available, or by vehicle size). Worth discussing concurrency: two vehicles should not be assigned the same spot simultaneously.', 'Practice problems'],
    [2, 'Design Splitwise (an expense-sharing app). How do you model balances?', 'Model User, Group, and Expense, with a Split strategy per expense (equal, exact amounts, or percentages) determining who owes what. Maintain a Balance ledger between pairs of users, and offer a simplify/settle-up algorithm that nets out chains of debts (A owes B, B owes C) into the minimum number of transactions.', 'Practice problems'],
    [2, 'Design an LRU cache. What is the class design, not just the algorithm?', 'A Cache interface exposing get/put, backed internally by a HashMap<Key, Node> for O(1) lookup and a doubly linked list of nodes ordered by recency for O(1) move-to-front and eviction of the tail. If concurrent access is required, discuss where locking or a concurrent-safe structure is needed around the shared map and list.', 'Practice problems'],
    [3, 'Design an Elevator System.', 'Elevator (current floor, direction, capacity), an ElevatorController/Dispatcher that assigns incoming requests to elevators, and a Request model distinguishing internal (button pressed inside the car) from external (hall call, with a direction) requests. A scheduling strategy such as SCAN/LOOK, continuing in the current direction and serving requests along the way before reversing, avoids starving requests and minimizes unnecessary direction changes.', 'Practice problems'],
    [3, 'Design a Library Management System.', 'Book (catalog metadata) versus BookItem (a physical copy with its own status), Member, a Loan/Reservation model with due dates and fines, and a Catalog supporting search by title/author/ISBN. Worth discussing: what happens when two members try to check out the last copy at the same time, and how holds/reservations are queued.', 'Practice problems'],

    // HLD basics
    [2, 'Walk through the high-level design of a URL shortener.', 'Generate a short code, either from a base62-encoded counter or a hash with collision checking, and store the code-to-long-URL mapping in a fast key-value store. On a request for the short code, look up the mapping and respond with a redirect (301 for permanent, 302 if you want to track each click). Extras to discuss: custom aliases, expiry, and click analytics.', 'HLD basics'],
    [2, 'How would you design a rate limiter?', 'Common algorithms: token bucket (allows bursts up to a bucket size, refills at a steady rate), leaky bucket (smooths bursts into a steady outflow), and fixed or sliding window counters (simplest, but fixed windows allow edge-case bursts at window boundaries). In a distributed setup, counters usually live in a fast shared store like Redis, using atomic operations or a Lua script to avoid race conditions across instances.', 'HLD basics'],
    [3, 'Design a caching layer for a read-heavy service. What are your options and trade-offs?', 'Cache-aside (application checks cache, falls back to the database and populates the cache on a miss) is the most common; write-through updates the cache on every write for consistency at some write latency cost; write-behind batches writes for throughput at the cost of durability risk. Also plan eviction (LRU/LFU), TTLs, and stampede protection (locking or jittered TTLs) so many clients do not all miss and hit the database at once when a hot key expires.', 'HLD basics'],
    [3, 'What does the high-level design of a notification system look like?', 'A notification service sits behind a queue so producers are decoupled from delivery, with per-channel adapters for push, email, and SMS providers. It needs user preference and rate-limiting logic (so users are not spammed), templating for message content, retries with a dead-letter queue for failed sends, and delivery tracking so you know what was actually sent and received.', 'HLD basics'],
    [3, 'How would a load balancer decide which backend server handles a request?', 'Common algorithms: round robin (simple, even rotation), least connections (send to the least busy server), weighted variants of either (accounting for different server capacities), and consistent hashing (routes the same client or key to the same backend, useful for sticky sessions or cache locality). Health checks continuously remove unhealthy backends from rotation. L4 (transport-level, fast, less visibility) versus L7 (application-level, can route on URL/header, more overhead) is a key trade-off to mention.', 'HLD basics'],
  ],
  notes: [
    { title: 'SOLID in Practice', points: [
      'S: one reason to change per class',
      'O: extend via new code, not edits to existing code (polymorphism)',
      'L: subtypes must honor the base type\'s contract, no surprises',
      'I: small, client-specific interfaces over one fat interface',
      'D: depend on abstractions; let a container inject the concrete type',
    ] },
    { title: 'Pattern Picker', points: [
      'Many optional constructor params -> Builder',
      'Need exactly one shared instance -> Singleton (prefer DI-scoped)',
      'Swap an algorithm at runtime -> Strategy',
      'Notify many listeners of a change -> Observer',
      'Add behavior without subclassing every combination -> Decorator',
      'Integrate a mismatched existing API -> Adapter',
      'Hide a complex subsystem behind one entry point -> Facade',
      'Control/intercept access to an object -> Proxy',
    ] },
    { title: 'LLD Interview Checklist', points: [
      '1. Clarify requirements and scope out of bounds explicitly',
      '2. Identify the core entities/nouns and their relationships',
      '3. Define interfaces before implementations',
      '4. Call out concurrency: what can two actors race on?',
      '5. Discuss extensibility: what is likely to change, and does the design absorb it cleanly?',
      '6. Talk through one or two edge cases end to end',
    ] },
  ],
};

export const genai = {
  questions: [
    // Python basics
    [1, 'What Python features make it a good fit for quick GenAI prototyping?', 'Dynamic typing and a huge ecosystem (numpy, pandas, requests) let you move fast without ceremony; every major LLM provider ships a first-class Python SDK; and interactive tools like Jupyter/REPLs make it easy to iterate on a prompt or a pipeline step by step before wiring it into an application.', 'Python basics'],
    [1, 'List versus generator in Python: why does it matter when streaming LLM tokens?', 'A list materializes every value in memory before you can use any of it. A generator, using yield, produces values lazily one at a time as they are requested. Streaming an LLM response token by token is exactly this pattern: each token is yielded and displayed as it arrives, instead of waiting for the full response to buffer.', 'Python basics'],

    // How LLMs work
    [1, 'What is a token, and why do LLMs operate on tokens instead of raw characters or whole words?', 'A token is a subword unit from a fixed vocabulary, produced by an algorithm like byte-pair encoding. Operating on subwords balances vocabulary size against sequence length, and lets the model handle rare or unseen words by splitting them into familiar pieces instead of failing on an out-of-vocabulary word.', 'How LLMs work'],
    [1, 'What is a prompt, and what is the difference between a system prompt and a user prompt?', 'A prompt is the input text given to the model to condition what it generates. The system prompt sets persistent behavior, role, and constraints for the whole conversation (set once, usually by the application), while the user prompt is the specific request or message for that turn.', 'How LLMs work'],
    [2, 'What is an embedding, and what does closeness in embedding space represent?', 'An embedding is a dense numeric vector representation of text (or other data) produced so that semantically similar inputs land close together under a distance metric like cosine similarity. Embeddings are the basis for semantic search, clustering, and retrieval in RAG systems, where you find the stored chunks closest to a query\'s embedding.', 'How LLMs work'],
    [2, 'Explain temperature and top-p sampling.', 'Temperature scales the probability distribution before sampling the next token: low temperature makes the output more deterministic and repetitive, high temperature makes it more random and creative. Top-p (nucleus) sampling instead restricts sampling to the smallest set of tokens whose cumulative probability exceeds p, trimming the unlikely tail regardless of how the distribution is shaped.', 'How LLMs work'],
    [2, 'What is the context window, and what happens when a conversation exceeds it?', 'The context window is the maximum number of tokens, input plus output combined, that a model can attend to in a single call. Once a conversation exceeds it, older content must be truncated, summarized, or moved out to a retrieval system (RAG) and pulled back in only when relevant, rather than kept verbatim in every call.', 'How LLMs work'],
    [3, 'What is the difference between pretraining, fine-tuning, and RLHF/alignment?', 'Pretraining learns general language patterns from a massive corpus of unlabeled text. Fine-tuning further trains the pretrained model on a smaller, labeled dataset to adapt it to a narrower task or domain. RLHF and other alignment techniques shape the model\'s behavior, such as helpfulness and safety, using human feedback as a reward signal rather than a fixed labeled dataset.', 'How LLMs work'],

    // LangChain & LangGraph
    [2, 'What problem does a framework like LangChain solve on top of calling an LLM API directly?', 'It standardizes the common building blocks that almost every LLM application needs: prompt templates, chaining multiple calls together, memory across turns, tool/function calling, and retrieval, so you are not hand-rolling the same glue code from scratch for every project.', 'LangChain & LangGraph'],
    [2, 'What is RAG (Retrieval-Augmented Generation), and why use it instead of fine-tuning?', 'RAG retrieves relevant chunks of text from an external knowledge source, usually a vector store, at query time, and includes them in the prompt as context so the model answers grounded in that data. It is cheaper and faster to keep current than fine-tuning, since updating the knowledge source does not require retraining the model.', 'LangChain & LangGraph'],
    [2, 'Walk through building a simple RAG pipeline over your own PDFs.', 'Split the PDFs into chunks, embed each chunk into a vector, and store the vectors in a vector database. At query time, embed the user\'s question, retrieve the top-k most similar chunks, and pass those chunks along with the question into the prompt so the model answers using that retrieved context.', 'LangChain & LangGraph'],
    [3, 'What is the difference between LangChain and LangGraph, and when would you reach for LangGraph?', 'LangChain composes calls in largely linear or simple DAG-like chains. LangGraph models a workflow as an explicit stateful graph of nodes and edges, which fits cyclic or branching logic better, such as an agent that loops, retries on failure, or waits on a condition before proceeding, which is awkward to express as a straight chain.', 'LangChain & LangGraph'],
    [3, 'How do you keep a chatbot\'s memory manageable across a long conversation?', 'Summarize older turns into a running summary instead of keeping the full transcript, keep only the most recent N turns verbatim, or retrieve only the relevant past turns via embeddings when needed, rather than sending the entire history into the context window on every call.', 'LangChain & LangGraph'],

    // Agentic workflows
    [2, 'What is tool calling (function calling) in the context of an LLM agent?', 'The model is given a schema describing available functions: their names, descriptions, and parameters. Instead of only answering in text, it can emit a structured call to one of them; the application executes that function and feeds the result back to the model so it can continue reasoning or respond using that result.', 'Agentic workflows'],
    [2, 'What is the ReAct pattern for agents?', 'ReAct interleaves Reasoning and Acting: the model thinks about what to do next, takes an action such as a tool call, observes the result, and uses that observation to decide the next reasoning step, repeating in a loop instead of producing a single one-shot answer.', 'Agentic workflows'],
    [3, 'How do you keep a multi-step agent from looping forever or taking a destructive action?', 'Cap the number of steps or iterations per run, require explicit confirmation before any irreversible action, validate tool outputs before feeding them back into the model rather than trusting them blindly, and set timeouts and cost/token budgets per run so a stuck loop fails safely instead of running indefinitely.', 'Agentic workflows'],
    [3, 'How would you design a multi-agent system where agents divide up a task?', 'A planner or orchestrator agent breaks the overall task into subtasks and routes each to a specialized worker agent, such as a researcher, a coder, and a reviewer, then aggregates their outputs. This needs a shared state or message format between agents and a clear way to detect that the overall task is complete rather than looping between agents forever.', 'Agentic workflows'],
    [4, 'What failure modes are unique to agentic systems compared to a single LLM call, and how do you guard against them?', 'Errors can compound across steps, tools can be misused or return unexpected results, loops can run indefinitely, and prompt injection can arrive through tool outputs or retrieved content rather than the user\'s own message. Guard against these with step limits, output validation between steps, sandboxing tool execution, and treating any external content the agent reads as untrusted data rather than as instructions it should follow.', 'Agentic workflows'],

    // Capstone project
    [2, 'What makes a GenAI side project "interview ready" rather than just a demo?', 'It solves a real problem, even a small one, has an architecture you can whiteboard and explain clearly, and handles realistic failure cases such as rate limits or malformed model output. Just as important, you can articulate the trade-offs you made and why, rather than only showing that it works once.', 'Capstone project'],
    [3, 'How do you talk about a RAG or agent project\'s limitations honestly in an interview?', 'Name concrete failure modes you actually observed, such as hallucination on out-of-scope questions or retrieval missing the relevant chunk, and what you would change with more time, such as better chunking or an evaluation harness, rather than presenting the project as finished and flawless, which reads as less credible to an interviewer.', 'Capstone project'],
    [3, 'How would you evaluate whether your GenAI project\'s outputs are actually good?', 'Build a small labeled test set of representative queries with expected answers or grading criteria, score outputs against it, whether by exact match, an LLM-as-judge, or manual review, and track that score as you change prompts or retrieval so improvements are measured rather than judged by feel.', 'Capstone project'],
  ],
  notes: [
    { title: 'LLM Vocabulary', points: [
      'Token: a subword unit the model actually processes',
      'Prompt: the input text conditioning the output (system vs user)',
      'Embedding: a vector where semantic closeness = geometric closeness',
      'Context window: the max tokens (in + out) a call can attend to',
      'Temperature: higher = more random; top-p: sample from the smallest high-probability set',
    ] },
    { title: 'RAG Pipeline', diagram: { type: 'flow', items: ['Chunk documents', 'Embed each chunk', 'Store in a vector DB', 'Embed the question', 'Retrieve top-k chunks', 'Prompt model with question + chunks'] }, points: [
      'Retrieval happens at query time, not training time',
      'Chunk size and overlap matter as much as the model choice',
      'Cheaper and faster to update than fine-tuning',
    ] },
    { title: 'Agent Loop (ReAct)', diagram: { type: 'flow', items: ['Think: what should I do next?', 'Act: call a tool', 'Observe: read the result', 'Repeat until done'] }, points: [
      'Cap max steps so a stuck loop fails safely',
      'Treat tool/retrieved output as untrusted data, not instructions',
      'Validate a tool\'s result before feeding it back into the model',
    ] },
  ],
};
