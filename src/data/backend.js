export const spring = {
  questions: [
    [1, 'What is Inversion of Control and Dependency Injection?', 'IoC: the container, not your code, creates and wires objects. DI is the mechanism: dependencies are supplied via constructor, setter, or field instead of being created with new. It gives loose coupling and easy testing with mocks.'],
    [1, 'What is a Spring Bean and what are the common bean scopes?', 'A bean is an object managed by the Spring IoC container. Scopes: singleton (default, one per container), prototype (new each request), request, session, application, websocket (web-aware).'],
    [1, 'Difference between @Component, @Service, @Repository and @Controller?', 'All are stereotype annotations that register beans via component scanning. @Service marks business logic, @Repository marks data access (adds exception translation), @Controller/@RestController marks web layer. They convey intent and enable layer-specific features.'],
    [2, 'Constructor vs field vs setter injection - which is best?', 'Constructor injection: immutable dependencies, mandatory deps guaranteed, easy unit testing without Spring, detects circular dependencies early. Field injection hides dependencies and hampers testing. Setter injection suits optional dependencies.'],
    [2, 'Explain the Spring bean lifecycle.', 'Instantiate -> populate dependencies -> Aware callbacks -> BeanPostProcessor before-init -> @PostConstruct / InitializingBean / init-method -> BeanPostProcessor after-init (proxies created here) -> ready -> on shutdown @PreDestroy / DisposableBean.'],
    [2, 'What is AOP and where does Spring use it?', 'Aspect-Oriented Programming separates cross-cutting concerns (logging, security, transactions) using aspects, pointcuts and advice. Spring implements it with proxies (JDK dynamic or CGLIB). @Transactional, @Async, @Cacheable and Spring Security method security are all AOP-based.'],
    [2, 'How does @Transactional work and what are its pitfalls?', 'A proxy wraps the bean, opens a transaction before the method and commits/rolls back after. Rolls back on unchecked exceptions by default. Pitfalls: self-invocation bypasses the proxy, private/final methods are not proxied, checked exceptions do not roll back without rollbackFor, and wrong propagation settings.'],
    [2, 'Explain transaction propagation levels.', 'REQUIRED (default: join or create), REQUIRES_NEW (suspend and start new), SUPPORTS, NOT_SUPPORTED, MANDATORY (must exist), NEVER, NESTED (savepoint). REQUIRES_NEW is typical for audit logging that must commit independently.'],
    [3, 'How do you resolve circular dependencies in Spring?', 'Best: redesign to break the cycle (extract a third component or use events). Workarounds: @Lazy injection, setter injection, or ObjectProvider. Spring Boot 2.6+ prohibits circular references by default.'],
    [3, 'What is the difference between BeanFactory and ApplicationContext?', 'BeanFactory is the basic lazy container. ApplicationContext extends it with eager singleton initialization, event publishing, i18n, resource loading, environment/profiles, and AOP integration. Use ApplicationContext.'],
    [3, 'Spring MVC request flow?', 'Request -> DispatcherServlet -> HandlerMapping finds controller -> HandlerAdapter invokes it (argument resolvers, validation) -> return value handled by message converters (JSON) or ViewResolver -> response. Filters/interceptors wrap the flow.'],
    [3, 'JPA/Hibernate: explain the N+1 problem and solutions.', 'Loading N parent entities triggers N extra queries for lazy children. Fix with JOIN FETCH, @EntityGraph, batch fetching (@BatchSize), DTO projections, or query design. Detect with SQL logging or tools like Hibernate Statistics.'],
    [4, 'How do you design a modular, testable Spring application at enterprise scale?', 'Layered/hexagonal architecture with domain free of framework code, constructor injection, clear module boundaries (Spring Modulith or Maven modules), profiles and externalized config, slice tests (@WebMvcTest, @DataJpaTest) plus Testcontainers integration tests, observability via Micrometer, and architecture tests (ArchUnit).'],
  ],
  notes: [
    { title: 'IoC Container', diagram: { type: 'flow', items: ['Config / annotations', 'IoC Container', 'Beans created', 'Injected into you'] }, points: ['You declare, Spring wires', 'Prefer constructor injection', 'Singleton scope is default'] },
    { title: 'Bean Lifecycle', diagram: { type: 'flow', items: ['Instantiate', 'Inject', '@PostConstruct', 'In use', '@PreDestroy'] }, points: ['BeanPostProcessors create proxies', 'Prototype beans: Spring does not call destroy'] },
    { title: '@Transactional Gotchas', points: ['Works via proxy: self-call is NOT intercepted', 'Only public methods (proxy)', 'Rolls back on RuntimeException by default', 'Use rollbackFor for checked', 'readOnly=true for queries'], code: '@Transactional(rollbackFor = Exception.class)\npublic void transfer(long a, long b, BigDecimal amt) {\n   debit(a, amt); credit(b, amt);\n}' },
  ],
};

export const springboot = {
  questions: [
    [1, 'What does Spring Boot provide over plain Spring?', 'Auto-configuration, starter dependencies, embedded servers (Tomcat/Jetty/Undertow), production-ready Actuator, opinionated defaults, externalized configuration, and easy packaging as an executable jar - minimal XML/boilerplate.'],
    [1, 'What does @SpringBootApplication include?', '@Configuration + @EnableAutoConfiguration + @ComponentScan (scanning the package of the annotated class and below).'],
    [1, 'What are Spring Boot starters?', 'Curated dependency descriptors like spring-boot-starter-web or -data-jpa that bring compatible libraries and trigger the matching auto-configuration.'],
    [2, 'How does auto-configuration work?', 'Boot reads auto-configuration classes listed in META-INF/spring/...AutoConfiguration.imports and applies those whose @Conditional checks pass (@ConditionalOnClass, @ConditionalOnMissingBean, @ConditionalOnProperty). Your own beans take precedence. Debug with --debug or the conditions report.'],
    [2, 'How do you manage configuration and profiles?', 'application.properties/yml with profile files (application-prod.yml), activated by spring.profiles.active. Precedence: command line > env vars > profile files > defaults. Bind with @ConfigurationProperties (typed, validated) rather than scattered @Value.'],
    [2, 'What is Spring Boot Actuator?', 'Production endpoints for health, metrics, info, env, loggers, threaddump, etc. (/actuator/health). Integrates with Micrometer for Prometheus/CloudWatch. Secure and expose only what is needed.'],
    [2, 'How do you handle exceptions globally in a REST API?', '@RestControllerAdvice with @ExceptionHandler methods returning consistent error bodies (RFC 7807 ProblemDetail in Spring 6). Map domain exceptions to status codes, hide internals, and include a correlation id.'],
    [2, 'How do you test Spring Boot apps?', 'Unit tests with JUnit + Mockito (no Spring), slice tests (@WebMvcTest, @DataJpaTest), full @SpringBootTest with Testcontainers for real DBs, MockMvc/WebTestClient for endpoints, and @MockBean/@MockitoBean for collaborators.'],
    [3, 'How do you secure a Spring Boot API with JWT/OAuth2?', 'Use Spring Security as an OAuth2 Resource Server validating JWTs from an IdP (Cognito, Keycloak, Auth0): SecurityFilterChain, stateless sessions, scope/role based authorization, method security (@PreAuthorize), CORS/CSRF config appropriate for token APIs, and short-lived tokens with refresh.'],
    [3, 'How would you tune a Spring Boot service for startup time and memory?', 'Lazy initialization for rarely used beans, trim unused starters, class data sharing/AOT, GraalVM native image (Spring Boot 3), right-size JVM in containers (-XX:MaxRAMPercentage), connection pool sizing, and virtual threads (spring.threads.virtual.enabled) for IO-heavy loads.'],
    [3, 'Explain Spring Boot 3 changes: Jakarta EE and Java 17 baseline.', 'Spring Boot 3 requires Java 17+, migrates javax.* to jakarta.*, uses Spring Framework 6, supports GraalVM native images, adds Micrometer observation/tracing, and ProblemDetail support. Upgrade via 2.7 first with the migration tool.'],
    [4, 'How do you decompose a monolith into Spring Boot microservices and what pitfalls do you avoid?', 'Start with a modular monolith and bounded contexts, extract services around business capabilities with own datastores, use API gateway, async messaging (events) for integration, resilience (Resilience4j circuit breakers, retries, timeouts), distributed tracing, contract testing, and avoid a distributed monolith by minimizing synchronous chains.'],
  ],
  notes: [
    { title: 'How Boot Starts', diagram: { type: 'flow', items: ['main()', 'SpringApplication.run', 'Auto-config', 'Embedded Tomcat'] }, points: ['@SpringBootApplication = Config + AutoConfig + Scan', 'Executable fat jar', 'Beans conditional on classpath'] },
    { title: 'Config Precedence', diagram: { type: 'stack', items: ['Command-line args', 'Environment variables', 'application-{profile}.yml', 'application.yml', 'Defaults'] }, points: ['Higher wins', 'Use @ConfigurationProperties for typed config'] },
    { title: 'Minimal REST Controller', points: ['@RestController = @Controller + @ResponseBody', 'Validate with @Valid', 'Return ResponseEntity for status control'], code: '@RestController\n@RequestMapping("/api/users")\nclass UserController {\n  @GetMapping("/{id}")\n  UserDto get(@PathVariable long id) {\n    return service.find(id);\n  }\n}' },
  ],
};

export const maven = {
  questions: [
    [1, 'What is Maven and what problems does it solve?', 'A build and dependency management tool that standardizes project structure, resolves dependencies (with transitive ones) from repositories, and runs a defined lifecycle (compile, test, package) via the POM.'],
    [1, 'What is the pom.xml and what are the coordinates of an artifact?', 'The Project Object Model describing the project, dependencies, plugins and build config. Coordinates: groupId, artifactId, version (plus packaging/classifier) uniquely identify an artifact.'],
    [1, 'Explain the Maven default lifecycle phases.', 'validate, compile, test, package, verify, install (to local ~/.m2), deploy (to remote repo). Running a phase runs all previous ones. Other lifecycles: clean and site.'],
    [2, 'Difference between compile, provided, runtime and test dependency scopes?', 'compile (default, everywhere), provided (needed to compile, supplied by runtime, e.g. servlet-api), runtime (not needed to compile, needed to run, e.g. JDBC driver), test (only test classpath), import (BOM in dependencyManagement).'],
    [2, 'dependencyManagement vs dependencies?', 'dependencyManagement declares versions/scopes centrally without adding the dependency; child modules then declare dependencies without versions. Used with parent POMs and BOMs to keep versions consistent.'],
    [2, 'How do you resolve dependency conflicts?', 'Maven picks the nearest definition wins (then first declared). Inspect with mvn dependency:tree, pin with dependencyManagement, exclude transitive artifacts with <exclusions>, and enforce with maven-enforcer-plugin (dependencyConvergence).'],
    [2, 'What are Maven profiles and how are they used?', 'Conditional build configuration activated by -P, property, JDK, or OS (dev/test/prod settings, skipping tests, different repos). Keep secrets out of the POM and use settings.xml or CI variables.'],
    [3, 'Multi-module builds and parent POM: how do you structure them?', 'A parent POM (packaging pom) lists <modules> and centralizes properties, dependencyManagement and pluginManagement. Reactor orders module builds by dependencies; use -pl and -am to build subsets and -T for parallel builds.'],
    [3, 'How do you speed up Maven builds in CI?', 'Cache ~/.m2, parallel builds (-T 1C), incremental/module selection (-pl -am), skip unnecessary steps (-DskipTests only for packaging stages), use a private repository mirror (Nexus/Artifactory), and split unit vs integration tests (surefire vs failsafe).'],
    [4, 'How do you manage releases and versioning across many services?', 'Semantic versioning, maven-release-plugin or CI-friendly versions (${revision}) with flatten plugin, immutable release artifacts in Nexus/Artifactory, BOMs for shared dependency sets, SBOM and vulnerability scanning (OWASP dependency-check), and automated dependency updates (Dependabot/Renovate).'],
  ],
  notes: [
    { title: 'Maven Lifecycle', diagram: { type: 'flow', items: ['validate', 'compile', 'test', 'package', 'verify', 'install', 'deploy'] }, points: ['Each phase runs earlier ones', 'install = local repo, deploy = remote repo', 'mvn clean package'] },
    { title: 'POM Skeleton', points: ['Coordinates: group / artifact / version', 'dependencyManagement for versions', 'Scopes: compile, provided, runtime, test'], code: '<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-web</artifactId>\n</dependency>' },
    { title: 'Handy Commands', points: ['mvn dependency:tree', 'mvn -pl api -am package', 'mvn -T 1C -DskipTests package', 'mvn versions:display-dependency-updates'] },
  ],
};

export const restapi = {
  questions: [
    [1, 'What is REST and what are its constraints?', 'Representational State Transfer: client-server, stateless, cacheable, uniform interface (resources with URIs, standard methods, representations, HATEOAS), layered system, and optional code on demand.'],
    [1, 'Explain HTTP methods and which are idempotent/safe.', 'GET (safe, idempotent), HEAD, OPTIONS (safe), PUT (idempotent replace), DELETE (idempotent), POST (not idempotent, create/action), PATCH (partial update, not guaranteed idempotent).'],
    [1, 'Common HTTP status codes and when to use them?', '200 OK, 201 Created (+Location), 204 No Content, 400 Bad Request, 401 Unauthorized (unauthenticated), 403 Forbidden, 404 Not Found, 409 Conflict, 422 Unprocessable, 429 Too Many Requests, 500 Internal Error, 502/503/504 upstream issues.'],
    [2, 'PUT vs PATCH vs POST?', 'POST creates (or triggers actions) where the server assigns identity. PUT replaces the whole resource at a known URI and is idempotent. PATCH modifies part of a resource (JSON Patch / merge patch).'],
    [2, 'How do you design REST URLs and versioning?', 'Nouns for resources, plural collections, hierarchy for relationships (/users/42/orders), query params for filtering/sorting/pagination. Version via URI (/v1), header, or media type - pick one and support deprecation policies.'],
    [2, 'How do you implement pagination, filtering and sorting?', 'Offset (page/size) is simple but slow on deep pages and unstable; cursor/keyset pagination scales and stays consistent. Return metadata (total, next cursor) and use Link headers or envelope. Whitelist sortable fields.'],
    [2, 'What does idempotency mean and how do you make POST safe to retry?', 'Repeating a request has the same effect as one. Use an Idempotency-Key header stored server-side with the result so retries return the original response instead of creating duplicates (payments, orders).'],
    [2, 'Authentication vs authorization; how do you secure REST APIs?', 'Authentication = who you are, authorization = what you may do. Use OAuth2/OIDC with JWT bearer tokens over HTTPS, scopes/roles, input validation, rate limiting, CORS restrictions, and never put secrets in URLs.'],
    [3, 'REST vs GraphQL vs gRPC?', 'REST: simple, cacheable, ubiquitous, but can over/under-fetch. GraphQL: client-shaped queries, single endpoint, harder caching and cost control. gRPC: binary protobuf over HTTP/2, strongly typed, great for internal service-to-service and streaming.'],
    [3, 'How do you document and evolve an API without breaking clients?', 'OpenAPI/Swagger specs as source of truth, contract tests (Pact), additive changes only (new optional fields), tolerant readers, deprecation headers and timelines, and version bumps for breaking changes. Lint specs in CI.'],
    [3, 'How do you handle rate limiting, caching and ETags?', 'Rate limit with token bucket at gateway (429 + Retry-After). Cache with Cache-Control, ETag/If-None-Match (304) and CDN for GETs. Use conditional requests with If-Match for optimistic concurrency on updates.'],
    [4, 'Design a resilient public API platform: what do you put in place?', 'API gateway (auth, throttling, routing), consistent error model, observability (correlation ids, RED metrics), timeouts/retries/circuit breakers on downstreams, idempotency, versioning strategy, SDKs and portal docs, SLAs/SLOs, and security testing with WAF and quotas per client.'],
  ],
  notes: [
    { title: 'HTTP Methods', points: ['GET read (safe)', 'POST create', 'PUT replace (idempotent)', 'PATCH partial update', 'DELETE remove (idempotent)'], diagram: { type: 'flow', items: ['Client', 'HTTP request', 'API', 'JSON response'] } },
    { title: 'Status Code Families', diagram: { type: 'stack', items: ['2xx success', '3xx redirect', '4xx client error', '5xx server error'] }, points: ['201 + Location on create', '401 vs 403: who vs what', '429 rate limited'] },
    { title: 'Good Endpoint Design', points: ['/users, /users/42, /users/42/orders', 'Filter: ?status=open&sort=-date', 'Cursor pagination: ?after=abc&limit=20', 'Consistent error JSON'], code: '{\n  "type": "validation_error",\n  "title": "Invalid input",\n  "status": 422,\n  "errors": [{"field":"email","msg":"required"}]\n}' },
  ],
};
