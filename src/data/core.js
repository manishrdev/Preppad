// Question format: [level, question, answer]  level 1=Junior 2=Mid 3=Senior 4=Lead
// Note format: { title, points: [], code?, diagram? }

export const java = {
  questions: [
    [1, 'What is the difference between JDK, JRE and JVM?', 'JVM executes bytecode and provides memory management/GC. JRE = JVM + core class libraries needed to run programs. JDK = JRE + developer tools (javac, jar, jdb, jlink). You install the JDK to develop, the JRE (or just a runtime image) to run.'],
    [1, 'Explain == vs equals() for Strings. What is the String pool?', '== compares references; equals() compares content. String literals are interned in the String pool (heap since Java 7) so "a" == "a" is true, but new String("a") == "a" is false. intern() returns the pooled instance.'],
    [1, 'Why is String immutable and what are the benefits?', 'Immutability makes Strings thread-safe, safe as HashMap keys (hashCode can be cached), enables the String pool, and improves security (class names, file paths, DB URLs cannot be altered after validation). Use StringBuilder for heavy concatenation, StringBuffer if synchronization is needed.'],
    [1, 'Difference between checked and unchecked exceptions?', 'Checked exceptions (extend Exception but not RuntimeException) must be caught or declared with throws - the compiler enforces it (IOException, SQLException). Unchecked (RuntimeException, Error) are not enforced (NullPointerException, IllegalArgumentException). Use checked for recoverable conditions, unchecked for programming errors.'],
    [1, 'What are final, finally and finalize?', 'final: constant variable / non-overridable method / non-extendable class. finally: block that always runs after try/catch (except JVM exit or crash) used for cleanup. finalize(): deprecated Object hook called before GC; do not use - prefer try-with-resources or Cleaner.'],
    [2, 'Explain the contract between equals() and hashCode(). What breaks if you violate it?', 'If a.equals(b) then a.hashCode() == b.hashCode(). Equal objects must land in the same bucket. Violating it makes HashMap/HashSet fail to find objects that "should" be equal (duplicates in sets, get() returning null). Also equals must be reflexive, symmetric, transitive, consistent.'],
    [2, 'How does HashMap work internally?', 'An array of buckets; index = (n-1) & hash(key). Collisions are chained as linked lists, converted to red-black trees when a bucket exceeds 8 entries (and table >= 64). Resize doubles capacity at load factor 0.75, rehashing entries. Average get/put O(1); worst case O(log n) with treeification. Not thread-safe.'],
    [2, 'HashMap vs ConcurrentHashMap vs Hashtable vs Collections.synchronizedMap?', 'Hashtable and synchronizedMap lock the whole map (coarse). ConcurrentHashMap uses CAS and per-bin locking (Java 8+) allowing concurrent reads and mostly non-blocking writes; no null keys/values; iterators are weakly consistent. Prefer ConcurrentHashMap for concurrent access.'],
    [2, 'What is the difference between an ArrayList and a LinkedList? When would you use each?', 'ArrayList: dynamic array, O(1) random access, amortized O(1) append, O(n) middle insert/remove, cache-friendly. LinkedList: doubly linked, O(1) insert/remove at known node/ends but O(n) access, high per-node memory. In practice ArrayList wins almost always; ArrayDeque beats LinkedList as a queue.'],
    [2, 'Explain how the Java Memory Model divides memory (heap, stack, metaspace).', 'Heap: objects, shared, garbage collected (young gen: Eden + survivors; old gen). Stack: per-thread frames with locals and references. Metaspace: class metadata in native memory (replaced PermGen in Java 8). Also program counter and native stacks. StackOverflowError = deep recursion; OutOfMemoryError = heap/metaspace exhausted.'],
    [3, 'Compare G1, ZGC and Parallel GC. How do you choose and tune?', 'Parallel: max throughput, long stop-the-world pauses - batch jobs. G1 (default since 9): region-based, predictable pauses via -XX:MaxGCPauseMillis, good general choice. ZGC/Shenandoah: concurrent, sub-millisecond pauses on huge heaps - latency-sensitive services. Tune by measuring with GC logs (-Xlog:gc*), sizing heap, and fixing allocation rates before touching flags.'],
    [3, 'Explain volatile, synchronized and the happens-before relationship.', 'volatile guarantees visibility and ordering of a variable but not atomicity of compound operations (i++). synchronized gives mutual exclusion plus visibility (unlock happens-before subsequent lock). Happens-before is the JMM rule set that defines when writes by one thread are guaranteed visible to another (locks, volatile, thread start/join, final field freezing).'],
    [3, 'How would you diagnose a memory leak in a production Java service?', 'Watch heap after full GC trending upward; capture a heap dump (jcmd GC.heap_dump / -XX:+HeapDumpOnOutOfMemoryError) and analyze dominators in Eclipse MAT or VisualVM. Typical culprits: static collections, unbounded caches, listeners not deregistered, ThreadLocals in pools, unclosed resources. Confirm fix with load test and JFR.'],
    [3, 'ExecutorService vs ForkJoinPool vs CompletableFuture - when to use which?', 'ExecutorService: fixed/cached thread pools for independent tasks (size by CPU vs IO-bound). ForkJoinPool: divide-and-conquer with work stealing (parallel streams). CompletableFuture: composing async pipelines (thenApply, thenCombine) with explicit executors. Never block the common pool; give IO tasks dedicated pools.'],
    [4, 'How do you design for high throughput and low latency on the JVM at scale?', 'Reduce allocation and object churn, choose the right GC and heap size, use bounded thread pools and backpressure, avoid lock contention (striping, immutable data, lock-free structures), pool connections, batch IO, profile with JFR/async-profiler, warm up JIT, set container-aware flags, and define SLOs with load tests before optimizing.'],
    [4, 'Virtual threads (Project Loom): what problem do they solve and what are the pitfalls?', 'They make blocking IO cheap: millions of lightweight threads scheduled onto carrier threads, enabling thread-per-request style without reactive complexity. Pitfalls: pinning inside synchronized blocks/native calls (improved in later releases), ThreadLocal overuse, CPU-bound work gains nothing, and downstream limits (DB pool) still need semaphores.'],
  ],
  notes: [
    { title: 'JVM Memory Layout', diagram: { type: 'stack', items: ['Metaspace (class metadata)', 'Heap: Old Gen', 'Heap: Young (Eden + S0/S1)', 'Thread Stacks (frames, locals)'] }, points: ['Heap is shared and garbage collected', 'Each thread has its own stack', 'Objects are born in Eden, survivors get promoted to Old', 'StackOverflowError = too deep; OutOfMemoryError = no space'] },
    { title: 'Collections Cheat-Sheet', points: ['ArrayList: fast get, slow middle insert', 'HashMap: O(1) avg, unordered, 1 null key', 'LinkedHashMap: insertion order (LRU with accessOrder)', 'TreeMap: sorted, O(log n)', 'ConcurrentHashMap: thread safe, no nulls', 'PriorityQueue: binary heap, poll = min'], code: 'Map<String,Integer> lru =\n  new LinkedHashMap<>(16, .75f, true) {\n    protected boolean removeEldestEntry(Map.Entry e){\n      return size() > 100;\n    }\n  };' },
    { title: 'Exceptions at a Glance', diagram: { type: 'flow', items: ['Throwable', 'Exception', 'RuntimeException', 'NPE / IAE'] }, points: ['Error = JVM problems, do not catch', 'Checked = must handle', 'Unchecked = bugs', 'Use try-with-resources for AutoCloseable'] },
  ],
};

export const oops = {
  questions: [
    [1, 'What are the four pillars of OOP?', 'Encapsulation (hide state behind methods), Abstraction (expose what, hide how), Inheritance (reuse via is-a), Polymorphism (one interface, many behaviours - overloading at compile time, overriding at run time).'],
    [1, 'Difference between abstract class and interface?', 'Abstract class: can hold state, constructors, and partial implementation; single inheritance. Interface: a contract, multiple implementation allowed, no instance state; since Java 8 supports default/static methods. Use interfaces for capabilities, abstract classes for shared base behaviour.'],
    [1, 'Method overloading vs overriding?', 'Overloading: same name, different parameters, resolved at compile time within a class. Overriding: subclass redefines a superclass method with the same signature, resolved at run time via dynamic dispatch. Overriding cannot reduce visibility or throw broader checked exceptions.'],
    [1, 'What is encapsulation and how do you achieve it in Java?', 'Bundling data with the methods that operate on it and restricting direct access: private fields, public getters/setters only where needed, and validation inside setters. It protects invariants and lets you change internals without breaking callers.'],
    [2, 'Composition vs inheritance - which do you prefer and why?', 'Prefer composition: inheritance couples subclass to superclass internals (fragile base class) and exposes the parent API. Composition (has-a) is more flexible, testable, and supports swapping behaviour at runtime. Use inheritance only for true is-a relationships.'],
    [2, 'Explain the SOLID principles with a one-line example each.', 'S: a class has one reason to change (split InvoicePrinter from Invoice). O: extend behaviour without modifying code (strategy). L: subtypes must be substitutable (Square breaking Rectangle). I: small focused interfaces. D: depend on abstractions, inject implementations.'],
    [2, 'What is polymorphism at runtime? What is dynamic dispatch?', 'A reference of a parent type can hold a child object; the JVM picks the overridden method based on the actual object type via the vtable. Fields and static methods are not polymorphic - they resolve by reference type.'],
    [2, 'What is the diamond problem and how does Java handle it?', 'Ambiguity when a class inherits the same method from two parents. Java forbids multiple class inheritance; for interface default methods the class must override and can call X.super.method() to choose.'],
    [3, 'Name common design patterns you have used and where they fit.', 'Singleton (config/registry - prefer DI-managed), Factory (create objects by type), Builder (many optional params), Strategy (swap algorithms), Observer (events), Decorator (add behaviour, e.g. Java IO), Adapter (integrate legacy API), Proxy (Spring AOP/transactions), Template Method.'],
    [3, 'How do you make a Singleton thread-safe? What are the pitfalls?', 'Best: enum singleton, or Bill Pugh holder idiom (static inner class lazy load). Double-checked locking needs a volatile field. Pitfalls: reflection, serialization and multiple classloaders can break naive singletons; global state hurts testing - prefer DI scope singleton.'],
    [3, 'What is the Liskov Substitution Principle violation you have seen in real code?', 'A subclass that throws UnsupportedOperationException for an inherited method (e.g. read-only list extending a mutable list), or strengthens preconditions. Fix by splitting interfaces or using composition so callers never need instanceof checks.'],
    [4, 'How do you apply Domain-Driven Design tactical patterns on top of OOP?', 'Model Entities (identity), Value Objects (immutable, equality by value), Aggregates with a root guarding invariants and transaction boundaries, Repositories as collection-like interfaces, Domain Services for cross-aggregate logic, and Domain Events. Keep the domain free of framework code.'],
  ],
  notes: [
    { title: 'The 4 Pillars', diagram: { type: 'flow', items: ['Encapsulation', 'Abstraction', 'Inheritance', 'Polymorphism'] }, points: ['Encapsulation = protect state', 'Abstraction = hide complexity', 'Inheritance = is-a reuse', 'Polymorphism = many forms'] },
    { title: 'SOLID in 5 Lines', points: ['S - Single responsibility', 'O - Open for extension, closed for modification', 'L - Liskov substitution', 'I - Interface segregation', 'D - Dependency inversion (depend on abstractions)'] },
    { title: 'Abstract Class vs Interface', points: ['Abstract: state + constructors, single parent', 'Interface: contract, many allowed', 'Default methods (Java 8) let interfaces evolve', 'Prefer interface + composition'], code: 'interface Shape { double area(); }\nrecord Circle(double r) implements Shape {\n  public double area(){ return Math.PI*r*r; }\n}' },
  ],
};

export const ds = {
  questions: [
    [1, 'Array vs linked list - compare complexity.', 'Array: O(1) index access, O(n) insert/delete in the middle, contiguous memory. Linked list: O(n) access, O(1) insert/delete given the node, extra pointer memory, poor cache locality.'],
    [1, 'Stack vs queue? Give real uses.', 'Stack is LIFO (undo, call stack, DFS, expression parsing). Queue is FIFO (task scheduling, BFS, buffering). Deque supports both ends. In Java use ArrayDeque for both.'],
    [1, 'What is Big-O notation? Rank O(1), O(log n), O(n), O(n log n), O(n^2).', 'Big-O describes how time or space grows with input size in the worst case, ignoring constants. Order: O(1) < O(log n) < O(n) < O(n log n) < O(n^2). Binary search is O(log n), good sorts are O(n log n), nested loops O(n^2).'],
    [1, 'How does binary search work and what is its precondition?', 'Repeatedly halve a sorted range by comparing the middle element with the target, discarding half each step: O(log n). Requires random access on sorted data. Beware of integer overflow in mid = lo + (hi - lo) / 2.'],
    [2, 'Explain how a hash table handles collisions.', 'Chaining stores colliding entries in a list/tree per bucket; open addressing probes for the next free slot (linear, quadratic, double hashing). Good hash functions and a load factor around 0.75 keep operations O(1) on average.'],
    [2, 'BFS vs DFS - when to use each?', 'BFS uses a queue and explores level by level - shortest path in unweighted graphs. DFS uses a stack/recursion - cycle detection, topological sort, connected components, backtracking. Both are O(V + E).'],
    [2, 'Explain quicksort vs mergesort.', 'Quicksort: in-place, average O(n log n), worst O(n^2) with bad pivots, unstable, cache friendly. Mergesort: guaranteed O(n log n), stable, needs O(n) extra space, good for linked lists and external sorting. Java uses dual-pivot quicksort for primitives and TimSort for objects.'],
    [2, 'What is a heap / priority queue and where is it used?', 'A complete binary tree stored in an array where each parent is <= (min-heap) its children. Insert and extract-min are O(log n), peek O(1). Used for top-K, scheduling, Dijkstra, merging sorted streams.'],
    [2, 'How would you detect a cycle in a linked list?', "Floyd's tortoise and hare: a slow pointer moves 1 step, a fast pointer 2. If they meet there is a cycle; O(n) time, O(1) space. Alternative: HashSet of visited nodes, O(n) space."],
    [3, 'Balanced trees: compare AVL, red-black and B-trees.', 'AVL: strictly balanced, faster lookups, more rotations on writes. Red-black: looser balance, cheaper inserts/deletes - used in TreeMap/HashMap bins. B-trees: wide nodes minimizing disk reads - databases and file systems.'],
    [3, 'Explain dynamic programming with an example and how you spot a DP problem.', 'DP solves problems with overlapping subproblems and optimal substructure by storing results (memoization top-down or tabulation bottom-up). Example: coin change dp[a] = min(dp[a-c]+1). Signals: "count ways", "min/max cost", choices with repeated states.'],
    [3, 'Design an LRU cache with O(1) get and put.', 'Combine a HashMap (key -> node) with a doubly linked list ordered by recency. On get/put move the node to the head; on capacity overflow evict the tail. In Java, LinkedHashMap with accessOrder=true and removeEldestEntry does it.'],
    [4, 'How do you choose data structures for a system that needs top-K trending items over a sliding window at scale?', 'Use a count-min sketch or hash map for counts per time bucket, a min-heap of size K for the leaders, and expire old buckets (ring buffer). Distribute by key partitioning, merge partial top-K, and accept approximation (probabilistic structures like HyperLogLog) when exactness is costly.'],
  ],
  notes: [
    { title: 'Big-O Cheat Sheet', points: ['Array access O(1), search O(n)', 'HashMap get/put O(1) avg', 'TreeMap O(log n)', 'Binary search O(log n)', 'Sorting O(n log n)', 'BFS/DFS O(V+E)'] },
    { title: 'Graph Traversals', diagram: { type: 'flow', items: ['Start node', 'Queue / Stack', 'Visit neighbours', 'Mark visited'] }, points: ['BFS = queue = shortest path (unweighted)', 'DFS = stack = paths, cycles, topo sort', 'Always track visited to avoid loops'], code: 'Queue<Integer> q = new ArrayDeque<>();\nq.add(start); seen.add(start);\nwhile(!q.isEmpty()){\n  int u = q.poll();\n  for(int v: adj[u]) if(seen.add(v)) q.add(v);\n}' },
    { title: 'Two-Pointer & Sliding Window', points: ['Sorted array pair sum: left/right pointers', 'Longest substring w/o repeat: window + set', 'Fast/slow pointers for cycles & midpoints', 'Turns O(n^2) into O(n)'] },
  ],
};

export const javaVersions = {
  questions: [
    [1, 'What major features did Java 8 introduce?', 'Lambdas and functional interfaces, Stream API, Optional, default and static interface methods, method references, the java.time API, CompletableFuture, and Nashorn (later removed).'],
    [1, 'What is a functional interface? Name built-in ones.', 'An interface with exactly one abstract method, usable as a lambda target and optionally annotated @FunctionalInterface. Built-ins: Function<T,R>, Predicate<T>, Consumer<T>, Supplier<T>, BiFunction, UnaryOperator, Runnable, Comparator.'],
    [2, 'What is the Stream API? Difference between intermediate and terminal operations?', 'A declarative pipeline over a data source. Intermediate operations (filter, map, sorted) are lazy and return a stream; terminal operations (collect, forEach, reduce, count) trigger the pipeline. A stream is single-use. Parallel streams use the common ForkJoinPool - use only for CPU-heavy, stateless work.'],
    [2, 'Explain Optional and its misuse.', 'Optional<T> models a possibly absent return value. Use map/flatMap/orElse/orElseThrow instead of isPresent()+get(). Do not use it for fields, parameters, or collections (return empty collections).'],
    [2, 'What did Java 9-11 add that developers use daily?', 'Java 9: modules (JPMS), List.of/Map.of factories, jshell. Java 10: var local type inference. Java 11 (LTS): HttpClient, String methods (isBlank, lines, strip, repeat), running single-file programs, removal of Java EE modules.'],
    [3, 'Java 17 (LTS) highlights?', 'Records (final), sealed classes, pattern matching for instanceof, text blocks, switch expressions, helpful NullPointerException messages, strong encapsulation of JDK internals, new macOS rendering pipeline, and removal of the applet API path.'],
    [3, 'What are records and sealed classes and why do they matter?', 'Records are concise immutable data carriers with generated constructor, accessors, equals, hashCode, toString. Sealed classes/interfaces restrict which classes may extend them (permits), enabling exhaustive switch over a closed hierarchy - together they model algebraic data types.'],
    [3, 'What is new in Java 21 (LTS)?', 'Virtual threads (final), pattern matching for switch and record patterns (final), sequenced collections (getFirst/getLast/reversed), generational ZGC, string templates (preview), and unnamed patterns (preview).'],
    [4, 'How do you plan a Java version upgrade (8 -> 17 -> 21) for a large system?', 'Inventory dependencies and build plugins, run jdeps/jdeprscan, upgrade incrementally to LTS steps, fix illegal reflective access (--add-opens temporarily), update frameworks (Spring Boot 3 needs Java 17 and jakarta.*), run the full test and performance suite on the new JVM, roll out canary, and update CI images and container base images.'],
  ],
  notes: [
    { title: 'Java Version Timeline', diagram: { type: 'flow', items: ['8: Lambdas', '11: HttpClient', '17: Records', '21: Virtual threads'] }, points: ['LTS = 8, 11, 17, 21', 'Java 8 -> functional style', 'Java 17 -> data modelling (records/sealed)', 'Java 21 -> concurrency (Loom)'] },
    { title: 'Modern Java Snippets', points: ['var for local inference', 'Text blocks for JSON/SQL', 'Switch expressions', 'Pattern matching'], code: 'record Point(int x, int y) {}\nString d = switch (obj) {\n  case Point(int x, int y) -> x + "," + y;\n  case String s -> s;\n  default -> "?";\n};' },
    { title: 'Streams Pipeline', diagram: { type: 'flow', items: ['Source', 'filter', 'map', 'collect'] }, points: ['Lazy until terminal op', 'Single use', 'Prefer groupingBy / partitioningBy collectors'] },
  ],
};

export const python = {
  questions: [
    [1, 'Difference between list, tuple, set and dict?', 'List: ordered, mutable. Tuple: ordered, immutable, hashable if contents are. Set: unordered unique elements, O(1) membership. Dict: key-value mapping, insertion ordered since 3.7, O(1) average lookup.'],
    [1, 'What are *args and **kwargs?', '*args collects extra positional arguments into a tuple; **kwargs collects extra keyword arguments into a dict. Useful for wrappers, decorators and flexible APIs. The same syntax unpacks when calling.'],
    [1, 'Explain mutable vs immutable types and the default-argument trap.', 'Lists, dicts, sets are mutable; int, str, tuple, frozenset are immutable. A mutable default argument (def f(x, acc=[])) is created once at definition time and shared between calls - use None and create inside the function.'],
    [2, 'What are decorators and how do they work?', 'A decorator is a callable that takes a function and returns a replacement, applied with @syntax. Use functools.wraps to preserve metadata. Common uses: logging, caching (lru_cache), auth, retry, timing.'],
    [2, 'Explain generators and yield. Why use them?', 'A generator function returns a lazy iterator, producing values on demand and preserving local state between yields. It saves memory for large or infinite sequences and enables pipeline processing.'],
    [2, 'What is the GIL and how does it affect concurrency?', 'The Global Interpreter Lock lets only one thread execute Python bytecode at a time in CPython. Threads still help for IO-bound work; for CPU-bound work use multiprocessing, native extensions, or async concurrency for IO. (Free-threaded builds are emerging in newer versions.)'],
    [2, 'is vs ==, and shallow vs deep copy?', '== compares values, is compares identity. copy.copy duplicates the outer container but shares nested objects; copy.deepcopy recursively copies. Assignment only binds another name to the same object.'],
    [2, 'What are list comprehensions and how do they compare with map/filter?', 'Concise expressions like [x*x for x in xs if x > 0]; usually more readable and as fast as or faster than map/filter with lambda. Generator expressions ( ... ) avoid building the list.'],
    [3, 'Explain asyncio: event loop, coroutines, await. When to use it?', 'Single-threaded cooperative concurrency: coroutines (async def) yield control at await points while the event loop runs other tasks. Best for many concurrent IO operations (HTTP calls, sockets). Blocking calls freeze the loop - offload to run_in_executor.'],
    [3, 'What are context managers and how do you write one?', 'Objects supporting with-statement resource management via __enter__/__exit__ or the @contextmanager decorator (yield). They guarantee cleanup of files, locks, transactions even on exceptions.'],
    [3, 'How do you profile and optimize slow Python code?', 'Measure first with cProfile/py-spy/line_profiler, fix algorithmic complexity, use built-ins and comprehensions, cache with lru_cache, vectorize with NumPy, batch IO, use multiprocessing or async as appropriate, and only then consider Cython or native extensions.'],
    [4, 'How would you structure and deploy a production Python API service?', 'FastAPI/Django with typed models (pydantic), dependency injection, layered modules, config via environment, Gunicorn+Uvicorn workers behind a load balancer, structured logging and tracing, DB migrations (Alembic), pytest with CI, container images with pinned dependencies, and health checks/autoscaling.'],
  ],
  notes: [
    { title: 'Python Data Types', points: ['list [] mutable ordered', 'tuple () immutable', 'set {} unique', 'dict {k:v} ordered by insertion', 'Mutable default args are shared!'], code: 'def add(x, acc=None):\n    acc = [] if acc is None else acc\n    acc.append(x)\n    return acc' },
    { title: 'Decorator Pattern', diagram: { type: 'flow', items: ['call f()', 'wrapper', 'run f', 'return'] }, points: ['Function in, function out', 'Use functools.wraps', 'Great for cache, retry, auth'], code: 'def timed(fn):\n    @wraps(fn)\n    def w(*a, **k):\n        t = time.time()\n        r = fn(*a, **k)\n        print(time.time() - t)\n        return r\n    return w' },
    { title: 'Concurrency Choices', points: ['IO-bound -> threads or asyncio', 'CPU-bound -> multiprocessing', 'GIL blocks parallel bytecode threads', 'Async needs non-blocking libs'] },
  ],
};

export const sql = {
  questions: [
    [1, 'Explain the different types of SQL JOINs.', 'INNER: rows matching in both tables. LEFT: all left rows plus matches (NULLs otherwise). RIGHT: mirror of left. FULL OUTER: all rows from both. CROSS: Cartesian product. SELF join joins a table to itself (e.g. employee-manager).'],
    [1, 'WHERE vs HAVING?', 'WHERE filters rows before grouping and cannot use aggregates; HAVING filters groups after GROUP BY and can use aggregates like COUNT(*) > 5.'],
    [1, 'DELETE vs TRUNCATE vs DROP?', 'DELETE removes selected rows (DML, logged, can be rolled back, fires triggers). TRUNCATE quickly removes all rows and resets identity (DDL, minimal logging). DROP removes the table structure entirely.'],
    [1, 'What are primary key, foreign key and unique constraints?', 'Primary key uniquely identifies a row (not null, one per table). Foreign key references a key in another table to enforce referential integrity. Unique prevents duplicates but allows nulls (varies by DB) and can exist multiple times per table.'],
    [2, 'What is an index? When can it hurt?', 'A B-tree (usually) structure that speeds lookups, sorting and joins on indexed columns. It slows writes, uses storage, and is ignored on low-selectivity columns or when functions wrap the column (WHERE LOWER(name)=...). Composite indexes follow the leftmost-prefix rule.'],
    [2, 'Explain ACID properties and isolation levels.', 'Atomicity, Consistency, Isolation, Durability. Isolation levels: Read Uncommitted (dirty reads), Read Committed, Repeatable Read (no non-repeatable reads), Serializable (no phantoms). Higher isolation means more locking or aborts.'],
    [2, 'How do you find the second highest salary?', 'SELECT MAX(salary) FROM emp WHERE salary < (SELECT MAX(salary) FROM emp); or use DENSE_RANK() OVER (ORDER BY salary DESC) and filter rank = 2, or OFFSET 1 LIMIT 1 on DISTINCT salaries.'],
    [2, 'What is normalization? Explain 1NF, 2NF, 3NF.', '1NF: atomic values, no repeating groups. 2NF: 1NF plus no partial dependency on part of a composite key. 3NF: 2NF plus no transitive dependency (non-key depends only on the key). Denormalize deliberately for read performance.'],
    [3, 'Explain window functions with an example.', 'They compute across a set of related rows without collapsing them: ROW_NUMBER, RANK, DENSE_RANK, LAG/LEAD, SUM() OVER (PARTITION BY dept ORDER BY hire_date). Example: top 3 earners per department using ROW_NUMBER() OVER (PARTITION BY dept ORDER BY salary DESC).'],
    [3, 'How do you debug and optimize a slow query?', 'Read the execution plan (EXPLAIN ANALYZE), look for full scans, bad join order, missing or unused indexes, and row estimate errors. Fix with proper indexes, covering indexes, rewriting subqueries, avoiding SELECT *, pagination by keyset, updating statistics, and partitioning if needed.'],
    [3, 'What causes deadlocks and how do you prevent them?', 'Transactions hold locks and wait on each other in a cycle. Prevent by accessing tables/rows in a consistent order, keeping transactions short, using appropriate isolation, and retrying on deadlock errors.'],
    [4, 'SQL vs NoSQL and how do you scale a relational database?', 'Choose by access patterns and consistency needs. Scale reads with replicas and caching, scale writes via vertical scaling, partitioning/sharding by a well-chosen key, CQRS, and moving hot data to purpose-built stores. Consider the CAP trade-offs and operational cost of cross-shard joins/transactions.'],
  ],
  notes: [
    { title: 'JOIN Types', diagram: { type: 'flow', items: ['INNER = A and B', 'LEFT = all A', 'RIGHT = all B', 'FULL = A or B'] }, points: ['Unmatched side returns NULL', 'CROSS = every combination', 'Self join for hierarchies'] },
    { title: 'Query Order of Execution', diagram: { type: 'stack', items: ['FROM / JOIN', 'WHERE', 'GROUP BY', 'HAVING', 'SELECT', 'ORDER BY / LIMIT'] }, points: ['Explains why aliases fail in WHERE', 'Filter early with WHERE'] },
    { title: 'Window Function Pattern', points: ['PARTITION BY = group without collapsing', 'ORDER BY inside OVER defines ranking', 'LAG/LEAD to compare neighbours'], code: 'SELECT name, dept, salary,\n  RANK() OVER (PARTITION BY dept\n               ORDER BY salary DESC) AS rk\nFROM employees;' },
  ],
};
