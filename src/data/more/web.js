export const react = {
  questions: [
    [1, "What does JSX compile to?", "JSX is syntax sugar for function calls: <div id=\"a\">Hi</div> becomes jsx(\"div\", { id: \"a\", children: \"Hi\" }) via the automatic runtime. The result is a plain object (a React element) describing the UI, not a real DOM node."],
    [1, "Why must you never mutate state directly?", "React compares old and new state by reference (Object.is), so mutating in place produces no detectable change and may skip the re-render. Always create a new array or object (spread, map, filter) so updates are predictable and memoization works."],
    [2, "What are stale closures in hooks and how do you avoid them?", "A callback or effect captures the props and state from the render in which it was created. If the dependency array omits a value, the effect keeps using the old one. Fix by listing dependencies (use the exhaustive-deps lint rule), using functional updates like setN(n => n + 1), or keeping the latest value in a ref."],
    [2, "When would you use useReducer over useState?", "When state has several related fields or transitions depend on the previous state and an action type. A reducer centralizes update logic, is easy to unit test, and dispatch has a stable identity so it can be passed down without breaking memoization. The trade-off is more boilerplate for simple state."],
    [2, "What is the purpose of useRef besides DOM access?", "A ref is a mutable box that persists across renders and does not trigger a re-render when changed. It is good for timer ids, previous values, and the latest callback. Do not read or write it during render for values that should drive the UI."],
    [2, "How do error boundaries work and what do they not catch?", "A class component with getDerivedStateFromError and componentDidCatch catches errors thrown during rendering, lifecycle methods and constructors of its children and shows a fallback. They do not catch errors in event handlers, async code, or SSR. Libraries like react-error-boundary provide a hook-friendly wrapper."],
    [3, "How does React reconcile a list and what goes wrong with index keys?", "React matches children by key (then by type and position) and reuses fibers with the same key. With index keys, inserting or reordering items shifts identities, so component state, focus and uncontrolled input values attach to the wrong rows and extra DOM work is done. Use stable ids from the data; index keys are only safe for static lists."],
    [3, "Explain useTransition vs useDeferredValue.", "Both mark work as non-urgent so urgent updates such as typing stay responsive. useTransition wraps the state update you control (startTransition(() => setQuery(v))) and exposes isPending. useDeferredValue defers a value you receive, for example from props, and lags behind it. Neither reduces total work; they only reprioritize it, and expensive children should also be memoized."],
    [3, "What is Suspense and how does it change data loading?", "Suspense lets a component signal that it is not ready (a lazy import, or a suspense-enabled data source) and shows the nearest fallback while waiting. It removes manual isLoading branching and enables streaming SSR and coordinated loading states. The trade-off is that you need a Suspense-compatible data layer, and waterfalls occur if fetches start only after render."],
    [3, "How would you profile and fix a slow React screen?", "Record with the React DevTools Profiler (enable highlight updates) to find components that re-render often or slowly, then confirm with the browser Performance panel for long tasks. Fixes in order: colocate or lower state, split context, memoize expensive children, virtualize long lists, and defer non-urgent work with transitions. Re-measure each step because memoization has its own cost."],
    [3, "Why can Context cause performance problems and how do you mitigate them?", "Every consumer re-renders when the provider value changes by reference, even if it only uses a slice. Mitigate by splitting contexts (state vs dispatch, or by domain), memoizing the value object, keeping fast-changing state out of context, or using a selector-based store such as Zustand or Redux with useSyncExternalStore."],
    [3, "What is hydration and what causes hydration mismatches?", "Hydration attaches event handlers and state to server-rendered HTML by re-running components on the client and expecting the same output. Mismatches come from non-deterministic render output: Date.now, Math.random, window checks, locale or timezone differences, or invalid HTML nesting. Fix by rendering such values in useEffect, or by using suppressHydrationWarning for unavoidable text differences."],
    [3, "How does useEffect differ from useLayoutEffect and useSyncExternalStore?", "useLayoutEffect runs synchronously after DOM mutation but before paint, so it is right for measuring layout, and it blocks painting so overuse hurts performance. useEffect runs after paint. useSyncExternalStore subscribes to external stores in a way that avoids tearing during concurrent rendering, and is what state libraries use internally."],
    [4, "How does React Fiber enable concurrent rendering?", "Fiber turns the render tree into a linked list of units of work that can be paused, resumed, or discarded, unlike the old recursive stack reconciler. In concurrent mode React works in time slices, yields to the browser, and can restart a render when higher-priority updates such as input arrive using lanes. The commit phase stays synchronous, so render functions must be pure because they may run multiple times."],
    [4, "Design a state management strategy for a large dashboard app.", "Classify state by kind: server state in React Query or SWR (caching, deduping, invalidation), URL state in the router for shareable filters, form state in a form library, and a small client store for cross-cutting UI state. Avoid mirroring server data into Redux. The trade-off is more tools to learn, but each has a clear ownership boundary and far less hand-written sync code."],
    [4, "SSR, SSG, ISR, streaming or client rendering: how do you choose?", "Choose per route by data freshness and SEO needs. SSG or ISR suits mostly static content and gives the best TTFB via CDN, SSR suits personalised or fast-changing pages at higher server cost, streaming SSR with Suspense improves perceived speed for slow data, and CSR is fine behind login. Consider hydration cost: React Server Components and islands reduce the JS shipped."],
    [4, "What are React Server Components and what are their limits?", "RSCs render only on the server, can await data or read the database directly, and ship no client JS for themselves. They cannot use state, effects or browser APIs; interactivity lives in client components marked with use client. Props crossing the boundary must be serializable. The trade-offs are a more complex mental model, framework coupling, and server infrastructure to operate."],
    [4, "When are micro-frontends justified and how do you implement them safely?", "They are justified when independent teams need separate deploy cycles and tech ownership, not for small teams. Common approaches are Module Federation, route-level composition, or web components. Risks are duplicated dependencies, inconsistent UX, and shared-state coupling, so enforce a shared design system, versioned contracts, singleton React, and performance budgets per remote."],
    [4, "How would you migrate a large class-based or legacy React codebase incrementally?", "Do not rewrite. Upgrade React versions first, add tests around critical flows, then adopt the strangler pattern: new code uses hooks and TypeScript, old components are converted when touched, and codemods handle mechanical changes. Track progress with lint rules and metrics, keep both styles interoperable, and avoid big-bang PRs that block feature work."],
    [4, "How do you keep a React app fast as it and its team grow?", "Set performance budgets (bundle size, LCP, INP) enforced in CI, route-level code splitting, analyze bundles regularly, and monitor real-user metrics with alerts. Establish ownership and conventions such as colocated state and a shared component library with tested primitives. The trade-off is process overhead, but regressions get caught at PR time instead of in production."],
    [3, "How do you design an optimistic-update flow with React Query or transitions and handle failure?", "Apply the expected change to the cache immediately (onMutate), keep a snapshot, roll back in onError, and invalidate in onSettled to reconcile with the server. Handle concurrent mutations by cancelling in-flight queries, and surface failures with a toast or retry. It makes the UI feel instant but requires idempotent APIs and careful conflict handling."],
    [4, "How would you design a design-system component library for many React apps?", "Build headless, accessible primitives with tokens for theming, publish as versioned packages with semantic versioning and a changelog, and support tree shaking via ESM and sideEffects flags. Provide Storybook and visual regression tests, and treat a11y as a release gate. Trade-off: central control gives consistency but can slow teams, so offer clear extension and escape-hatch patterns."],
  ],
  notes: [
    { title: "Hooks Rules", points: ["Call hooks at the top level only", "Never inside loops, ifs or nested functions", "Only in components or custom hooks", "Order must be identical every render", "Lint: rules-of-hooks + exhaustive-deps"], code: `// BAD\nif (ok) { useState(0); }\n// GOOD\nconst [n, setN] = useState(0);` },
    { title: "Concurrent Rendering", diagram: { type: "flow", items: ["Urgent update", "Interrupt render", "Render low lane", "Commit"] }, points: ["Render phase can pause or restart", "Commit phase is synchronous", "Keep render functions pure", "startTransition marks non-urgent"] },
    { title: "Rendering Strategies", diagram: { type: "stack", items: ["CSR: browser builds UI", "SSR: HTML per request", "SSG/ISR: prebuilt HTML", "RSC: server-only comps"] }, points: ["Pick per route", "Hydration costs JS + CPU", "Streaming improves TTFB feel", "Avoid hydration mismatches"] },
    { title: "Avoiding Extra Renders", points: ["Colocate state near its use", "Split big contexts", "Pass stable props to memo children", "Derive values, do not mirror state", "Measure before memoizing"], code: `const total = useMemo(\n  () => items.reduce(add, 0),\n  [items]\n);` },
  ],
  cards: [
    [3, "Render vs commit phase?", "Render computes the tree and may pause or restart; commit applies DOM changes and runs layout effects synchronously."],
    [3, "Purpose of useSyncExternalStore?", "Subscribe to external stores without tearing in concurrent rendering."],
    [4, "What are lanes in React?", "Priority bit-flags that let React schedule and batch updates of different urgency."],
    [3, "Batching in React 18?", "State updates are batched automatically everywhere, including promises, timeouts and native handlers."],
    [2, "Why is key not a prop?", "React consumes key and ref for identity; they are not passed to the child via props."],
    [4, "What does use client do in RSC?", "Marks a module boundary; that module and its imports are bundled for the browser."],
    [3, "useLayoutEffect vs useEffect?", "Layout effect runs before paint, blocking; useEffect runs after paint."],
    [3, "What is tearing?", "Parts of the UI showing different versions of external state in the same render."],
    [4, "Strict Mode double-render purpose?", "Dev only: surfaces impure renders and missing effect cleanup."],
    [2, "What does React.lazy need?", "A Suspense boundary above it and a default-exported component from dynamic import()."],
  ],
};

export const javascript = {
  questions: [
    [1, "What is the difference between null and undefined?", "undefined means a variable was declared but has no value (or a property or argument is missing). null is an explicit empty value assigned by the developer. typeof null is object (a historical quirk), and null == undefined is true while === is false."],
    [1, "What do map, filter and reduce do?", "map returns a new array by transforming each item, filter keeps items passing a test, and reduce folds the array into a single value with an accumulator. None of them mutate the original array. Always pass an initial value to reduce so empty arrays do not throw."],
    [2, "What is the difference between shallow and deep copy, and how do you deep clone?", "Spread and Object.assign copy only one level, so nested objects stay shared. structuredClone handles most data types including Dates, Maps and cycles but not functions or DOM nodes. JSON.parse(JSON.stringify()) drops undefined, functions and Dates."],
    [2, "How do call, apply and bind differ?", "call invokes a function with a given this and arguments listed individually; apply takes them as an array; bind returns a new function with this (and optionally leading arguments) fixed. Bound functions cannot have their this rebound later."],
    [2, "What is event delegation?", "Attach one listener to a parent and use event.target (or closest) to identify the child that triggered the event, relying on bubbling. It saves memory, and it works for elements added later. It does not work for events that do not bubble, such as focus, unless you use focusin."],
    [2, "What are Symbol, WeakMap and WeakSet used for?", "Symbols create unique property keys that avoid collisions. WeakMap and WeakSet hold keys weakly, so entries can be garbage collected when nothing else references the key object. They are not iterable and are ideal for private metadata or caches tied to object lifetime."],
    [3, "Explain the event loop in Node vs the browser, including nextTick and microtask starvation.", "Browsers run one macrotask, drain microtasks, then may render. Node has phases (timers, poll, check for setImmediate) and process.nextTick runs before promise microtasks. A microtask that keeps scheduling microtasks starves rendering and IO, so chunk long work or yield with setTimeout or scheduler.postTask."],
    [3, "How do V8 hidden classes and inline caches affect performance?", "V8 gives objects with the same property-addition order a shared hidden class (shape), letting inline caches resolve property access to a fixed offset. Adding properties in different orders, deleting properties, or mixing types makes call sites polymorphic or megamorphic and slower. Initialize all fields in the constructor in a consistent order."],
    [3, "How does garbage collection work in V8 and what causes leaks?", "V8 uses a generational, mark-and-sweep collector: a young generation (scavenger) for short-lived objects and an old generation with incremental and concurrent marking. Leaks are reachable-but-unused objects: forgotten listeners, timers, global caches, closures capturing large scopes, detached DOM nodes. Confirm with heap snapshot diffs."],
    [3, "What is tree shaking and what defeats it?", "Bundlers drop unused exports by statically analyzing ESM imports. It is defeated by CommonJS, modules with side effects not marked via package.json sideEffects, re-export barrels that import everything, and dynamic property access. Prefer named ESM imports and check the bundle analyzer."],
    [3, "How do XSS attacks work and how do you defend against them?", "XSS injects script that runs in your origin, via reflected, stored or DOM-based sinks such as innerHTML, document.write or eval. Defend with output encoding, using textContent, sanitizing HTML with a vetted library (DOMPurify), Trusted Types, and a strict Content Security Policy. Also use HttpOnly cookies so stolen script cannot read session tokens."],
    [3, "How do you handle cancellation of async work?", "Use AbortController: pass signal to fetch and to your own async functions, and call abort() to cancel. Aborted fetches reject with an AbortError that should be treated as non-error. Promises themselves are not cancellable, so design APIs to accept a signal and check signal.aborted."],
    [3, "What is the difference between microtask ordering in async/await and then chains?", "await schedules its continuation as a microtask after the awaited promise settles, so an async function runs synchronously until the first await. Returning a promise from a then callback or async function can add extra microtask ticks compared with returning a plain value. Ordering questions usually resolve by tracing the microtask queue step by step."],
    [3, "Explain Proxy and Reflect and a real use case.", "A Proxy wraps an object and intercepts operations such as get, set and has through traps; Reflect provides the default behavior to forward to. Use cases include reactivity systems (Vue 3), validation and logging. The trade-off is overhead on every intercepted access, and proxies can be hard to debug."],
    [4, "How would you design a memory-leak investigation for a long-lived SPA in production?", "Reproduce with a repeated user flow, take heap snapshots before and after forced GC in DevTools, and compare retained objects and detached DOM. Add real-user monitoring of performance.memory or heap trends, and use allocation sampling to find the retaining path. Fix by cleanup in unmount, AbortController, and bounded caches; add a regression soak test."],
    [4, "ESM vs CommonJS in a Node library: how do you ship both and what are the pitfalls?", "Use package.json exports with import and require conditions, and build dual outputs. Pitfalls include the dual package hazard (two copies with separate state), default-export interop differences, and top-level await being ESM-only. Modern Node can require ESM in some versions, but shipping ESM-first with a CJS build for older consumers is the safe path."],
    [4, "How do you design a strict CSP for a modern app and roll it out safely?", "Prefer nonce- or hash-based script-src with strict-dynamic instead of allowlisting hosts, block inline handlers, set object-src none and base-uri none. Roll out with Content-Security-Policy-Report-Only and a report endpoint, fix violations, then enforce. The trade-off is refactoring inline scripts and third-party tags, but it strongly limits XSS impact."],
    [4, "How would you keep the main thread responsive with heavy computation?", "Move CPU-bound work to Web Workers (with transferable objects to avoid copying), or chunk it and yield between chunks using scheduler.yield, requestIdleCallback or setTimeout. Use OffscreenCanvas or WASM for heavy numeric work. Trade-offs are message-passing overhead and no direct DOM access from workers."],
    [4, "How do you reduce JavaScript bundle size at scale?", "Analyze with a bundle visualizer, split by route and by heavy feature via dynamic import, remove or replace large dependencies, enable tree shaking and minification, and target modern browsers to avoid unneeded polyfills. Enforce size budgets in CI. Beware over-splitting, which increases request count and waterfalls."],
    [4, "How do prototype pollution attacks work and how do you prevent them?", "Attackers supply keys like __proto__ or constructor.prototype in merged or parsed input, modifying Object.prototype so every object inherits attacker values, which can lead to XSS or privilege bypass. Prevent by rejecting those keys, using Object.create(null) or Map for dictionaries, freezing prototypes, and updating vulnerable deep-merge libraries."],
    [4, "How do you design a resilient client for flaky networks (retries, backoff, idempotency)?", "Retry only idempotent requests, or send idempotency keys for writes, using exponential backoff with jitter and a max attempt cap. Respect Retry-After, use timeouts with AbortController, and add a circuit breaker for repeated failures. Queue offline mutations and reconcile on reconnect. The trade-off is added complexity and possible duplicate side effects if the server is not idempotent."],
    [4, "What are the security trade-offs of storing auth tokens in localStorage vs cookies?", "localStorage is readable by any script, so one XSS exposes the token. HttpOnly, Secure, SameSite cookies are not readable by script but need CSRF protection (SameSite, CSRF tokens) and careful CORS. A common robust pattern is a short-lived access token in memory plus a refresh token in an HttpOnly cookie."],
  ],
  notes: [
    { title: "V8 Performance Tips", points: ["Keep object shapes consistent", "Init all fields in constructor", "Avoid delete on hot objects", "Do not mix types in arrays", "Profile before micro-optimizing"], code: `class P { constructor(x, y) {\n  this.x = x; this.y = y;\n} }` },
    { title: "Common Memory Leaks", diagram: { type: "flow", items: ["Add listener", "Component gone", "Still referenced", "Never GC'd"] }, points: ["Timers and intervals not cleared", "Listeners not removed", "Global caches that grow", "Detached DOM nodes held in vars"] },
    { title: "XSS Defense Layers", diagram: { type: "stack", items: ["CSP + Trusted Types", "Sanitize HTML input", "Encode output", "HttpOnly cookies"] }, points: ["Prefer textContent over innerHTML", "Never eval user data", "Defense in depth"] },
    { title: "Cancelling Async Work", points: ["AbortController gives a signal", "fetch accepts signal", "abort() rejects with AbortError", "Ignore AbortError in handlers", "Cancel on unmount or new input"], code: `const ac = new AbortController();\nfetch(url, { signal: ac.signal });\nac.abort();` },
  ],
  cards: [
    [3, "process.nextTick vs Promise.then?", "In Node, nextTick callbacks run before promise microtasks."],
    [3, "What is a hidden class?", "V8 internal shape describing an object's property layout, enabling fast inline-cached access."],
    [4, "What is the dual package hazard?", "Loading both ESM and CJS builds of a package creates two instances with separate state."],
    [3, "Why avoid delete on objects?", "It can change the hidden class and push objects to slow dictionary mode."],
    [4, "What does strict-dynamic do in CSP?", "Trusts scripts loaded by an already-trusted nonce or hash script, ignoring host allowlists."],
    [3, "WeakMap key requirement?", "Keys must be objects (or non-registered symbols); entries do not prevent GC."],
    [2, "structuredClone limitation?", "Cannot clone functions or DOM nodes; throws DataCloneError."],
    [3, "queueMicrotask vs setTimeout(0)?", "Microtask runs before the next macrotask and before rendering; timeout runs later."],
    [4, "What is prototype pollution?", "Injecting properties into Object.prototype through unsafe merges of attacker-controlled keys."],
    [3, "Transferable objects?", "ArrayBuffers moved (not copied) to a worker via postMessage, leaving the sender empty."],
  ],
};

export const html = {
  questions: [
    [1, "What is the difference between em, rem and px units?", "px is an absolute CSS pixel; em is relative to the element's own font size (or parent for font-size), so it compounds when nested; rem is relative to the root font size. Use rem for scalable, accessible typography and spacing because it respects user font-size settings."],
    [1, "What does the alt attribute do and when should it be empty?", "alt provides a text alternative for images used by screen readers and shown if the image fails. Use descriptive text for informative images and alt=\"\" for purely decorative ones so assistive tech skips them. Omitting the attribute entirely is an error."],
    [2, "Explain position: static, relative, absolute, fixed and sticky.", "static is normal flow. relative offsets from its normal position while still occupying space. absolute is removed from flow and positioned against the nearest positioned ancestor. fixed is positioned against the viewport. sticky behaves as relative until a scroll threshold, then sticks within its container."],
    [2, "How do srcset, sizes and picture work for responsive images?", "srcset lists candidate files with widths, sizes tells the browser the rendered width at each breakpoint, and the browser picks the best file for the viewport and pixel density. picture with source elements handles art direction and modern formats such as AVIF and WebP. Always set width and height attributes to avoid layout shift."],
    [2, "What are CSS custom properties and how do they differ from preprocessor variables?", "Custom properties (--x) are resolved at runtime, cascade and inherit, and can be changed by JavaScript or media queries, which makes theming and dark mode easy. Sass variables are compile-time only. The trade-off is that invalid values only fail at computed-value time."],
    [2, "How do async and defer differ on script tags?", "Both download without blocking HTML parsing. defer executes in order after parsing finishes, before DOMContentLoaded. async executes as soon as it is downloaded, in no guaranteed order. Use defer for app scripts and async for independent scripts like analytics."],
    [3, "Describe the critical rendering path.", "The browser builds the DOM from HTML and the CSSOM from CSS, combines them into a render tree, then performs layout, paint and compositing. CSS is render-blocking and synchronous scripts are parser-blocking. Optimize by inlining critical CSS, deferring scripts, preloading key resources and reducing DOM size."],
    [3, "What are ARIA roles, states and properties, and what is the first rule of ARIA?", "ARIA adds semantics for assistive tech: roles (dialog, tab), states (aria-expanded) and properties (aria-labelledby). The first rule is not to use ARIA if a native element such as button can do the job, because ARIA changes semantics but adds no behavior. Wrong ARIA is worse than none, so test with a screen reader."],
    [3, "How do you make a custom modal dialog accessible?", "Prefer the native dialog element with showModal(), which gives focus trapping, Escape to close and inert background. Otherwise use role=dialog, aria-modal, a label, move focus in on open, trap it, restore focus on close. Ensure it can be dismissed by keyboard."],
    [3, "What causes layout thrashing and reflow, and how do you avoid it?", "Interleaving DOM reads that force layout (offsetHeight, getBoundingClientRect) with writes makes the browser recompute layout repeatedly. Batch reads then writes, use requestAnimationFrame, and animate transform and opacity, which can run on the compositor. contain and content-visibility can limit layout scope."],
    [3, "Explain the three Core Web Vitals and their thresholds.", "LCP (loading) measures when the largest content element renders, good is 2.5 s or less. INP (responsiveness) measures interaction latency, good is 200 ms or less. CLS (visual stability) measures unexpected layout shifts, good is 0.1 or less. They are measured at the 75th percentile of real users."],
    [3, "How does stacking context work and why does z-index sometimes fail?", "z-index only applies within a stacking context, and children cannot escape their parent's context. Contexts are created by positioned elements with z-index, opacity below 1, transform, filter, isolation: isolate and others. A high z-index in one context cannot beat a sibling context with higher order."],
    [3, "When would you use container queries over media queries?", "Container queries style a component based on its container's size, so the same component adapts in a sidebar or main area. Media queries respond only to the viewport. Container queries make reusable components more self-contained, though the container needs container-type set and adds a small layout cost."],
    [3, "How do CSS cascade layers (@layer) help?", "Layers let you define explicit precedence groups (reset, base, components, utilities) that take priority over specificity, so later layers win regardless of selector strength. This tames third-party CSS and reduces !important use. Unlayered styles beat layered ones, which is a common surprise."],
    [4, "How would you architect CSS for a large multi-team codebase?", "Combine design tokens (custom properties), a layer or naming strategy (BEM, CSS Modules or utility-first like Tailwind), and component-scoped styles to avoid global collisions. Enforce with stylelint and visual regression tests. Trade-offs: utilities are fast and consistent but verbose markup, modules give isolation but need tooling, and global CSS is simple but does not scale."],
    [4, "How would you improve LCP on a slow product page?", "Identify the LCP element via DevTools or RUM, then cut its chain: fast TTFB and CDN, preload the hero image with fetchpriority=high, use AVIF or WebP with responsive sizes, and inline critical CSS. Do not lazy-load the LCP image. Remove render-blocking scripts and verify with field data, since lab gains may not show in real users."],
    [4, "Which HTTP security headers would you set and why?", "Content-Security-Policy to limit script sources, Strict-Transport-Security to force HTTPS, X-Content-Type-Options: nosniff, frame-ancestors (or X-Frame-Options) against clickjacking, Referrer-Policy, and Permissions-Policy. Cross-Origin-Opener-Policy and Embedder-Policy enable isolation. Roll CSP out in report-only mode first because it can break third-party scripts."],
    [4, "How does the browser layout engine handle flex and grid, and what are performance implications?", "Both are resolved during the layout phase and can require multiple passes for content-based sizing, so deeply nested flex or auto-sized grids can be costly. Prefer fixed or fr tracks where possible, limit DOM depth, and use contain or content-visibility: auto to skip offscreen work. Measure with the Performance panel layout events before optimizing."],
    [4, "How would you set up an accessibility program for a product team?", "Shift left: accessible design-system components, linting (eslint-plugin-jsx-a11y), axe in unit and CI tests, and manual keyboard and screen reader audits on key flows. Define WCAG 2.2 AA as the target and track defects like any bug class. Automated tools catch only a portion of issues, so manual testing and disabled-user feedback remain essential."],
    [4, "How do you deliver a fast experience on low-end devices and slow networks?", "Set performance budgets, ship less JavaScript, server-render or statically render content, and use adaptive loading with Save-Data and Network Information hints where available. Cache aggressively with a service worker, compress with Brotli, and use content-visibility for long pages. Test on throttled CPU and network profiles, not just a developer laptop."],
    [4, "How would you prevent CLS across a content-heavy site with ads and embeds?", "Reserve space using width and height or aspect-ratio for images, video and embeds, and fixed min-height slots for ads. Load web fonts with font-display and size-adjust fallbacks to reduce font swap shifts, and insert dynamic content below the fold or in response to user actions. Verify with the Layout Shift regions in DevTools and field CLS data."],
    [4, "What is the trade-off between inline critical CSS and external stylesheets?", "Inlining critical CSS removes a render-blocking request and improves first paint, but it is not cacheable across pages, grows HTML, and needs a build step to extract it. External CSS caches well but blocks rendering. A common approach is inline the above-the-fold CSS and load the rest asynchronously, keeping the inlined part small."],
  ],
  notes: [
    { title: "Critical Rendering Path", diagram: { type: "flow", items: ["DOM + CSSOM", "Render tree", "Layout", "Paint", "Composite"] }, points: ["CSS blocks rendering", "Sync JS blocks parsing", "Use defer and preload", "Animate transform and opacity"] },
    { title: "Core Web Vitals", points: ["LCP: 2.5s or less", "INP: 200ms or less", "CLS: 0.1 or less", "Judged at 75th percentile field data", "Lab tools help debug only"], diagram: { type: "stack", items: ["LCP: loading", "INP: interaction", "CLS: stability"] } },
    { title: "ARIA Basics", points: ["Native element first", "Roles: what it is", "States: aria-expanded etc", "Labels: aria-label(ledby)", "ARIA adds no keyboard behavior"], code: `<button aria-expanded="false"\n  aria-controls="menu">Menu</button>` },
    { title: "Security Headers", points: ["CSP: limit script sources", "HSTS: force HTTPS", "nosniff: stop MIME guessing", "frame-ancestors: no clickjack", "Referrer-Policy: limit leakage"], code: `Content-Security-Policy:\n  default-src 'self'; object-src 'none'` },
  ],
  cards: [
    [3, "INP good threshold?", "200 ms or less at the 75th percentile."],
    [3, "What creates a stacking context?", "Positioned with z-index, opacity < 1, transform, filter, isolation: isolate."],
    [4, "content-visibility: auto does what?", "Skips layout and paint of offscreen content until it is near the viewport."],
    [3, "Which CSS props are compositor-friendly?", "transform and opacity; they avoid layout and paint."],
    [4, "First rule of ARIA?", "Do not use ARIA if a native HTML element already provides the semantics."],
    [3, "Why set width and height on images?", "Lets the browser reserve space via aspect ratio, preventing layout shift."],
    [2, "defer vs async?", "defer runs in order after parsing; async runs as soon as downloaded, unordered."],
    [4, "What is fetchpriority=high for?", "Hints the browser to prioritize a resource, typically the LCP image."],
    [3, "Logical property example?", "margin-inline-start replaces margin-left for RTL-friendly layouts."],
    [4, "CSP report-only header?", "Content-Security-Policy-Report-Only logs violations without blocking, for safe rollout."],
  ],
};
