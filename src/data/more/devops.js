export const jenkins = {
  questions: [
    [1, 'What is a Jenkinsfile and where should it live?', 'A text file defining the pipeline as code. Keep it in the root of the application repository so pipeline changes are versioned, reviewed and branch-specific alongside the code they build.'],
    [1, 'What does the post section do in a declarative pipeline?', 'It defines steps that run after the stages finish, with conditions such as always, success, failure, unstable and cleanup. Typical uses are publishing test reports, sending notifications and cleaning the workspace.'],
    [2, 'What is a Multibranch Pipeline?', 'A job type that scans a repository and automatically creates a pipeline for each branch or pull request containing a Jenkinsfile, and removes them when branches are deleted. It gives per-branch builds without manual job setup.'],
    [2, 'How do you run stages in parallel and what should you watch out for?', 'Use the parallel block inside a stage with several nested stages, each can have its own agent. Watch for shared workspace collisions, resource contention on agents and making sure failFast is set only where cancelling siblings is acceptable.'],
    [2, 'What is the difference between stash/unstash and archiveArtifacts?', 'stash/unstash temporarily passes files between stages or agents within a single run and is discarded afterwards. archiveArtifacts stores files with the build permanently for download. Use an artifact repository like Nexus/Artifactory for real releases instead of relying on Jenkins storage.'],
    [2, 'How do you pass parameters and use environment variables in a pipeline?', 'Declare a parameters block (string, choice, boolean) and read them via params.NAME; define env in the environment block, and use withEnv for scoped values. Avoid putting secrets in parameters because they can appear in build history.'],
    [3, 'How would you design Jenkins agents on Kubernetes and what are the trade-offs?', 'The Kubernetes plugin launches one pod per build from a pod template, giving clean ephemeral environments and elastic capacity. Trade-offs are pod start-up latency, cold dependency caches (mitigate with PVC or a remote cache), needing resource requests/limits, and securing the Docker-build path (use Kaniko or BuildKit rather than mounting the Docker socket).'],
    [3, 'What is the CPS transformation and why do pipelines behave strangely with some Groovy code?', 'Pipeline Groovy runs under the CPS interpreter so it can survive controller restarts by serializing state. Non-serializable objects held across steps throw NotSerializableException, and some Groovy idioms (closures in collections, .each with steps) behave poorly. Move heavy logic into @NonCPS methods or, better, into external scripts and shared library classes.'],
    [3, 'A pipeline works on a developer branch but fails randomly on main. How do you investigate flaky builds?', 'Compare agents, workspace state and environment between runs, check for shared mutable state such as reused workspaces, ports or test databases, and look for time-dependent or order-dependent tests. Reproduce with the same container image, quarantine flaky tests with tracking, add retries only as a stopgap, and record flake rate as a metric.'],
    [3, 'How do you design and version a shared library safely?', 'Separate vars/ (global steps) from src/ (classes), tag releases with semver and let teams pin @Library("lib@v2") while main is used only for testing. Add unit tests with JenkinsPipelineUnit, keep steps small and documented, and deprecate before removing. Trade-off: a floating version is convenient but one bad commit breaks every pipeline.'],
    [3, 'How do you secure a Jenkins controller?', 'Use SSO with matrix or role-based authorization, disable builds on the controller, enable the script security sandbox and review approvals, keep plugins updated, restrict who can configure jobs, run behind TLS, and use folder-scoped credentials. Enable CSRF protection and agent-to-controller access control so a compromised agent cannot read controller secrets.'],
    [3, 'Explain how Jenkins credentials scoping and folders limit blast radius.', 'Credentials can be defined at system, global or folder level; folder-scoped credentials are visible only to jobs in that folder, so teams cannot use each other secrets. Combine with folder RBAC and prefer short-lived secrets from Vault or a cloud role, since anyone who can edit a Jenkinsfile can potentially exfiltrate credentials bound to the job.'],
    [3, 'How do you use Jenkins Configuration as Code (JCasC) and what does it solve?', 'A YAML file describes controller settings, security realm, credentials references, agents and jobs (with the Job DSL plugin), applied at startup. It makes the controller reproducible, reviewable and rebuildable after failure, avoiding snowflake UI configuration. Secrets are referenced from environment or files rather than committed.'],
    [4, 'Your Jenkins controller is the bottleneck with thousands of jobs. How do you scale it?', 'A single controller does not scale horizontally, so options are pruning old builds and heavy plugins, moving to lightweight executors on dynamic agents, sharding by team or domain into several controllers behind a common SSO and shared library, or moving to a managed system. Sharding limits blast radius but adds operational overhead and fragmented visibility, so measure queue time and controller CPU/heap first.'],
    [4, 'Design a disaster recovery plan for Jenkins.', 'Treat the controller as cattle: JCasC plus a job source of truth in Git (Jenkinsfiles, Job DSL) and plugins pinned in a list, so a new controller can be rebuilt from code. Back up JENKINS_HOME (credentials, build history) to durable storage on a schedule and test restores. Decide RTO/RPO with the business; build history can often be sacrificed, credentials and configuration cannot.'],
    [4, 'How would you migrate 500 Jenkins jobs to GitHub Actions or another managed CI?', 'Inventory jobs, group by pattern, and rank by usage and risk. Build reusable workflows that mirror the shared library, migrate one archetype at a time with parallel runs to compare results, and keep Jenkins for exotic jobs until the end. Trade-offs are lost plugin functionality, re-modelling credentials and self-hosted runner needs, so decommission on a schedule to avoid running both forever.'],
    [4, 'How do you govern a plugin ecosystem across many teams?', 'Maintain an approved plugin list baked into the controller image, run updates through a staging controller with smoke pipelines, subscribe to security advisories, and remove unused plugins. Plugins share a classloader and can conflict or introduce vulnerabilities, so fewer, well-maintained plugins beat convenience. Enforce via image builds rather than manual UI installs.'],
    [4, 'Build a secure supply chain in Jenkins: what would you include?', 'Pinned base images, dependency scanning and SBOM generation, image scanning, signing of artifacts (cosign) with provenance attestation, isolated ephemeral builders with no long-lived credentials (OIDC to cloud), and promotion only of signed artifacts. Protect the Jenkinsfile with branch protection and required reviews so a PR cannot rewrite the pipeline that gates itself.'],
    [3, 'How do you handle pull requests from forks safely in Jenkins?', 'Fork code is untrusted and could modify the Jenkinsfile to steal credentials. Use trust levels in the SCM source (only trust collaborators or use the Jenkinsfile from the target branch), avoid binding secrets in PR builds, run them on isolated ephemeral agents, and require maintainer approval before running builds. This costs contributor convenience but blocks credential theft.'],
    [4, 'How do you make Jenkins builds cost-efficient at scale?', 'Use autoscaled spot or preemptible agents for interruptible work with retries, ephemeral pods sized to actual usage, shared remote caches (build cache, dependency proxy), test impact analysis to run fewer tests, and idle timeouts. Track cost per pipeline and queue time; the trade-off is spot interruptions and cache complexity versus lower spend.'],
    [4, 'How do you provide pipeline standardization without blocking team autonomy?', 'Offer a golden-path shared library with opinionated defaults, and allow overrides through a small declarative config file (for example a pipeline.yaml) rather than arbitrary Groovy. Enforce mandatory security and quality gates centrally and treat everything else as optional. Measure adoption and gather feedback so the platform stays useful rather than a bottleneck.'],
    [4, 'A bad shared library release broke hundreds of pipelines. How do you respond and prevent a repeat?', 'Roll back immediately by moving the pinned tag or default version to the last good release, communicate status, then run a blameless review. Prevent recurrence with library unit and integration tests, canary rollout to a few pilot teams, semver pinning with staged promotion, and a contract test suite of sample pipelines executed on every library change.'],
  ],
  notes: [
    { title: 'Parallel and Matrix', diagram: { type: 'flow', items: ['Prepare', 'Parallel tests', 'Merge results', 'Package'] }, points: ['parallel {} runs stages together', 'matrix runs axes combinations', 'failFast aborts siblings', 'Each branch needs own workspace', 'Publish results in post'], code: 'stage("Test") {\n  parallel {\n    stage("Unit") { steps { sh "make unit" } }\n    stage("Lint") { steps { sh "make lint" } }\n  }\n}' },
    { title: 'Jenkins Security Checklist', points: ['SSO + role-based access', 'Zero executors on controller', 'Folder-scoped credentials', 'Script security sandbox on', 'Update plugins, track advisories', 'Untrusted PRs: no secrets'] },
    { title: 'Shared Library Layout', diagram: { type: 'stack', items: ['Jenkinsfile', '@Library(lib@v2)', 'vars/ global steps', 'src/ classes'] }, points: ['vars = callable steps', 'src = Groovy classes', 'resources = static files', 'Pin versions with tags', 'Test with pipeline unit'], code: '@Library("acme-lib@v2") _\nbuildJava(jdk: 17)' },
    { title: 'Jenkins as Code', diagram: { type: 'flow', items: ['Git repo', 'JCasC YAML', 'Controller image', 'Rebuild anytime'] }, points: ['JCasC config in Git', 'Plugins pinned in a list', 'Secrets from env or vault', 'Back up JENKINS_HOME', 'Test restores'] },
  ],
  cards: [
    [1, 'Where does a Jenkinsfile live?', 'In the root of the repo it builds, versioned with the code.'],
    [2, 'stash vs archiveArtifacts?', 'stash passes files within one run; archiveArtifacts keeps them with the build.'],
    [3, 'What is @NonCPS for?', 'Marks Groovy methods that run outside the CPS interpreter, needed for non-serializable objects, but they cannot call pipeline steps.'],
    [3, 'Why zero executors on the controller?', 'Builds on the controller can read its secrets and hurt stability; agents isolate the work.'],
    [3, 'What does JCasC give you?', 'A reproducible, reviewable controller configuration in YAML instead of UI clicks.'],
    [3, 'Safe way to build images in K8s agents?', 'Kaniko or BuildKit, not mounting the host Docker socket.'],
    [4, 'Danger of untrusted fork PRs?', 'They can edit the Jenkinsfile to steal credentials; use trust settings and no secrets.'],
    [4, 'Fix for a bad shared library release?', 'Roll back the pinned version, then add tests and staged rollout.'],
    [4, 'How to scale beyond one controller?', 'Shard by team or domain with shared SSO and library, or move to managed CI.'],
    [4, 'Jenkins DR strategy in one line?', 'Rebuild from code (JCasC, Jenkinsfiles) and back up JENKINS_HOME for credentials and history.'],
  ],
};

export const cicd = {
  questions: [
    [1, 'What is a quality gate?', 'An automated check in the pipeline that must pass before code moves on, such as test coverage, static analysis rules or no critical vulnerabilities. It stops bad changes early and makes standards enforceable rather than optional.'],
    [1, 'Why should CI builds be fast and what is a good approach to keep them so?', 'Slow feedback discourages frequent commits and delays fixes. Run fast unit tests first, parallelize slower suites, cache dependencies and only rebuild what changed.'],
    [2, 'What is a feature flag and how does it relate to deployment?', 'A runtime switch that turns functionality on or off without redeploying. It decouples deployment from release, enabling dark launches, gradual rollouts and instant disable. The cost is flag debt, so remove old flags and test flag combinations.'],
    [2, 'What is Infrastructure as Code and why use it in CI/CD?', 'Defining infrastructure in version-controlled files (Terraform, CloudFormation, Pulumi) so environments are reproducible and reviewable. Pipelines can plan, review and apply changes, and detect drift instead of relying on manual console edits.'],
    [2, 'How do you manage configuration and secrets across environments?', 'Keep code identical and inject environment-specific config at deploy time from config maps or parameter stores, with secrets from a secret manager. Never bake secrets into images or commit them, and rotate them regularly.'],
    [2, 'What is a flaky test and how do you handle it?', 'A test that passes and fails without code changes, usually due to timing, shared state or external dependencies. Quarantine it so it does not block the pipeline, track it, and fix the root cause; blind retries hide real problems.'],
    [3, 'Explain the expand/contract pattern for zero-downtime schema changes.', 'Expand: add new columns or tables in a backward-compatible way while the old version still works. Migrate data and deploy code that uses the new schema, then contract by removing old structures in a later release. It supports rolling and canary deploys and rollback, but takes multiple releases.'],
    [3, 'How do you implement automated rollback in a canary deployment?', 'Define SLO-based success criteria (error rate, latency, saturation) and let a controller such as Argo Rollouts or Flagger compare canary to baseline at each traffic step. If a metric breaches its threshold the rollout aborts and traffic returns to the stable version. Trade-off: good metrics and enough traffic are required, otherwise the analysis is noise.'],
    [3, 'How do you speed up a pipeline that takes 45 minutes?', 'Profile stages first. Then parallelize independent work, cache dependencies and build layers, split tests into shards, run only affected tests, use faster runners and move slow suites to a post-merge or nightly stage. Keep a fast pre-merge path so the trade-off in coverage is deliberate.'],
    [3, 'How do you secure the pipeline itself against supply chain attacks?', 'Pin dependencies and actions by digest or hash, use a private proxy registry, sign artifacts and verify signatures at deploy, generate SBOMs and provenance (SLSA), give runners short-lived OIDC credentials and least privilege, and require reviews for pipeline definition changes. The pipeline is a high-value target because it holds deploy rights.'],
    [3, 'Monorepo vs polyrepo for CI/CD: what are the trade-offs?', 'Monorepo eases shared code and atomic changes but needs path-based triggers, build caching and tools like Bazel or Nx to avoid rebuilding everything. Polyrepo gives clear ownership and simple pipelines but makes cross-repo changes and version alignment harder. Choose by team structure and coupling.'],
    [3, 'What is progressive delivery?', 'Releasing gradually with control and feedback: canaries, feature flags, traffic shadowing and automated analysis, rather than all-at-once. It limits blast radius and lets metrics decide whether to continue, at the cost of more tooling and observability requirements.'],
    [3, 'How do you handle environment promotion and approvals without becoming a bottleneck?', 'Promote the same immutable artifact through environments with automated gates (tests, scans, policy checks) and reserve manual approval for genuinely high-risk changes. Pre-approved change types and audit trails satisfy compliance while keeping flow.'],
    [4, 'How do you use DORA metrics to drive improvement without gaming them?', 'Use them as team-level diagnostic signals, not targets or individual performance scores. Pair throughput measures (deploy frequency, lead time) with stability (change failure rate, time to restore) so speed is not bought with quality. Investigate bottlenecks behind the numbers, such as review wait time or flaky tests, rather than pushing the numbers directly.'],
    [4, 'A deployment caused a production outage. How do you run the incident and what changes afterwards?', 'Stabilize first by rolling back or disabling the flag, communicate status, then hold a blameless post-incident review to find contributing factors. Improve the system: smaller batch size, better canary checks, automated rollback, tests for the failure class and clearer ownership. Focus on preventing recurrence in the process rather than blaming a person.'],
    [4, 'Design a multi-region deployment pipeline for a critical service.', 'Deploy region by region in waves (one canary region, then others) using the same signed artifact, with automated health analysis between waves and a global kill switch. Keep configuration per region in Git, and ensure schema changes are backward compatible because regions run mixed versions. Trade-off: slower full rollout versus a much smaller blast radius.'],
    [4, 'How do you build a platform engineering approach for CI/CD?', 'Offer a self-service paved road: templates, standardized pipelines, environments on demand and built-in security, treated as a product with users, documentation and feedback loops. Teams may step off the road but own the consequences. Success is measured through adoption, lead time and developer satisfaction rather than tool count.'],
    [3, 'How do you decide between GitOps pull-based and push-based deployment?', 'Pull-based (Argo CD, Flux) keeps cluster credentials inside the cluster, gives continuous drift correction and a clean audit trail in Git. Push-based pipelines are simpler to start and better for non-Kubernetes targets or orchestrated sequences. Many teams combine CI for build and test with GitOps for deploy.'],
    [4, 'How do you deal with database and stateful changes in a continuous deployment model?', 'Separate schema deployment from application release, make every migration backward compatible, and test migrations against production-sized data for lock time. Use online schema-change tools for large tables, feature flags to switch code paths, and backups plus tested restore for the irreversible cases. Not everything can roll back, so design forward fixes.'],
    [4, 'How do you justify investment in CI/CD improvements to leadership?', 'Tie work to business outcomes: lead time to market, incident cost, developer hours lost to waiting and failures. Use baseline DORA and pipeline data, propose a small pilot with measurable targets, and report before and after. Avoid tool-centric arguments; frame as risk reduction and throughput gain.'],
    [4, 'How do you handle compliance and audit requirements (SOX, PCI) in a continuous delivery model?', 'Codify controls as pipeline policy: mandatory reviews, separation of duties enforced through branch protection, automated evidence collection, immutable logs and signed artifacts. Auditors get traceability from commit to production without manual tickets. This shifts compliance from periodic paperwork to continuous, automated verification.'],
    [4, 'Explain the trade-offs of ephemeral preview environments per pull request.', 'They give realistic testing and fast feedback per change, and remove environment contention. Costs include infrastructure spend, data seeding, secrets handling and cleanup of orphaned environments. Mitigate with auto-expiry, shared services with namespaces and lightweight data fixtures.'],
  ],
  notes: [
    { title: 'Progressive Delivery Loop', diagram: { type: 'flow', items: ['Deploy 5%', 'Analyze metrics', 'Promote or abort', 'Full rollout'] }, points: ['Metrics decide, not people', 'Compare canary to baseline', 'Abort returns traffic', 'Needs enough traffic', 'Flags add a kill switch'] },
    { title: 'Expand / Contract Migration', diagram: { type: 'flow', items: ['Add column', 'Dual write', 'Backfill + switch', 'Drop old'] }, points: ['Each step is backward compatible', 'Old and new code both run', 'Spread over several releases', 'Rollback stays possible'] },
    { title: 'Pipeline Security', diagram: { type: 'stack', items: ['Signed artifact', 'SBOM + scan', 'Pinned deps', 'Least-privilege runner'] }, points: ['Pin actions and images by digest', 'OIDC instead of static keys', 'Review pipeline changes', 'Verify signatures at deploy', 'Provenance (SLSA)'] },
    { title: 'GitOps Flow', diagram: { type: 'flow', items: ['CI builds image', 'Update manifest', 'Argo CD syncs', 'Cluster matches Git'] }, points: ['Git is desired state', 'Operator pulls, no push creds', 'Rollback = git revert', 'Drift is detected and healed'], code: 'kubectl -n argocd get applications\nargocd app sync my-app' },
  ],
  cards: [
    [1, 'What is a quality gate?', 'An automated pass/fail check that must succeed before the pipeline proceeds.'],
    [2, 'Feature flag benefit?', 'Decouples deploy from release; enables gradual rollout and instant disable.'],
    [3, 'Expand/contract in one line?', 'Add backward-compatible schema first, switch code, remove old structure later.'],
    [3, 'What decides canary promotion?', 'SLO metrics compared to baseline, evaluated automatically per traffic step.'],
    [3, 'Push vs pull deploy?', 'Pull (GitOps) keeps creds in the cluster and heals drift; push is simpler to start.'],
    [3, 'Why pin actions by digest?', 'Tags can be moved; a digest guarantees the exact code and blocks supply chain tampering.'],
    [4, 'DORA stability metrics?', 'Change failure rate and time to restore service.'],
    [4, 'Danger of gaming DORA?', 'Speed without stability; use as team diagnostics, not targets.'],
    [4, 'Multi-region rollout approach?', 'Waves per region with automated health checks and a kill switch.'],
    [4, 'Compliance in CD?', 'Codify controls as pipeline policy with automated evidence and signed artifacts.'],
  ],
};

export const aws = {
  questions: [
    [1, 'What are S3 storage classes and lifecycle rules?', 'Storage classes trade cost against access pattern, such as Standard for frequent access, Infrequent Access, and Glacier tiers for archives. Lifecycle rules automatically transition or expire objects by age, cutting cost without manual work.'],
    [1, 'What is the shared responsibility model?', 'AWS secures the cloud itself (hardware, facilities, core services), while the customer secures what they put in it: data, IAM, network configuration, OS patching on EC2 and application code. The split shifts with the service, with managed services leaving less to the customer.'],
    [2, 'What is the difference between an IAM role and an instance profile, and why prefer roles over access keys?', 'A role is an identity assumed to receive temporary credentials; an instance profile is the container that attaches a role to an EC2 instance. Temporary credentials rotate automatically and cannot leak as long-lived keys in code or config, so they are safer.'],
    [2, 'S3 consistency, versioning and how do you protect against accidental deletion?', 'S3 provides strong read-after-write consistency. Enable versioning so deletes create markers and old versions remain, add MFA delete or Object Lock for immutability, and use replication or backups for resilience against account-level mistakes.'],
    [2, 'What are VPC endpoints and why use them?', 'They let resources in a VPC reach AWS services privately without traversing the internet or NAT. Gateway endpoints cover S3 and DynamoDB; interface endpoints (PrivateLink) cover many other services. They improve security and can reduce NAT data charges.'],
    [2, 'How do CloudFormation and Terraform compare?', 'CloudFormation is AWS-native with stack state managed by AWS and deep service coverage. Terraform is multi-cloud with a plan/apply workflow and explicit state file that must be stored and locked remotely. Choose by multi-cloud needs, team skills and module ecosystem.'],
    [3, 'How does IAM policy evaluation work when several policy types apply?', 'An explicit Deny in any applicable policy wins. Otherwise access needs an Allow, and if SCPs or permission boundaries are in play the action must be allowed by all of them (their intersection with the identity policy). Resource policies can grant cross-account access independently in some cases, so evaluate the whole set.'],
    [3, 'Design a private VPC network for a three-tier app with hybrid connectivity.', 'Use subnets per tier per AZ (public for load balancers, private app, isolated data), NAT gateways per AZ for egress, and VPC endpoints for AWS services. Connect on-premises via Site-to-Site VPN or Direct Connect with Transit Gateway for scale. Trade-off: NAT per AZ costs more but avoids cross-AZ dependency and charges.'],
    [3, 'Explain S3 performance and design considerations for a data lake.', 'Use prefixes and multipart upload for parallel throughput, columnar formats such as Parquet with partitioning for Athena and Glue, and lifecycle policies to tier cold data. Secure with bucket policies, encryption and Lake Formation permissions. Small files hurt query performance, so compact them.'],
    [3, 'Compare DynamoDB capacity modes and how to avoid hot partitions.', 'On-demand suits unpredictable traffic and is simpler; provisioned with auto scaling is cheaper for steady load. Hot partitions come from skewed partition keys, so choose high-cardinality keys, add write sharding suffixes if needed, and design for access patterns. Use DAX or caching for read-heavy hot keys.'],
    [3, 'How do you achieve zero-downtime deployments on ECS or EC2 behind an ALB?', 'Use rolling or blue/green deployments with CodeDeploy or ECS deployment controllers, ALB health checks and connection draining. Shift traffic gradually using weighted target groups and auto-roll back on alarms. Ensure backward-compatible database changes since old and new versions overlap.'],
    [3, 'When would you use SQS with a dead-letter queue, and how do you keep processing idempotent?', 'Use SQS to buffer and decouple; failed messages exceeding maxReceiveCount move to a DLQ for inspection and redrive. Delivery is at-least-once for standard queues, so consumers must be idempotent, for example by using an idempotency key stored in DynamoDB. FIFO queues give ordering and deduplication with lower throughput.'],
    [3, 'How do you monitor and troubleshoot an AWS workload?', 'CloudWatch metrics, logs and alarms for the baseline, X-Ray or OpenTelemetry for traces, CloudTrail for API audit, VPC Flow Logs for network questions and Config for resource change history. Define SLO-based alarms and dashboards, and avoid alert noise by alerting on symptoms rather than every resource metric.'],
    [4, 'Design a multi-account strategy for an enterprise.', 'Use AWS Organizations with organizational units for security, infrastructure, sandbox and workload accounts, per environment or per workload. Apply SCPs as guardrails, centralize logging and security tooling in dedicated accounts, and use IAM Identity Center for access. Trade-off: more accounts give isolation and cleaner billing but need automation for networking and account vending.'],
    [4, 'How do SCPs and permission boundaries differ, and when do you use each?', 'SCPs set maximum permissions at the account or OU level and never grant access themselves. Permission boundaries cap what a specific IAM role or user can be granted, allowing safe delegation, such as letting developers create roles that cannot exceed a boundary. Use SCPs for org-wide guardrails and boundaries for delegated administration.'],
    [4, 'Design a DR plan for a critical application with RPO of minutes and RTO under an hour.', 'Warm standby in a second Region: data replicated via Aurora Global Database or DynamoDB global tables, infrastructure defined in code and kept scaled down, Route 53 health-check failover. Rehearse failover regularly and automate runbooks. Trade-off against active-active is lower cost but a scaling delay and more operational steps during failover.'],
    [4, 'How would you architect a cost governance program on AWS?', 'Enforce tagging and account structure for allocation, set budgets and anomaly detection, review rightsizing and idle resources, and commit through Savings Plans after measuring baseline usage. Build cost into engineering by showing teams their spend. Avoid over-committing before workloads stabilize, since commitments reduce flexibility.'],
    [3, 'Transit Gateway vs VPC peering for many VPCs: how do you decide?', 'Peering is simple, low latency and cheap for a few VPCs but is non-transitive and becomes a mesh. Transit Gateway acts as a central hub with route tables for segmentation, scaling to many VPCs and on-premises links, with per-attachment and data processing charges. Choose Transit Gateway once the mesh becomes unmanageable or you need central inspection.'],
    [4, 'Design secure cross-account access for a CI/CD pipeline deploying to many accounts.', 'The pipeline account assumes a narrowly scoped deployment role in each target account, using OIDC federation so CI has no static keys. Constrain roles with least-privilege policies, external conditions and SCPs, and log everything with CloudTrail. Trade-off: many roles need automation to maintain, but avoiding shared long-lived credentials limits blast radius.'],
    [4, 'How would you migrate a large on-premises workload to AWS?', 'Assess and group workloads, pick a strategy per application (rehost, replatform, refactor, retire), build a landing zone first, and migrate in waves starting with low-risk systems. Use replication tooling for data and run parallel validation before cutover. Trade-off: lift-and-shift is fast but keeps inefficiencies, while refactoring gains more benefits at higher cost and risk.'],
    [4, 'How do you design for tenant isolation in a multi-tenant SaaS on AWS?', 'Options range from pool (shared resources with tenant IDs and enforced access controls) to silo (account or stack per tenant). Use IAM conditions or row-level controls, tenant-scoped encryption keys and per-tenant metrics. Silo gives strongest isolation and easier compliance but higher cost and operations, so many use a tiered hybrid.'],
    [4, 'A production account was compromised via leaked credentials. How do you respond and harden?', 'Contain by disabling or rotating the exposed keys, revoking active sessions and isolating affected resources, then use CloudTrail to scope the activity. Eradicate persistence such as new users, roles or access keys, and restore from clean state. Harden by removing long-lived keys, enforcing MFA, adding GuardDuty and SCP guardrails, and holding a blameless review.'],
  ],
  notes: [
    { title: 'IAM Policy Evaluation', diagram: { type: 'flow', items: ['Explicit Deny?', 'SCP allows?', 'Boundary allows?', 'Identity Allow?'] }, points: ['Explicit deny always wins', 'Default is implicit deny', 'SCP and boundary only limit', 'They never grant on their own', 'Resource policies can add access'] },
    { title: 'Multi-Account Layout', diagram: { type: 'stack', items: ['Management account', 'Security / Log OU', 'Infrastructure OU', 'Workload OUs'] }, points: ['SCPs as guardrails', 'Central logging account', 'IAM Identity Center for SSO', 'Account vending automated', 'Account = strong boundary'] },
    { title: 'DR Ladder', diagram: { type: 'flow', items: ['Backup/restore', 'Pilot light', 'Warm standby', 'Active-active'] }, points: ['Cost rises left to right', 'RTO and RPO fall', 'Pick from business need', 'Test failover regularly', 'Automate the runbook'] },
    { title: 'Private Connectivity', points: ['Gateway endpoint: S3, DynamoDB', 'Interface endpoint = PrivateLink', 'NAT per AZ for egress', 'Transit Gateway = hub for many VPCs', 'Peering is non-transitive'], code: 'aws ec2 create-vpc-endpoint \\\n  --vpc-id vpc-123 \\\n  --service-name com.amazonaws.us-east-1.s3 \\\n  --route-table-ids rtb-456' },
  ],
  cards: [
    [1, 'Shared responsibility model?', 'AWS secures the cloud; you secure what you put in it.'],
    [2, 'Gateway endpoint services?', 'S3 and DynamoDB.'],
    [3, 'Explicit Deny vs Allow?', 'An explicit Deny always overrides any Allow.'],
    [3, 'Does an SCP grant permissions?', 'No, it only sets the maximum; identity policies must still allow.'],
    [3, 'Standard SQS delivery guarantee?', 'At-least-once, so consumers must be idempotent.'],
    [3, 'Cause of DynamoDB hot partitions?', 'Skewed or low-cardinality partition keys.'],
    [4, 'Permission boundary use?', 'Caps the permissions a delegated role or user can ever get.'],
    [4, 'Peering vs Transit Gateway?', 'Peering is non-transitive and meshy; TGW is a scalable hub.'],
    [4, 'Warm standby meaning?', 'A scaled-down but fully functional copy in another Region, scaled up on failover.'],
    [4, 'First step after leaked keys?', 'Disable or rotate them, revoke sessions, then investigate with CloudTrail.'],
  ],
};

export const architecture = {
  questions: [
    [1, 'What is the difference between horizontal and vertical scaling?', 'Vertical scaling adds more CPU or memory to one machine and is simple but has a ceiling and single point of failure. Horizontal scaling adds more instances behind a load balancer, giving elasticity and resilience but requiring stateless or partitioned design.'],
    [1, 'What is a load balancer and why use one?', 'It distributes incoming requests across multiple servers to improve capacity and availability, using health checks to avoid failed nodes. It can operate at layer 4 or layer 7 and often terminates TLS.'],
    [2, 'What is idempotency and why does it matter in distributed systems?', 'An operation is idempotent if repeating it has the same effect as doing it once. Networks cause retries and duplicate messages, so idempotent handlers, often using an idempotency key, make retries safe and prevent double charges or duplicate records.'],
    [2, 'Message queue vs event streaming platform?', 'A queue delivers each message to one consumer and removes it after processing, suited to task distribution. A log-based stream such as Kafka retains ordered events so many consumers can read and replay independently. Choose by whether you need replay and fan-out or simple work distribution.'],
    [2, 'What is database sharding and what are its trade-offs?', 'Splitting data across nodes by a shard key to scale writes and storage. Trade-offs include cross-shard queries and transactions being hard, rebalancing complexity and hot shards if the key is skewed. Try indexing, replicas and caching first.'],
    [2, 'What are SLIs, SLOs and error budgets?', 'An SLI measures behaviour (such as request success rate), an SLO is the target for it, and the error budget is the allowed unreliability. When the budget is spent, teams slow feature work to focus on reliability, which balances velocity and stability.'],
    [3, 'Explain the outbox pattern and what problem it solves.', 'A service writes its data change and an outbox event in the same local transaction; a separate relay publishes outbox rows to the broker. This avoids the dual-write problem where the database commit succeeds but the message publish fails. Delivery is at-least-once, so consumers must be idempotent.'],
    [3, 'How do you choose between choreography and orchestration for a saga?', 'Choreography is decentralized with services reacting to events: loosely coupled but the flow is hard to see and debug as it grows. Orchestration uses a central coordinator that makes the flow explicit and easier to monitor, at the cost of a coordinating component and some coupling. Prefer orchestration for complex multi-step processes.'],
    [3, 'What are the pitfalls of event sourcing?', 'Event schema evolution requires upcasting and versioning, rebuilding state needs snapshots for performance, queries need projections that are eventually consistent, and deleting data (privacy laws) is awkward. It fits domains needing a full audit trail, but adds significant complexity if used everywhere.'],
    [3, 'How do you design a rate limiter?', 'Common algorithms are token bucket, leaky bucket and sliding window, keyed by user, API key or IP. For distributed setups keep counters in Redis with atomic operations or enforce at the gateway. Trade-offs: strict global accuracy costs latency, so approximate local limits with periodic sync are often acceptable.'],
    [3, 'Explain consistent hashing and where it is used.', 'Keys and nodes are hashed onto a ring and each key is assigned to the next node clockwise, so adding or removing a node moves only a fraction of keys. Virtual nodes even out the load. It is used in distributed caches, Dynamo-style stores and sharded systems.'],
    [3, 'How do you handle cache stampede and inconsistent cache after writes?', 'For stampedes use request coalescing (single flight), locks or early refresh with jittered TTLs, and serve stale while revalidating. For consistency, invalidate or update on write, or use short TTLs; accept that cache-aside can serve stale data briefly. Pick by how costly staleness is.'],
    [3, 'What is the strangler fig pattern?', 'Incrementally replacing a legacy system by routing selected functionality through a facade to new services while the old system continues serving the rest. Over time the legacy shrinks and is retired. It lowers risk compared to a big-bang rewrite but requires maintaining both and careful data synchronization.'],
    [3, 'How do you define service boundaries in a microservices design?', 'Use domain-driven design bounded contexts aligned to business capabilities and team ownership, keep data private to each service and prefer coarse boundaries at first. Signs of wrong boundaries are chatty calls, shared databases and coordinated releases. Splitting later is easier than merging badly split services.'],
    [4, 'Discuss CAP and PACELC with a real design decision.', 'CAP says that during a partition you choose consistency or availability; PACELC adds that even without a partition you trade latency against consistency. For example a shopping cart can favour availability with eventual consistency, while an account balance ledger favours consistency. State the choice per use case, not per system.'],
    [4, 'Your system has cascading failures under load. How do you diagnose and redesign?', 'Look for retry storms, unbounded queues, exhausted thread or connection pools and slow dependencies. Introduce timeouts, retry budgets with backoff and jitter, circuit breakers, bulkheads and load shedding with backpressure. Test with load and chaos experiments, since failure modes usually appear only under stress.'],
    [4, 'How would you decompose a monolith with a shared database?', 'First modularize inside the monolith, then identify seams and break data ownership step by step, using the strangler pattern, change data capture or an anti-corruption layer. Avoid a distributed monolith where services still share tables. Expect a long period of dual running and invest in observability and automated tests before splitting.'],
    [4, 'How do you design for multi-region active-active and what makes it hard?', 'Requires data replication with conflict handling (last-writer-wins, CRDTs or region-affinity for writes), global traffic routing and idempotent event handling. Hard parts are consistency, cross-region latency, failover testing and cost. Many systems choose active-passive or partitioned ownership by tenant because full active-active is expensive and complex.'],
    [4, 'What makes an effective ADR and architecture governance process?', 'An ADR records context, decision, alternatives, consequences and status, is short, stored with the code and immutable except for superseding. Governance should be lightweight: guardrails and fitness functions checked in the pipeline, plus review for high-impact decisions. Heavy approval boards slow teams, so favour advice from architects with team autonomy.'],
    [4, 'How do you design an observability strategy for a distributed system?', 'Standardize on OpenTelemetry for logs, metrics and traces with correlation IDs, define SLOs per user journey, and alert on symptoms and error budget burn rather than causes. Control cost with sampling and retention tiers. Provide dashboards and runbooks, and make instrumentation part of the definition of done.'],
    [4, 'Design a system that must process payments exactly once.', 'True exactly-once delivery is not achievable across networks; instead combine at-least-once delivery with idempotency keys and deduplication so the effect happens once. Persist state transitions transactionally, use the outbox pattern and reconcile against the payment provider periodically. Trade-off: extra storage and complexity, but this is the standard safe approach.'],
    [4, 'How do you evolve APIs and schemas without breaking consumers?', 'Prefer additive, backward-compatible changes, version explicitly when breaking, and use consumer-driven contract tests plus schema registries for events. Deprecate with published timelines and monitor usage before removal. Tolerant readers and expand/contract for data changes let producers and consumers deploy independently.'],
  ],
  notes: [
    { title: 'Outbox Pattern', diagram: { type: 'flow', items: ['Local transaction', 'Outbox table', 'Relay / CDC', 'Message broker'] }, points: ['Data + event in one commit', 'Fixes the dual-write problem', 'Delivery is at-least-once', 'Consumers must be idempotent'] },
    { title: 'CQRS Shape', diagram: { type: 'flow', items: ['Command', 'Write model', 'Events', 'Read model'] }, points: ['Separate write and read models', 'Read side is eventually consistent', 'Scale each side independently', 'Adds complexity: use selectively'] },
    { title: 'PACELC Quick Guide', points: ['Partition: choose A or C', 'Else: choose latency or C', 'Cart: favour availability', 'Ledger: favour consistency', 'Decide per use case'] },
    { title: 'Strangler Migration', diagram: { type: 'flow', items: ['Facade / proxy', 'Route one feature', 'New service', 'Retire legacy'] }, points: ['No big-bang rewrite', 'Legacy shrinks slowly', 'Beware shared database', 'Sync data via CDC or ACL'] },
  ],
  cards: [
    [1, 'Vertical vs horizontal scaling?', 'Vertical: bigger machine. Horizontal: more machines behind a load balancer.'],
    [2, 'What is an error budget?', 'The allowed unreliability implied by the SLO; spending it pauses risky change.'],
    [3, 'Outbox pattern solves what?', 'The dual-write problem between a database commit and a message publish.'],
    [3, 'Consistent hashing benefit?', 'Adding or removing a node moves only a small fraction of keys.'],
    [3, 'Cache stampede fix?', 'Request coalescing, jittered TTLs or stale-while-revalidate.'],
    [3, 'Strangler fig in one line?', 'Route features gradually to new services until the legacy system is retired.'],
    [4, 'What does PACELC add to CAP?', 'Without a partition you still trade latency against consistency.'],
    [4, 'Exactly-once payments how?', 'At-least-once delivery plus idempotency keys and deduplication.'],
    [4, 'Danger of shared database in microservices?', 'It creates a distributed monolith with hidden coupling.'],
    [4, 'What goes in an ADR?', 'Context, decision, alternatives, consequences and status.'],
  ],
};
