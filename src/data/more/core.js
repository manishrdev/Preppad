export const java = {
  questions: [
    [1, `What is autoboxing and what pitfall does it introduce?`, `Autoboxing converts between primitives and wrapper types automatically (int <-> Integer). Pitfalls: NullPointerException when unboxing a null, == on Integer compares references (only cached -128..127 are identical), and hidden allocation in tight loops which hurts performance. Prefer primitives in hot paths.`],
    [1, `What is the difference between an interface default method and an abstract class method?`, `A default method gives an implementation inside an interface so you can add behavior without breaking implementers. Interfaces still cannot hold instance state, while abstract classes can have fields and constructors. Conflicts between two defaults must be resolved explicitly with Interface.super.method().`],
    [2, `What is the difference between fail-fast and fail-safe iterators?`, `Fail-fast iterators (ArrayList, HashMap) throw ConcurrentModificationException if the collection is structurally modified during iteration, detected via modCount; this is best-effort, not guaranteed. Concurrent collections (CopyOnWriteArrayList, ConcurrentHashMap) give snapshot or weakly consistent iterators that never throw it. Trade-off: copy-on-write costs memory and write time.`],
    [2, `How does try-with-resources work and what are suppressed exceptions?`, `Any AutoCloseable declared in the try header is closed automatically in reverse order, even on exceptions. If both the body and close() throw, the body exception is primary and the close exception is attached via addSuppressed(), retrievable with getSuppressed(). This avoids the old finally pattern where the close exception masked the real one.`],
    [2, `Explain generics type erasure and its consequences.`, `Generic type parameters are erased at compile time to their bounds (usually Object), with casts inserted by the compiler. Consequences: no new T(), no instanceof List<String>, no generic arrays, overloads that differ only by type arguments clash, and reflection sees raw types (unless captured via a superclass token). Bridge methods are generated to keep polymorphism working.`],
    [2, `What is the difference between wait/notify and Lock/Condition?`, `wait/notify work with an object's intrinsic monitor and must be called inside synchronized; one wait set per object. Lock with Condition offers multiple condition queues per lock, tryLock, timed and interruptible acquisition, and fairness options. Always wait in a loop to guard against spurious wakeups. Trade-off: explicit locks require disciplined unlock in finally.`],
    [3, `A service shows long GC pauses only after a deploy. How do you investigate?`, `Enable GC logging (-Xlog:gc*) and compare pause causes, allocation rate, promotion rate and old-gen occupancy before and after the deploy. Look for humongous allocations in G1, premature promotion from new large caches, or a changed heap/region size. Take a heap histogram or JFR allocation profile to find the new allocator, then fix the code or retune (region size, IHOP, MaxGCPauseMillis). Roll back first if user impact is high.`],
    [3, `Explain how the JIT compiler works: tiered compilation, inlining and deoptimization.`, `Code starts interpreted, then C1 compiles hot methods with light profiling, and C2 recompiles the hottest with aggressive optimizations (inlining, escape analysis, loop unrolling) based on profile data. When a speculative assumption breaks (a new subclass loaded, an unexpected branch), the JVM deoptimizes back to the interpreter. Consequence: microbenchmarks need warmup (use JMH) and cold-start latency is real.`],
    [3, `What is escape analysis and when does it help?`, `C2 analyzes whether an object reference escapes its method or thread. If not, it can scalar-replace the object (fields kept in registers, no heap allocation) and elide locks on it. It reduces GC pressure for short-lived temporaries, but it is not guaranteed, is defeated by large methods that are not inlined, and should not be relied on for correctness.`],
    [3, `How does ClassLoader delegation work and how can it cause NoClassDefFoundError or LinkageError?`, `Loaders ask their parent first (bootstrap, platform, application), so core classes cannot be replaced. Web containers and plugin systems invert or isolate this, so the same class name can be loaded twice by different loaders and a cast between them fails with ClassCastException. NoClassDefFoundError usually means a class was present at compile time but its static initializer failed or it is missing at runtime; check dependency versions and shaded jars.`],
    [3, `Why can double-checked locking fail without volatile, and what are the alternatives?`, `Without volatile, the reference may be published before the constructor's writes are visible to another thread, so it sees a half-constructed object due to reordering. Declaring the field volatile establishes a happens-before edge and fixes it. Simpler alternatives are the initialization-on-demand holder idiom or an enum singleton, both relying on class-initialization guarantees.`],
    [3, `How do you find and fix thread contention in a production service?`, `Take several thread dumps (jstack) or a JFR profile and look for many threads BLOCKED or WAITING on the same monitor or lock. Fix by shrinking critical sections, using striped or lock-free structures (ConcurrentHashMap, LongAdder), splitting locks, or removing shared mutable state. Verify with the same profile; beware that raising thread counts often worsens contention.`],
    [3, `What are the trade-offs of using Java serialization and what do you use instead?`, `Native serialization is fragile (tied to class structure and serialVersionUID), slow, bloats payloads, and is a well-known security risk because deserializing untrusted data can execute gadget chains. Prefer JSON, Protobuf or Avro with explicit schemas, and if you must deserialize, use ObjectInputFilter to allow-list classes. Schema-based formats also give cross-language and versioning support.`],
    [4, `Design a low-latency trading or ad-serving service in Java. What JVM-level decisions matter?`, `Minimize allocation on the hot path (object reuse, primitive collections, off-heap buffers), pick a low-pause collector (ZGC or Shenandoah) or tune G1, pre-touch heap and warm up JIT before taking traffic. Pin critical threads, avoid contended locks and use ring buffers (Disruptor-style) for handoff. Measure p99/p999 with HdrHistogram, and watch for safepoint pauses and coordinated omission in your benchmarks.`],
    [4, `Your service has a p99 spike every few minutes but CPU and heap look normal. How do you find the cause?`, `Correlate spikes with GC logs, safepoint logs (-Xlog:safepoint), JIT deoptimization and compilation events, and infrastructure signals such as CPU throttling in containers, noisy neighbors, or GC threads sized from host cores. JFR gives latency events with low overhead. Common culprits are time-to-safepoint delays from long counted loops, cgroup CPU limits, and periodic cache refresh or log rotation jobs.`],
    [4, `How would you size and configure a JVM inside Kubernetes containers?`, `Use container-aware JVMs (default since Java 10) and set -XX:MaxRAMPercentage rather than a fixed -Xmx so heap follows the limit, leaving headroom for metaspace, thread stacks, direct buffers and code cache. Set CPU requests and limits consciously because ActiveProcessorCount drives GC and ForkJoin thread counts. OOMKilled with a healthy heap means non-heap memory, so enable Native Memory Tracking to confirm.`],
    [4, `Monolith to microservices: how do you handle a Java shared-library dependency hell across 40 services?`, `Avoid a shared domain library that couples releases; share only stable contracts (API schemas, a thin platform BOM) and version them semantically. Use a BOM or Gradle version catalog to align transitive dependencies, enforce with dependency-convergence checks in CI, and let teams upgrade on a schedule with automated PRs (Renovate/Dependabot). Prefer duplication over a wrong shared abstraction for business logic.`],
    [4, `What strategies do you use for zero-downtime deployment and graceful shutdown of a Java service?`, `On SIGTERM, fail readiness first so the load balancer stops routing, wait a drain period, then stop accepting work, finish in-flight requests with a bounded timeout, close pools and flush queues. Use rolling or blue/green deployment with backward-compatible schema and API changes (expand then contract). Warm up JIT and caches before marking ready, and make consumers idempotent because redelivery can happen.`],
    [4, `How would you make a large Java codebase safer against null and concurrency bugs at scale?`, `Establish conventions: immutable value types (records), constructor injection, Optional only for return types, and nullability annotations checked by tooling (NullAway, Error Prone, or JSpecify). For concurrency, prefer higher-level constructs and confine mutable state to a single thread or actor, and run static analysis plus stress tests in CI. Tooling in the build beats code review reminders, though it needs gradual rollout on legacy code.`],
    [3, `Explain how you would build a resilient outbound HTTP client layer in Java.`, `Set explicit connect, read and overall timeouts, use connection pooling, and apply retries only for idempotent calls with exponential backoff and jitter. Add circuit breakers and bulkheads (Resilience4j) so one slow dependency cannot exhaust threads, plus a retry budget to avoid retry storms. Propagate deadlines and trace IDs, and expose metrics on latency, errors and pool saturation.`],
    [4, `When would you choose reactive (WebFlux/Reactor) over virtual threads or a classic thread pool?`, `Reactive shines for streaming, backpressure-aware pipelines and very high fan-out with few threads, but it costs readability, debugging and library compatibility. Virtual threads give similar scalability for blocking IO with plain imperative code and are usually the simpler default on Java 21. Classic pools remain fine for moderate concurrency. Choose by workload: backpressure semantics push toward reactive, request-per-thread simplicity toward virtual threads.`],
    [4, `Describe how you would lead an investigation of a native memory leak in a Java service where the heap is stable.`, `Enable -XX:NativeMemoryTracking=summary and diff snapshots with jcmd to see which JVM area grows (threads, metaspace, code cache, GC structures). If NMT is flat, suspect direct ByteBuffers, JNI libraries, or glibc malloc arena fragmentation, and test with MALLOC_ARENA_MAX or jemalloc plus its profiler. Check for classloader leaks on redeploys. Fix the owner and add container memory alerts on RSS, not just heap.`],
  ],
  notes: [
    { title: `Generics Cheat-Sheet`, points: [`PECS: producer extends, consumer super`, `List<?> read-only-ish, unknown type`, `Erased at runtime: no new T()`, `Invariant: List<Integer> is not List<Number>`, `Bounded: <T extends Comparable<T>>`], code: `static <T extends Comparable<T>> T max(List<T> xs) {\n  T best = xs.get(0);\n  for (T x : xs) if (x.compareTo(best) > 0) best = x;\n  return best;\n}` },
    { title: `Thread Lifecycle`, points: [`NEW: created, not started`, `RUNNABLE: running or ready`, `BLOCKED: waiting for monitor`, `WAITING: wait/join/park`, `TERMINATED: run() finished`], diagram: { type: `flow`, items: [`NEW`, `RUNNABLE`, `BLOCKED/WAITING`, `TERMINATED`] } },
    { title: `Concurrency Toolbox`, points: [`AtomicInteger: CAS counters`, `LongAdder: high-contention counts`, `CountDownLatch: one-shot wait`, `Semaphore: limit concurrent access`, `ReentrantLock: tryLock, fairness`, `CompletableFuture: async chains`] },
    { title: `Java Heap Generations`, points: [`Young: Eden + 2 survivors`, `Minor GC copies live objects`, `Survivors age then get promoted`, `Old: long-lived objects`, `Metaspace: native, not heap`], diagram: { type: `stack`, items: [`Eden`, `Survivor S0/S1`, `Old Generation`, `Metaspace (native)`] } },
  ],
  cards: [
    [3, `Default initial capacity of HashMap?`, `16 buckets with load factor 0.75, resize at 12 entries.`],
    [3, `Integer cache range?`, `-128 to 127; == is true only inside this range.`],
    [2, `Which JVM flag sets max heap?`, `-Xmx (or -XX:MaxRAMPercentage in containers).`],
    [3, `What does volatile NOT guarantee?`, `Atomicity of compound actions like i++.`],
    [4, `Default GC since Java 9?`, `G1 (Garbage-First).`],
    [3, `Treeify threshold in HashMap?`, `8 entries per bin (and table size at least 64).`],
    [2, `Difference: Comparable vs Comparator?`, `Comparable = natural order inside the class; Comparator = external, pluggable order.`],
    [3, `What does WeakReference do?`, `Referent is collected at the next GC when only weakly reachable; used in caches like WeakHashMap.`],
    [4, `Cause of OutOfMemoryError: unable to create native thread?`, `OS or container thread/native memory limit hit, not heap exhaustion.`],
    [3, `Which collections keep insertion order?`, `LinkedHashMap, LinkedHashSet, ArrayList; HashMap makes no ordering promise.`],
  ],
};

export const oops = {
  questions: [
    [1, `What is the difference between a class and an object?`, `A class is a blueprint that defines fields and behavior; an object is a runtime instance of that class with its own state. Many objects can be created from one class, each with independent instance data but sharing the class's methods and static members.`],
    [1, `What are access modifiers in Java and when do you use each?`, `private: only within the class. Package-private (default): same package. protected: same package plus subclasses. public: everywhere. Default to the narrowest visibility that works, since it keeps the public API small and easier to change.`],
    [2, `What is coupling vs cohesion and what do you aim for?`, `Cohesion is how strongly a module's responsibilities belong together; coupling is how much modules depend on one another. Aim for high cohesion and low coupling: classes that change for one reason and talk through small interfaces. Over-decoupling adds indirection, so balance against simplicity.`],
    [2, `Explain the Open/Closed Principle with an example.`, `Software entities should be open for extension but closed for modification. Instead of a switch on payment type inside a checkout class, define a PaymentMethod interface and add new implementations without editing checkout. The trade-off is up-front abstraction; apply it where variation is actually expected.`],
    [2, `What is the difference between association, aggregation and composition?`, `Association is a general uses-a relationship. Aggregation is a has-a where the part can exist independently (Team and Player). Composition is a stronger has-a where the part's lifecycle is owned by the whole (Order and OrderLine). It matters for deletion semantics and ownership design.`],
    [2, `What is the Law of Demeter?`, `A method should only talk to its immediate collaborators, not to objects returned by them (avoid a.getB().getC().doX()). It reduces coupling to internal structure. Excessively applying it creates many delegating wrapper methods, so use judgment; fluent builders and data structures are fine exceptions.`],
    [3, `What is the Strategy pattern and how does it differ from State?`, `Strategy lets the client choose an interchangeable algorithm behind an interface (sorting or pricing rules). State also swaps behavior objects, but the object changes them itself as its internal state transitions. Strategy is chosen externally and is usually stateless; State encodes a lifecycle, with transitions between states.`],
    [3, `How does dependency injection relate to Dependency Inversion?`, `Dependency Inversion is the principle that high-level modules depend on abstractions, not concrete details. Dependency injection is a technique that supplies those dependencies from outside (constructor injection preferred). A container like Spring automates wiring but is optional; the win is testability and swappable implementations.`],
    [3, `When is inheritance the right choice? Describe the fragile base class problem.`, `Use it for a true is-a relationship with substitutability and a base class designed for extension. The fragile base class problem is when a change in the parent's internal behavior (e.g. a method that now calls another overridable method) silently breaks subclasses. Mitigate with final classes, documenting self-use, or composition.`],
    [3, `How do you design an immutable class in Java?`, `Make the class final, fields private and final, no setters, initialize in the constructor, and defensively copy mutable inputs and outputs (collections, dates). Immutable objects are inherently thread-safe and safe map keys. The cost is object creation for updates; use builders or withers, or records for simple cases.`],
    [3, `Explain the Builder pattern and when it beats telescoping constructors.`, `Builder constructs an object step by step through named setters and a final build(), yielding readable code and validation at build time. It beats constructors with many optional parameters and supports immutability. Trade-off: extra boilerplate, mitigated by Lombok or records with static factories when parameter counts are small.`],
    [3, `What is the Observer pattern and what problems does it introduce at scale?`, `Subjects notify registered observers of state changes, decoupling producers from consumers. Problems: memory leaks from forgotten listeners (lapsed listener), unpredictable ordering, cascading updates, and exceptions in one observer affecting others. For cross-service needs move to an event bus or message broker with explicit delivery semantics.`],
    [3, `How do you handle equals, hashCode and inheritance for value objects?`, `Symmetry breaks when a subclass adds fields and compares itself with its parent via instanceof. Either make the value class final, use getClass() comparisons consistently, or prefer composition over subclassing. Records generate correct equals/hashCode for all components, which is the simplest modern answer.`],
    [4, `A team's domain model is an anemic set of getters/setters plus large service classes. How do you refactor incrementally?`, `Identify invariants that are enforced in services and move them into the entities or value objects that own the data. Introduce value objects for primitives with rules (Money, Email), make setters private and express operations as intention-revealing methods. Refactor behind existing tests, one aggregate at a time, so the change is safe. Do not rewrite everything, as anemic models are sometimes fine for simple CRUD.`],
    [4, `How do you decide where to draw module or package boundaries in a large codebase?`, `Group by business capability (bounded context) rather than technical layer, so changes stay local, and make dependencies point inward toward the domain. Enforce boundaries with tools (ArchUnit, Java modules, or separate build modules) and keep public surfaces small. Trade-off: cross-module reuse needs explicit APIs and sometimes duplication, which is cheaper than tangled coupling.`],
    [4, `Explain hexagonal (ports and adapters) architecture and its cost.`, `The domain core defines ports (interfaces) for what it needs and offers, while adapters implement them for web, DB, messaging and so on; dependencies point toward the core. It makes domain logic testable without infrastructure and lets you swap technology. Costs are more classes, mapping code between layers, and a learning curve, so it pays off in long-lived, complex systems more than small CRUD services.`],
    [3, `How do you avoid overusing design patterns and over-engineering?`, `Start with the simplest thing that works and introduce a pattern when a real duplication, rigidity or variation pain appears (rule of three). Patterns are names for solutions to recurring forces, not goals. Review for YAGNI, prefer plain functions and composition, and measure success by how cheaply the next change lands.`],
    [4, `You must evolve a public library API without breaking hundreds of consumers. What is your approach?`, `Use semantic versioning, additive changes first, and deprecate before removal with clear migration docs and a long sunset window. Keep interfaces small and stable, hide implementation in internal packages, and prefer sealed types or factories to retain freedom. Use binary-compatibility checks (japicmp) in CI; offer adapters for old behavior when a breaking change is unavoidable.`],
    [4, `Explain Event Sourcing and CQRS from an object modeling perspective, including risks.`, `Event sourcing stores state as an append-only log of domain events; aggregates rebuild by replaying them, giving full audit and temporal queries. CQRS separates the write model from read models optimized for queries. Risks: eventual consistency in reads, event schema versioning, replay cost (mitigated with snapshots) and higher operational complexity. Use it where audit and complex domains justify it.`],
    [4, `How do you keep aggregates consistent in a distributed system when one use case spans multiple services?`, `Keep one aggregate per transaction inside a service and coordinate across services with sagas: a sequence of local transactions with compensating actions, either choreographed via events or orchestrated. Use the outbox pattern for reliable event publishing and idempotent handlers for redelivery. Accept eventual consistency and design the UX and invariants around it, since 2PC hurts availability.`],
    [4, `How do you approach mentoring a team to write testable object-oriented code?`, `Set conventions such as constructor injection, small interfaces, pure functions for logic and thin adapters for IO, then demonstrate them in code reviews and pairing. Introduce characterization tests around legacy code before refactoring. Track feedback signals (test speed, mock-heavy tests, flaky tests) as design smells rather than blaming individuals.`],
    [4, `Discuss the trade-offs of using inheritance-based frameworks (e.g. large base classes) versus composition-based ones in a platform team.`, `Base classes give convenience and enforce structure but lock users into a hierarchy, make behavior implicit and complicate upgrades because internal changes ripple to subclasses. Composition (interfaces, hooks, decorators) is more flexible and testable but requires more wiring. For platform code that will have many consumers, prefer composition and explicit extension points with narrow contracts.`],
  ],
  notes: [
    { title: `GoF Pattern Families`, points: [`Creational: Factory, Builder, Singleton`, `Structural: Adapter, Decorator, Facade, Proxy`, `Behavioral: Strategy, Observer, Command`, `Choose by pain, not by name`, `Prefer composition over inheritance`] },
    { title: `Immutability Recipe`, points: [`final class, private final fields`, `No setters`, `Defensive copy in and out`, `Thread-safe for free`, `Record if plain data`], code: `public record Money(long cents, String cur) {\n  public Money {\n    if (cents < 0) throw new IllegalArgumentException();\n  }\n}` },
    { title: `Ports and Adapters`, points: [`Domain has no framework imports`, `Ports = interfaces owned by domain`, `Adapters = DB, HTTP, queue`, `Dependencies point inward`, `Test the core with fakes`], diagram: { type: `flow`, items: [`Web adapter`, `Port (interface)`, `Domain core`, `DB adapter`] } },
    { title: `Code Smells to Spot`, points: [`Long method / god class`, `Feature envy`, `Shotgun surgery`, `Primitive obsession`, `Switch on type codes`, `Fix: extract, move, replace with polymorphism`] },
  ],
  cards: [
    [1, `What does SRP stand for?`, `Single Responsibility Principle: one reason to change per class.`],
    [3, `Which pattern wraps an object to add behavior dynamically?`, `Decorator (java.io streams are the classic example).`],
    [3, `Which pattern provides a simplified interface to a subsystem?`, `Facade.`],
    [2, `Adapter pattern purpose?`, `Converts one interface into another that clients expect.`],
    [3, `What is a Value Object?`, `Immutable, no identity, equal by attributes (Money, Address).`],
    [4, `What is an Aggregate in DDD?`, `A cluster of objects with one root that guards invariants and is the unit of consistency.`],
    [3, `Liskov Substitution in one line?`, `Subtypes must be usable wherever the base type is expected without breaking behavior.`],
    [3, `Interface Segregation in one line?`, `Prefer several small, focused interfaces over one fat interface.`],
    [4, `Anti-corruption layer?`, `A translation boundary that stops a foreign or legacy model from leaking into your domain.`],
    [2, `Favor what over inheritance?`, `Composition.`],
  ],
};

export const ds = {
  questions: [
    [1, `What is the difference between a stack overflow and heap exhaustion in data structure terms?`, `Stack overflow occurs when recursion or deep call chains exceed the call stack size; convert to an iterative approach with an explicit stack or use tail calls where supported. Heap exhaustion occurs when allocated structures (lists, maps, trees) grow beyond available memory. Recognizing which one you have shapes the fix.`],
    [1, `What is a set and how is it different from a list?`, `A set stores unique elements and typically offers O(1) average membership tests (hash-based) or O(log n) with ordering (tree-based). A list is ordered, allows duplicates, and membership is O(n). Use a set when you need deduplication or fast contains.`],
    [2, `Explain the trie data structure and when to use it.`, `A trie stores strings by character along tree paths, so lookup, insert and prefix search cost O(length of the key) regardless of the number of keys. Ideal for autocomplete and prefix matching. Trade-off: heavy memory use with many child pointers; compressed tries (radix) or arrays reduce it.`],
    [2, `What is a union-find (disjoint set) and its complexity?`, `It tracks partitions of elements with find (which set) and union (merge) operations. With path compression and union by rank/size, operations run in nearly constant amortized time, O(alpha(n)). Used for connected components, Kruskal's MST, and cycle detection in undirected graphs.`],
    [2, `What is the difference between a min-heap and a BST, and why is heap building O(n)?`, `A heap only guarantees parent-child order, so it gives the min in O(1) but has no efficient search; a BST supports ordered search in O(log n) if balanced. Building a heap bottom-up with sift-down is O(n) because most nodes are near the leaves and sift very short distances, unlike n insertions at O(n log n).`],
    [2, `How do you reverse a linked list in place, and what is the complexity?`, `Iterate with prev, curr and next pointers: save curr.next, point curr.next to prev, advance both. It takes O(n) time and O(1) space. The recursive version uses O(n) stack space, which can overflow on long lists.`],
    [3, `Explain Dijkstra's algorithm, its complexity, and when it fails.`, `It finds shortest paths from a source in graphs with non-negative weights by repeatedly settling the closest unvisited node using a priority queue: O((V+E) log V) with a binary heap. It fails with negative edges since a settled node may later improve; use Bellman-Ford (O(VE)) for negatives or to detect negative cycles.`],
    [3, `How does topological sort work and where is it used?`, `It orders nodes of a directed acyclic graph so each edge goes from earlier to later. Use Kahn's algorithm (repeatedly remove zero in-degree nodes) or DFS with post-order reversal, both O(V+E). If nodes remain, there is a cycle. Applications: build systems, task scheduling, dependency resolution.`],
    [3, `What is a Bloom filter and what are its trade-offs?`, `A bit array with multiple hash functions that answers "possibly present" or "definitely absent" in O(k) with very low memory. It allows false positives but no false negatives, and standard versions do not support deletion (counting variants do). Used before expensive lookups such as disk or database checks.`],
    [3, `Compare hash table resizing strategies and their effect on latency.`, `Standard resize rehashes everything at once, giving amortized O(1) but occasional O(n) spikes, which hurt tail latency. Incremental rehashing (as in Redis) moves a few buckets per operation, keeping latency smooth at the cost of holding two tables temporarily. Pre-sizing when the count is known avoids resizes entirely.`],
    [3, `How would you find the k-th largest element in an unsorted array?`, `Options: sort O(n log n); a min-heap of size k in O(n log k) using O(k) space (great for streams); or Quickselect with average O(n) and worst case O(n^2) (mitigated by randomized pivots or median-of-medians). Choose the heap for streaming or small k, Quickselect for one-shot in-memory data.`],
    [3, `Explain a segment tree or Fenwick tree and when you would use it.`, `Both support range queries and point updates in O(log n). A Fenwick tree (BIT) is compact and simple for prefix sums; a segment tree is more general (min, max, gcd, lazy range updates). Use them instead of prefix arrays when the data is mutable, since recomputing prefix sums per update is O(n).`],
    [3, `What is amortized analysis? Give an example.`, `It averages the cost of an operation over a sequence instead of the worst single case. A dynamic array doubles when full: one append can cost O(n) but n appends cost O(n) total, so each is O(1) amortized. It does not bound individual latency, which matters in real-time systems.`],
    [3, `What is the difference between an LSM tree and a B-tree?`, `B-trees update pages in place and give predictable reads, with random write IO. LSM trees buffer writes in memory and flush sorted runs, merging them by compaction, so writes are fast and sequential while reads may check several levels (mitigated with Bloom filters) and compaction adds write amplification. LSM suits write-heavy workloads, B-trees suit read-heavy or mixed.`],
    [4, `Design an autocomplete system for millions of queries per day.`, `Store popular query strings in a trie (or compressed trie) with top-K suggestions cached at each node, rebuilt offline from logs by frequency and recency. Serve from memory with sharding by prefix, cache hot prefixes at the edge, and update via periodic snapshots rather than per-request writes. Trade-offs: freshness vs cost, personalization vs cache hit rate, and typo tolerance which needs additional fuzzy matching.`],
    [4, `How would you count unique visitors across billions of events per day?`, `Exact counting needs memory proportional to the distinct count, so use HyperLogLog, which estimates cardinality with about 1-2% error using kilobytes and supports merging across shards. Keep exact sets only for small segments or where billing accuracy demands it. Trade-off is approximation error vs storage; partition by time window and merge sketches for roll-ups.`],
    [4, `Design a rate limiter data-structure-wise. Compare token bucket, sliding window log and sliding window counter.`, `Token bucket stores tokens and last refill time per key (O(1) memory), allowing bursts. Sliding window log stores every timestamp: exact but memory-heavy. Sliding window counter combines two fixed-window counters weighted by overlap, giving O(1) memory with a small approximation. In distributed setups use Redis with atomic Lua scripts and accept small over-admission across regions.`],
    [4, `How do you design consistent hashing and why does it matter for caches?`, `Nodes and keys are hashed onto a ring and each key maps to the next node clockwise, so adding or removing a node only remaps about 1/N of the keys instead of nearly all with modulo hashing. Virtual nodes even out load and let heterogeneous capacity be expressed. Trade-offs: lookup structure (sorted array or tree) and hot-key handling still need care.`],
    [4, `Design a data structure for a time-series store that supports fast recent writes and range queries.`, `Partition by time into chunks, buffer recent writes in memory (write-ahead logged), flush to immutable sorted columnar blocks, and compress timestamps with delta-of-delta and values with XOR-style encoding. Index blocks by min/max time for pruning and downsample old data into roll-ups. Trade-off: high ingestion and compression vs update-in-place and out-of-order write complexity.`],
    [4, `You need to deduplicate a stream of 10 billion IDs that does not fit in memory. What are your options?`, `External sort then scan adjacent duplicates, hash partition the data into buckets that fit in memory and dedupe each, or use a Bloom filter as a first pass and verify the positives. Partitioning gives exact results and parallelizes well. Choose the Bloom approach only when some errors are acceptable or a later exact check exists.`],
    [4, `Explain how you would choose between an in-memory index, a database index and a search engine for lookups.`, `In-memory maps give the lowest latency but cost RAM, lose data on restart and need invalidation. Database indexes give transactional consistency and ordered access with moderate latency. Search engines (inverted indexes) suit full text, faceting and relevance, but are eventually consistent and heavier to run. Pick by access pattern, data size and consistency needs; often combine, with the database as source of truth.`],
    [4, `How would you detect and handle hot keys in a sharded cache or database?`, `Detect through per-key request metrics or sampling at the proxy. Mitigate with local in-process caches for the hottest keys, replicating hot keys across several shards (key suffixes with random spread on reads), request coalescing, and rate limiting. Trade-offs include stale reads and extra memory, so use short TTLs and only apply it to detected keys.`],
  ],
  notes: [
    { title: `Sorting Algorithms Table`, points: [`Quick: avg n log n, in place`, `Merge: n log n, stable, O(n) space`, `Heap: n log n, in place, not stable`, `Insertion: n^2 but great for tiny arrays`, `Java sorts objects with TimSort (stable)`] },
    { title: `Trie Structure`, points: [`Node holds children by char`, `Flag marks end of word`, `Insert and search in O(L)`, `Great for prefix and autocomplete`, `Memory heavy, compress if needed`], diagram: { type: `flow`, items: [`root`, `c`, `a`, `t (end)`] } },
    { title: `Union-Find Template`, points: [`parent[] array, each node its own root`, `find with path compression`, `union by size or rank`, `Near O(1) per operation`, `Use for components and Kruskal`], code: `int find(int x) {\n  return p[x] == x ? x : (p[x] = find(p[x]));\n}\nvoid union(int a, int b) { p[find(a)] = find(b); }` },
    { title: `Probabilistic Structures`, points: [`Bloom: membership, no false negatives`, `HyperLogLog: distinct count`, `Count-Min sketch: frequency estimates`, `Trade accuracy for tiny memory`, `Mergeable across shards`] },
  ],
  cards: [
    [1, `Time to access an array element by index?`, `O(1).`],
    [2, `Complexity of building a heap from n items?`, `O(n) with bottom-up heapify.`],
    [3, `Dijkstra with a binary heap?`, `O((V+E) log V), non-negative weights only.`],
    [3, `Kahn's algorithm is used for?`, `Topological sort via in-degree zero queue; detects cycles.`],
    [3, `Bloom filter error type?`, `False positives possible, false negatives never.`],
    [4, `HyperLogLog is used for?`, `Approximate count of distinct elements in tiny memory.`],
    [3, `Worst case of quicksort and how to avoid?`, `O(n^2) on bad pivots; use random or median-of-three pivot.`],
    [3, `Stable sort meaning?`, `Equal elements keep their original relative order.`],
    [4, `Why virtual nodes in consistent hashing?`, `To balance load and smooth rebalancing across nodes.`],
    [2, `Space complexity of BFS?`, `O(V) in the worst case for the queue and visited set.`],
  ],
};

export const javaVersions = {
  questions: [
    [1, `What does the var keyword do and where is it allowed?`, `Introduced in Java 10, var lets the compiler infer the type of a local variable from its initializer (var list = new ArrayList<String>()). It is only for locals, for-loop variables and lambda parameters (Java 11), not fields, method parameters or return types. The type is still static; use it when the type is obvious from the right side.`],
    [1, `What are text blocks?`, `Multi-line string literals delimited by three double quotes, final in Java 15. They preserve line breaks, strip common incidental indentation and reduce escaping, which suits JSON, SQL and HTML snippets. Escape sequences still work and a trailing backslash joins lines.`],
    [2, `What are switch expressions (Java 14)?`, `Switch can now return a value using arrow labels (case A, B -> value) with no fall-through, and yield inside block bodies. The compiler requires exhaustiveness for expressions, so enums or sealed types must be fully covered or have a default. This removes a classic source of missing-break bugs.`],
    [2, `What useful API additions came with Java 9 to 16 for collections and streams?`, `List.of/Set.of/Map.of immutable factories (9), Optional.ifPresentOrElse and or (9), Stream.takeWhile/dropWhile (9), Collectors.toUnmodifiableList (10), Optional.orElseThrow() without arguments (10), and Stream.toList() (16). Immutable factories reject nulls and throw on modification.`],
    [2, `What is the Java module system (JPMS) and do you need it?`, `Java 9 modules use module-info.java to declare exports and requires, giving strong encapsulation and reliable configuration; jlink can build trimmed runtimes. Most application teams stay on the classpath and only meet JPMS through strong encapsulation of JDK internals, which broke reflection-based libraries and required --add-opens flags. It is more valuable for library and platform authors.`],
    [2, `What are helpful NullPointerException messages?`, `Since Java 14 (on by default from 15) the JVM describes which expression was null, such as Cannot invoke String.length() because the return value of getName() is null. It speeds debugging with no code change. It can also expose local names only if compiled with -g.`],
    [3, `Explain pattern matching for instanceof and for switch, and how they evolved.`, `instanceof patterns (final in 16) bind a typed variable in one step: if (o instanceof String s && s.length() > 3). Pattern matching for switch (final in 21) allows type patterns, guards with when, and null cases in a switch, with exhaustiveness checked over sealed hierarchies. Together with record patterns they replace visitor-style code with less boilerplate.`],
    [3, `What are record patterns (Java 21) and how do they compose?`, `A record pattern deconstructs a record in a pattern, e.g. case Point(int x, int y) when x == y -> ..., and patterns can nest for records that contain records. They combine with sealed interfaces so a switch over a hierarchy can be exhaustive without a default. Limits: records only, no arbitrary classes, so deconstruction is intentionally simple.`],
    [3, `Sealed classes: how do they work and how do they interact with switch?`, `sealed with permits restricts which classes may extend a type, and each permitted subtype must be final, sealed or non-sealed. It models closed algebraic data types, so a pattern switch over the hierarchy can be checked for exhaustiveness at compile time. Trade-off: adding a new subtype breaks every switch that must handle it, which is usually the desired signal.`],
    [3, `What are virtual threads and how do they differ from platform threads?`, `Final in Java 21 (Project Loom), virtual threads are lightweight threads scheduled by the JVM onto a small pool of carrier threads; blocking IO unmounts them, so millions can exist. Use Executors.newVirtualThreadPerTaskExecutor() and do not pool them. Watch for pinning (synchronized blocks around blocking calls in Java 21, improved in Java 24), ThreadLocal overuse and downstream resource limits since more threads means more concurrent requests.`],
    [3, `What are sequenced collections in Java 21?`, `New interfaces SequencedCollection, SequencedSet and SequencedMap give a uniform API for collections with a defined encounter order: getFirst, getLast, addFirst, removeLast and reversed(). List, Deque, LinkedHashSet, LinkedHashMap and SortedSet now implement them. It fills the gap where getting the last element differed for every collection type.`],
    [3, `Compare records with Lombok @Value and traditional POJOs.`, `Records (Java 16) are shallowly immutable, final, with generated constructor, accessors, equals, hashCode and toString, and they are language-level so no annotation processing is needed. Lombok offers more flexibility (builders, inheritance, mutability options) but relies on compiler hacks. Records cannot extend classes and only shallowly immutable: a List component can still be mutated unless copied in a compact constructor.`],
    [3, `What changed in garbage collection across Java 11 to 21?`, `G1 has been default since 9. ZGC became production-ready in 15, and Shenandoah too (in OpenJDK builds); generational ZGC arrived in 21 and is the recommended mode for it later. Parallel and Serial remain for throughput and small footprints. Also CMS was removed in 14. Upgrade gains often come simply from newer G1 improvements, so re-measure before tuning.`],
    [3, `What is the Foreign Function and Memory API and why does it matter?`, `Final in Java 22, it lets Java call native libraries and manage off-heap memory safely, replacing most JNI usage and much of sun.misc.Unsafe. It gives deterministic lifetimes via arenas and bounds-checked memory segments, with jextract generating bindings. Trade-off: native calls still bypass JVM safety guarantees if misused, and libraries need migration time.`],
    [4, `You are leading a Java 8 to 21 migration. What are the main technical risks?`, `Strong encapsulation of JDK internals (illegal reflective access now fails), removed modules such as javax.xml.bind, Nashorn and Java EE APIs, changed defaults (G1, UTF-8 by default in 18), and library or agent compatibility (bytecode tools, older Spring, Lombok, Mockito). Approach: move through LTS steps, upgrade libraries first, use jdeps and jdeprscan, run tests on the new JVM in CI and roll out canary. The payoff includes performance and lower memory.`],
    [4, `How would you decide between adopting virtual threads and keeping reactive code in an existing platform?`, `Virtual threads let blocking code scale, so new IO-bound services can drop reactive complexity. Existing reactive services work and rewriting them has cost, so migrate opportunistically where readability and debugging hurt. Check for pinning, connection pool limits and databases that now see more concurrency, and add limits with semaphores. CPU-bound work and backpressure-heavy streaming do not benefit.`],
    [4, `How would you structure a policy for LTS adoption and preview features across many teams?`, `Standardize on LTS releases (8, 11, 17, 21, 25) with a supported window and a published upgrade cadence, and run a shared BOM and base image. Ban preview and incubator APIs in production code because they can change or disappear between releases, but allow experiments in isolated branches. Automate upgrade PRs and compatibility checks so the cost per team stays low.`],
    [4, `How do records, sealed types and pattern matching change domain modeling in Java?`, `They enable data-oriented programming: model data as immutable records, closed alternatives as sealed interfaces, and behavior as exhaustive switch functions over them. Results are simpler code, compiler-checked handling of new cases and fewer visitor classes. It complements OOP rather than replacing it; keep encapsulated stateful services as classes and do not put persistence entities into records blindly, since JPA needs mutable proxies.`],
    [4, `What can go wrong when adopting Java 17 or 21 in containers with older frameworks or agents?`, `Old APM agents, bytecode-instrumentation libraries and ASM versions may fail on new class file versions; reflection into JDK internals fails unless opened; and old TLS or crypto defaults may reject legacy endpoints. Also jlink-trimmed runtimes may miss modules discovered only at runtime. Mitigate with staging soak tests, jdeps analysis and pinned agent versions.`],
    [4, `Explain scoped values and structured concurrency, and how they might reshape concurrent Java code.`, `Structured concurrency treats a group of related tasks as one unit of work with clear lifetimes and error propagation; it was still a preview API in the releases where it was introduced. Scoped values share immutable data with child tasks more cheaply and safely than ThreadLocal, which matters with millions of virtual threads. Preview status means APIs may change, so pilot them behind a feature flag and check current JEP status before committing.`],
    [4, `What performance or startup features in the JDK help microservices and serverless?`, `Class-Data Sharing (AppCDS) and Class Data Sharing archives cut startup and memory, jlink reduces image size, and Project CRaC or GraalVM native image offer faster starts at the price of limited dynamic features (reflection config, agents). Native image trades peak throughput and build time for start speed; measure against tuned JIT with warm pools before choosing.`],
    [4, `How do you evaluate whether to use the newest non-LTS Java release?`, `Non-LTS releases get about six months of updates, so they suit teams that upgrade continuously with strong CI and small dependency surfaces. For most enterprises the LTS path lowers risk and support cost. Benefits of tracking latest include early access to performance and language improvements and smaller incremental upgrades, which are easier than big-bang jumps.`],
  ],
  notes: [
    { title: `Features by LTS`, points: [`8: lambdas, streams, java.time`, `11: HttpClient, var in lambdas`, `17: records, sealed, text blocks`, `21: virtual threads, pattern switch`, `Non-LTS releases bridge the gaps`], diagram: { type: `flow`, items: [`Java 8`, `Java 11`, `Java 17`, `Java 21`] } },
    { title: `Records and Sealed Types`, points: [`Record = immutable data carrier`, `Compact constructor validates`, `Sealed limits subtypes`, `permits lists allowed classes`, `Switch over sealed is exhaustive`], code: `sealed interface Shape permits Circle, Sq {}\nrecord Circle(double r) implements Shape {}\nrecord Sq(double s) implements Shape {}\ndouble area(Shape s) {\n  return switch (s) {\n    case Circle c -> Math.PI * c.r() * c.r();\n    case Sq q -> q.s() * q.s();\n  };\n}` },
    { title: `Virtual Thread Rules`, points: [`One virtual thread per task`, `Never pool them`, `Great for blocking IO`, `Not for CPU-bound work`, `Avoid long synchronized blocking`, `Limit downstream with semaphores`], code: `try (var ex = Executors.newVirtualThreadPerTaskExecutor()) {\n  ex.submit(() -> fetch(url));\n}` },
    { title: `Java 9 to 16 Grab-Bag`, points: [`9: modules, List.of, JShell`, `10: var`, `14: switch expressions`, `15: text blocks, ZGC production`, `16: records, instanceof patterns`, `16: Stream.toList()`] },
  ],
  cards: [
    [1, `Which Java version added lambdas?`, `Java 8.`],
    [2, `Version where var was added?`, `Java 10 (local variable type inference).`],
    [3, `Java 21 headline feature?`, `Virtual threads (final), plus pattern matching for switch and record patterns.`],
    [3, `Are records mutable?`, `Fields are final (shallow immutable); components holding mutable objects can still change.`],
    [3, `Sealed keyword companions?`, `permits, plus final, sealed or non-sealed on subtypes.`],
    [2, `Stream.toList() since?`, `Java 16; returns an unmodifiable list.`],
    [3, `Which GC arrived as generational mode in 21?`, `ZGC.`],
    [4, `What replaced most JNI use in Java 22?`, `Foreign Function and Memory API (final in 22).`],
    [2, `Java 11 String additions?`, `isBlank, lines, strip, repeat.`],
    [3, `Java LTS releases after 8?`, `11, 17, 21 and 25.`],
  ],
};

export const python = {
  questions: [
    [1, `What is the difference between a module and a package in Python?`, `A module is a single .py file; a package is a directory of modules, traditionally with an __init__.py (namespace packages can omit it). Imports resolve via sys.path. Use packages to organize code and control public names with __all__.`],
    [1, `What are f-strings and why prefer them?`, `Formatted string literals (f"Hello {name}") evaluate expressions inline, are faster than % formatting and str.format, and are more readable. They support format specs (f"{x:.2f}") and the debug form f"{x=}". Do not build SQL or shell commands with them, as that invites injection.`],
    [2, `Explain the difference between @staticmethod, @classmethod and instance methods.`, `Instance methods receive self and work on an object. Classmethods receive cls and are used for alternative constructors and behavior tied to the class or subclasses. Staticmethods receive neither and are just namespaced functions; often a module-level function is clearer.`],
    [2, `What are dataclasses and when do you use them?`, `The @dataclass decorator generates __init__, __repr__ and __eq__ from annotated fields, with options like frozen=True, slots=True and order=True. They suit simple data holders. For validation and parsing use pydantic; for heavy performance consider slots or plain tuples.`],
    [2, `How does Python manage memory?`, `CPython uses reference counting for immediate reclamation plus a cyclic garbage collector to collect reference cycles, with a private heap and small-object allocator (pymalloc). Reference cycles with __del__ or large caches can delay freeing. Tools: tracemalloc, gc, objgraph.`],
    [2, `What is the difference between a shallow and a deep copy in a nested structure, and how do mutable defaults relate?`, `A shallow copy duplicates the outer container but shares inner objects, so mutating a nested list affects both; deepcopy recursively clones. Mutable default arguments are evaluated once at definition, so use None as the default and create the object inside the function.`],
    [3, `Explain how the GIL affects multithreading and what changed with free-threaded builds.`, `The GIL allows one thread to execute Python bytecode at a time, so CPU-bound threads do not scale, while IO-bound threads still benefit since the lock is released during blocking calls. Python 3.13 introduced an experimental free-threaded build (PEP 703) without the GIL, at the cost of single-thread overhead and library compatibility. For CPU work use multiprocessing, native extensions or process pools.`],
    [3, `What are metaclasses and when would you use one?`, `A metaclass is the class of a class (default type); it controls class creation. Used by ORMs and frameworks to register subclasses or add attributes. Most needs are covered by class decorators or __init_subclass__, which are simpler, so avoid metaclasses unless you need to control creation itself.`],
    [3, `How do descriptors and __slots__ work?`, `Descriptors implement __get__/__set__/__delete__ and power properties, methods and many framework fields. __slots__ replaces the per-instance dict with fixed slots, reducing memory and speeding attribute access but preventing dynamic attributes and complicating multiple inheritance.`],
    [3, `What is the MRO and how does super() work with multiple inheritance?`, `Method Resolution Order is computed by C3 linearization and viewable through Class.__mro__. super() follows the MRO, not just the direct parent, so cooperative multiple inheritance requires every class to call super().method() and accept **kwargs consistently. Mixins should be small and stateless to avoid surprises.`],
    [3, `Explain type hints, mypy and Protocol.`, `Type hints are not enforced at runtime; tools like mypy or pyright check them statically. Protocol enables structural typing (duck typing checked statically) so a class satisfies an interface without inheriting. Introduce hints gradually, focus on public APIs, and use TypedDict or dataclasses for structured data.`],
    [3, `How do you handle a memory spike when processing a 20 GB CSV in Python?`, `Stream instead of loading everything: iterate with csv or pandas chunksize, use generators, select needed columns and dtypes, and consider Arrow/Polars or DuckDB for out-of-core processing. Write results incrementally. Measure with tracemalloc; avoid accumulating intermediate lists.`],
    [3, `Compare threading, multiprocessing and asyncio for a web scraper that also parses heavy HTML.`, `Fetching is IO-bound: asyncio or threads give concurrency cheaply. Parsing is CPU-bound, so hand it to a process pool (run_in_executor with ProcessPoolExecutor) to bypass the GIL. Keep a bounded queue between stages for backpressure and add rate limits and timeouts to respect target sites.`],
    [3, `What are common causes of slow Python startup or import time?`, `Heavy top-level imports, work executed at import (network, DB connections), large frameworks and many small files. Diagnose with python -X importtime, then use lazy imports, move initialization into functions and trim dependencies. Serverless environments feel this most.`],
    [4, `Your Django/FastAPI service leaks memory over days in production. How do you find it?`, `Track RSS per worker over time and compare with request volume; take tracemalloc snapshots or use memray/objgraph in a canary to diff allocations. Typical causes: unbounded caches or global lists, lru_cache on methods holding self, C-extension leaks, or fragmentation. Mitigate with worker recycling (max-requests in gunicorn) while you fix the root cause.`],
    [4, `How would you design a background job system in Python, and how do you make jobs safe to retry?`, `Use a broker-backed queue (Celery, RQ, Arq or cloud queue) with visible acks after completion, retries with exponential backoff and dead-letter queues. Make jobs idempotent via idempotency keys or upserts and store job state. Trade-off: at-least-once delivery is simpler and more common than exactly-once; design consumers around it.`],
    [4, `How do you scale a CPU-bound Python workload?`, `First profile and vectorize with NumPy or push hot loops to compiled code (Cython, Numba, Rust via PyO3). Then parallelize with processes, Ray or Dask across cores or machines, being careful about serialization costs. Only after that consider architecture changes. Each step trades complexity for speed, so measure the gain.`],
    [4, `How do you manage dependencies and reproducible builds in a large Python monorepo?`, `Pin direct and transitive dependencies with lock files (uv, pip-tools, Poetry) and build in containers with the same Python version. Separate library and application constraints, run vulnerability scanning, and use private indexes with hash checking. Automate upgrade PRs so pinning does not become a stale burden.`],
    [4, `What are the risks of using pickle, eval or dynamic imports on user-influenced data, and what do you do instead?`, `pickle can execute arbitrary code on load, and eval/exec run arbitrary expressions, so both are remote-code-execution risks with untrusted input. Use JSON, Protobuf or msgpack with schema validation, and restrict plugin loading to an allow-list. If pickle is unavoidable, sign or encrypt payloads and keep them internal.`],
    [4, `Design an ETL pipeline in Python that must process late and duplicate data reliably.`, `Land raw data immutably, then transform in idempotent steps keyed on natural or event IDs so reruns do not duplicate. Use watermarks and allowed lateness for time windows and upserts (MERGE) into the target. Add data quality checks, lineage and orchestration (Airflow, Dagster) with retries. Trade-off: reprocessing capacity and storage vs correctness guarantees.`],
    [4, `When should a team move a Python service to another language or split out a component?`, `Consider it when profiling shows the bottleneck is inherent to Python, such as tight CPU loops or latency budgets that GIL and interpreter overhead cannot meet, and vectorization or native extensions do not suffice. Prefer extracting the hot component behind a clear interface over a total rewrite. Weigh hiring, ecosystem and operational cost, not only benchmarks.`],
    [4, `How do you enforce code quality and safe change in a fast-growing Python codebase?`, `Adopt formatter and linter (ruff, black), type checking in CI at least for new modules, pre-commit hooks, and testing with pytest including property-based tests where suitable. Set coverage on critical paths rather than a blanket number, use CI gates and code owners. Introduce strictness gradually so it does not stall delivery.`],
  ],
  notes: [
    { title: `Python Data Model Hooks`, points: [`__init__ and __repr__`, `__eq__ and __hash__ together`, `__iter__ and __next__: iterators`, `__enter__ and __exit__: with`, `__getitem__: indexing`, `__call__: callable objects`] },
    { title: `Import Resolution Flow`, points: [`Check sys.modules cache first`, `Search sys.path finders`, `Load and execute module once`, `Bind name in importer`, `Circular imports: import inside function`], diagram: { type: `flow`, items: [`sys.modules`, `sys.path search`, `exec module`, `bind name`] } },
    { title: `Dataclass Quick Use`, points: [`Annotated fields become init args`, `frozen=True makes immutable`, `slots=True saves memory`, `field(default_factory=list) for mutables`, `__post_init__ for validation`], code: `from dataclasses import dataclass, field\n@dataclass(frozen=True, slots=True)\nclass Order:\n    id: int\n    items: tuple = field(default_factory=tuple)` },
    { title: `Speeding Up Python`, points: [`Profile first: cProfile, py-spy`, `Better algorithm beats micro-tuning`, `Use built-ins, sets, dict lookups`, `NumPy vectorization`, `Processes for CPU work`, `Cython, Numba or Rust for hot loops`] },
  ],
  cards: [
    [1, `What does len() of a dict return?`, `Number of key-value pairs.`],
    [2, `Purpose of __init_subclass__?`, `Hook called when a class is subclassed; simple alternative to metaclasses.`],
    [3, `What is LEGB?`, `Name lookup order: Local, Enclosing, Global, Builtin.`],
    [3, `Complexity of dict lookup?`, `O(1) average; keys must be hashable.`],
    [3, `What does functools.lru_cache do?`, `Memoizes function results by arguments; watch memory and methods holding self.`],
    [4, `Free-threaded Python arrived in?`, `Experimental in 3.13 (PEP 703).`],
    [2, `Difference: list.sort() vs sorted()?`, `sort() is in place and returns None; sorted() returns a new list.`],
    [3, `What does the walrus operator do?`, `:= assigns inside an expression (Python 3.8+).`],
    [3, `asyncio.gather vs TaskGroup?`, `TaskGroup (3.11) gives structured concurrency with cancellation on failure.`],
    [4, `Why avoid pickle on untrusted data?`, `Loading can execute arbitrary code.`],
  ],
};

export const sql = {
  questions: [
    [1, `What is the difference between UNION and UNION ALL?`, `UNION combines result sets and removes duplicates, which requires a sort or hash step; UNION ALL keeps duplicates and is faster. Use UNION ALL unless you specifically need deduplication. Both need the same number and compatible types of columns.`],
    [1, `How do NULLs behave in comparisons and aggregates?`, `Any comparison with NULL (= NULL) yields unknown, so use IS NULL / IS NOT NULL. Aggregates such as SUM, AVG and COUNT(column) ignore NULLs, while COUNT(*) counts rows. NOT IN with a subquery containing NULL returns no rows, so prefer NOT EXISTS.`],
    [2, `What is a CTE and how does it differ from a subquery or temp table?`, `A common table expression (WITH name AS (...)) names a query for readability and enables recursion. Some databases inline it like a subquery while others (older PostgreSQL) materialize it, which affects performance. Temp tables persist within the session, can be indexed and are useful for reuse across statements.`],
    [2, `Explain EXISTS vs IN.`, `EXISTS checks for at least one matching row and stops early; IN compares against a list or subquery result. Modern optimizers often produce the same plan, but NOT IN has the NULL trap while NOT EXISTS does not. Test with EXPLAIN on your engine.`],
    [2, `What is a view and what is a materialized view?`, `A view is a stored query executed on each access, useful for abstraction and security. A materialized view stores results physically and must be refreshed, trading freshness for read speed. Use them for expensive aggregates read often.`],
    [2, `What are the differences between clustered and non-clustered indexes?`, `A clustered index defines the physical order of rows (one per table; the primary key in InnoDB, optional in SQL Server), so range scans on it are fast. Non-clustered indexes are separate structures with pointers to rows or the clustered key. Secondary index lookups may need an extra key lookup unless covering.`],
    [3, `Explain MVCC and how it affects reads and writes.`, `Multi-version concurrency control keeps multiple row versions so readers see a consistent snapshot without blocking writers and vice versa. PostgreSQL stores old versions in the table, requiring VACUUM; InnoDB uses undo logs. Cost: bloat or long undo history from long-running transactions, so keep transactions short.`],
    [3, `How does a composite index work and how do you order its columns?`, `The B-tree is sorted by the first column, then the second within it (leftmost prefix rule), so queries filtering on the first column or a prefix can use it. Put equality columns first, then range columns, and consider selectivity and sort needs. A query that filters only on the second column typically cannot use it efficiently.`],
    [3, `What is a covering index and when is it valuable?`, `An index that contains all columns a query needs, so the engine answers from the index without visiting the table (index-only scan). It cuts random IO on hot queries. Trade-off: bigger indexes and slower writes; INCLUDE columns (PostgreSQL, SQL Server) add payload without affecting ordering.`],
    [3, `Explain optimistic vs pessimistic locking.`, `Pessimistic locking (SELECT ... FOR UPDATE) locks rows up front, safe under high contention but reduces concurrency and risks deadlocks. Optimistic locking uses a version column and update ... where version = ? and retries on conflict, best when conflicts are rare. Choose by contention level and cost of retry.`],
    [3, `Why can OFFSET pagination be slow and what is the alternative?`, `OFFSET N makes the database read and discard N rows, so deep pages get slower and results shift when data changes. Keyset (seek) pagination uses WHERE (created_at, id) < (last values) ORDER BY ... LIMIT, using the index directly for stable, fast pages. Trade-off: no jumping to arbitrary page numbers.`],
    [3, `How do you write an upsert and what are the concurrency concerns?`, `Use INSERT ... ON CONFLICT DO UPDATE (PostgreSQL), ON DUPLICATE KEY UPDATE (MySQL) or MERGE (SQL Server/Oracle). It needs a unique constraint as the conflict target. MERGE in some engines has race conditions if not run in a proper isolation level; prefer the native atomic form and retry on serialization errors.`],
    [3, `What does a query planner use statistics for, and what happens when they are stale?`, `Statistics on row counts, value distributions and correlations let the planner estimate cardinality and choose join order, join algorithm and index use. Stale stats cause bad estimates and plans, like nested loops over millions of rows. Fix with ANALYZE / UPDATE STATISTICS, extended statistics, or plan hints as a last resort.`],
    [3, `Explain recursive CTEs with an example use case.`, `A recursive CTE has an anchor query plus a recursive member joined with UNION ALL, iterating until no new rows appear. Used for hierarchies such as org charts or category trees. Guard against cycles with depth limits or path tracking, and consider closure tables or ltree for heavy hierarchical reads.`],
    [4, `A report query that took 2 seconds now takes 2 minutes after data growth. Walk through your approach.`, `Capture the plan with actual timings (EXPLAIN ANALYZE), compare estimated vs actual rows, and check for sequential scans, spills to disk, and bad join order. Add or adjust indexes, refresh statistics, rewrite non-sargable predicates, and consider pre-aggregation or a materialized view. For recurring heavy reports move to a read replica or warehouse rather than tuning the OLTP primary.`],
    [4, `How do you design a zero-downtime schema migration on a large table?`, `Use expand-and-contract: add nullable columns or new tables first, deploy code that writes both, backfill in small batches with throttling, then switch reads and drop the old structures later. Create indexes concurrently (PostgreSQL CREATE INDEX CONCURRENTLY) or use online tools (gh-ost, pt-online-schema-change). Avoid long locks and test the rollback path.`],
    [4, `Explain replication topologies and their consistency trade-offs.`, `Asynchronous replicas give low write latency and read scaling but can lag, so read-your-writes needs routing or session pinning. Synchronous replication prevents data loss on failover at the cost of latency and availability. Multi-primary adds conflict resolution complexity. Choose based on RPO/RTO targets and monitor replica lag and failover drills.`],
    [4, `How do you approach sharding a relational database, and what do you lose?`, `Pick a shard key aligned with the dominant access pattern and tenant boundary, avoid hot shards, and use a lookup or consistent hashing scheme that allows rebalancing. You lose cross-shard joins and transactions, global unique constraints and simple aggregation, which pushes work to the application or requires sagas. Exhaust indexing, caching, read replicas and partitioning first.`],
    [4, `Design a multi-tenant data model. What are the options and risks?`, `Options are shared tables with a tenant_id column (cheapest, needs row-level security and tenant-leading indexes), schema per tenant, or database per tenant (strong isolation, costly at scale, harder migrations). Noisy neighbors, cross-tenant leaks from missing filters and per-tenant restores are the main risks. Choose by tenant count, compliance needs and size variance.`],
    [4, `How do you guarantee an exactly-once effect when a service consumes messages and writes to a database?`, `True exactly-once delivery does not exist across systems; get exactly-once effect through idempotent writes: store the message ID with a unique constraint in the same transaction as the business update, or use the transactional outbox and inbox patterns. Duplicate deliveries then become no-ops. Cost is an extra table and cleanup job for processed IDs.`],
    [4, `When is a database the wrong tool for a queue or a cache, and when is it acceptable?`, `Polling tables for queues works at modest volume using SELECT ... FOR UPDATE SKIP LOCKED and gives transactional enqueueing, but it stresses the primary at scale and adds bloat. Caches in a database ignore memory speed and TTL semantics. Use a broker (Kafka, SQS) or Redis when throughput, fan-out or latency demand it; a DB-backed queue is acceptable for low volume with transactional needs.`],
    [4, `How do you plan for backup, point-in-time recovery and disaster recovery, and how do you validate them?`, `Combine periodic base backups with continuous WAL/binlog archiving for point-in-time recovery, store copies in separate regions and accounts, and define RPO/RTO with the business. Backups are only real if restored: run scheduled restore drills and measure the recovery time. Also protect against logical errors with delayed replicas and immutable storage.`],
  ],
  notes: [
    { title: `Isolation Anomalies`, points: [`Dirty read: uncommitted data seen`, `Non-repeatable read: row changes`, `Phantom: new rows appear`, `Read Committed: no dirty reads`, `Serializable: prevents all`, `Higher isolation costs concurrency`] },
    { title: `Index Choice Guide`, points: [`Equality columns first`, `Then range column`, `Leftmost prefix rule`, `Covering index avoids table hits`, `Functions on columns break use`, `Every index slows writes`], diagram: { type: `flow`, items: [`Filter`, `Index seek`, `Key lookup`, `Result`] } },
    { title: `Keyset Pagination`, points: [`Stable ordering key required`, `Use last seen value as cursor`, `Index makes each page fast`, `No skipped rows on inserts`, `Cannot jump to page N`], code: `SELECT id, created_at\nFROM orders\nWHERE (created_at, id) < ($1, $2)\nORDER BY created_at DESC, id DESC\nLIMIT 50;` },
    { title: `Safe Migrations`, points: [`Expand: add nullable column`, `Dual write from code`, `Backfill in small batches`, `Switch reads`, `Contract: drop old column later`], diagram: { type: `flow`, items: [`Expand`, `Backfill`, `Switch reads`, `Contract`] } },
  ],
  cards: [
    [1, `COUNT(*) vs COUNT(col)?`, `COUNT(*) counts rows; COUNT(col) skips NULLs.`],
    [2, `What is a sargable predicate?`, `One that can use an index, like col >= 10, not WHERE fn(col) = 10.`],
    [3, `SELECT FOR UPDATE SKIP LOCKED use?`, `Lets workers claim different rows for DB-backed queues without blocking.`],
    [3, `What does VACUUM do in PostgreSQL?`, `Reclaims space from dead row versions left by MVCC.`],
    [3, `Default isolation level in PostgreSQL?`, `Read Committed.`],
    [4, `Default isolation level in MySQL InnoDB?`, `Repeatable Read.`],
    [3, `What is a deadlock victim?`, `The transaction the DB aborts to break a lock cycle; retry it.`],
    [2, `ROW_NUMBER vs RANK?`, `ROW_NUMBER is unique per row; RANK gives ties the same rank and skips numbers.`],
    [4, `What is CDC?`, `Change data capture: streaming row changes from the DB log to other systems.`],
    [3, `Why avoid SELECT * in production queries?`, `Extra IO and payload, breaks covering indexes, and is fragile when schemas change.`],
  ],
};
