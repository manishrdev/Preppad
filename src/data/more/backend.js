export const spring = {
  questions: [
    [1, `What does @Autowired do and what happens if no matching bean exists?`, `@Autowired asks the container to inject a matching bean by type (then by qualifier or name if ambiguous). If none is found and the dependency is required, startup fails with NoSuchBeanDefinitionException; if several match you get NoUniqueBeanDefinitionException. Use @Qualifier, @Primary, or required=false / Optional to control this. On a single constructor, the annotation is optional.`],
    [1, `What is the difference between @Bean and @Component?`, `@Component is class-level and picked up by component scanning, so you must own the source. @Bean is method-level inside a @Configuration class and lets you construct and customise third-party or conditional objects explicitly. Use @Bean when you need construction logic or cannot annotate the class.`],
    [2, `What does @Configuration do differently from a plain @Component with @Bean methods?`, `@Configuration classes are CGLIB-proxied (full mode), so calling one @Bean method from another returns the container singleton rather than creating a new object. In a plain @Component (lite mode) inter-bean method calls are ordinary Java calls and create new instances. Use proxyBeanMethods=false when you do not rely on inter-bean calls, to speed up startup.`],
    [2, `How do @Qualifier and @Primary resolve multiple candidate beans?`, `@Primary marks the default candidate when several beans of a type exist. @Qualifier at the injection point picks a specific bean by name or custom qualifier and overrides @Primary. Injecting List<T> or Map<String,T> gets all implementations, which suits strategy patterns.`],
    [2, `How do Spring events work and when would you use @TransactionalEventListener?`, `ApplicationEventPublisher publishes events and @EventListener methods receive them, synchronously on the publisher thread by default. @TransactionalEventListener ties delivery to a transaction phase, typically AFTER_COMMIT, so side effects like emails or messages only fire if the data actually committed. Trade-off: in AFTER_COMMIT the transaction is over, so writes in the listener need REQUIRES_NEW, and a failure there is not rolled back with the original work.`],
    [2, `What is the difference between @Autowired on a collection and @Value, and how do you inject properties safely?`, `@Value injects a single property or SpEL expression and fails late on typos or type mismatches. For grouped settings prefer @ConfigurationProperties with validation, which is type-safe and relaxed-binding. @Value remains fine for one-off values with a default such as a placeholder with a colon default.`],
    [3, `A method annotated @Transactional calls another @Transactional(REQUIRES_NEW) method in the same class. What actually happens?`, `Nothing new: self-invocation bypasses the proxy, so the inner annotation is ignored and both run in the caller's transaction. Fix by moving the inner method to another bean, injecting the bean into itself via a proxy, using TransactionTemplate programmatically, or AspectJ weaving mode. Moving it to a separate bean is the cleanest and most testable option.`],
    [3, `A service catches an exception inside a @Transactional method and the caller later gets UnexpectedRollbackException. Why?`, `When an inner REQUIRED method participating in the outer transaction throws a RuntimeException, the proxy marks the shared transaction rollback-only. Swallowing the exception in the outer method does not clear that flag, so the commit attempt fails with UnexpectedRollbackException. Either let the exception propagate, use REQUIRES_NEW or NESTED for the part that may fail, or avoid calling transactional code whose failure you intend to tolerate.`],
    [3, `How does Spring create proxies, and what are the JDK vs CGLIB implications?`, `A BeanPostProcessor (for example the auto-proxy creator) wraps the bean after initialisation. JDK dynamic proxies implement the bean's interfaces; CGLIB subclasses the class, and Spring Boot defaults to CGLIB (proxyTargetClass=true). CGLIB cannot proxy final classes or final methods, and injecting by concrete type only works with CGLIB. Proxies also mean this-calls, private methods and constructors are not advised.`],
    [3, `What is the difference between @Transactional readOnly=true and just doing reads without it, and what are the risks?`, `readOnly is a hint: Hibernate skips dirty checking and flushes (FlushMode MANUAL), and some drivers or routing datasources send the query to a replica. It does not stop writes at the database level everywhere, and some engines ignore it. Risks: routing reads to a lagging replica within a read-then-write flow, and assuming it enforces immutability.`],
    [3, `Explain the Open Session In View pattern and why many teams disable it.`, `Spring Boot enables OSIV by default (spring.jpa.open-in-view), keeping the EntityManager open through view rendering so lazy associations load in controllers. The cost is that a database connection can be held for the entire request, including slow serialisation or downstream calls, exhausting the pool, and it hides N+1 queries. Disabling it forces you to fetch what you need in the service layer with fetch joins or DTO projections.`],
    [3, `How do you make @Async and @Scheduled work correctly in production?`, `@Async runs through a proxy, so it has the same self-invocation limits, and by default Boot uses an executor with bounded threads but an unbounded queue if you configure it naively. Define a named ThreadPoolTaskExecutor with sized core/max/queue and a rejection policy, and handle exceptions with an AsyncUncaughtExceptionHandler or by returning CompletableFuture. Security and MDC context do not propagate automatically; use a TaskDecorator. For @Scheduled on multiple instances use a lock such as ShedLock.`],
    [3, `How do @Conditional annotations and BeanFactoryPostProcessor differ from BeanPostProcessor?`, `@Conditional decides whether a bean definition is registered at all. A BeanFactoryPostProcessor modifies bean definitions before any bean is instantiated (property placeholders, for example). A BeanPostProcessor works on instantiated bean instances around initialisation (proxies, @Autowired processing). Mixing them up is why BFPP beans should be declared static, since they are created very early.`],
    [3, `How do you handle a slow database call that is exhausting your HikariCP pool?`, `Confirm with pool metrics (active, pending, timeout counts) and a thread dump showing threads waiting in getConnection. Then find why connections are held long: OSIV, transactions spanning remote calls, or missing indexes. Set connection timeout and leak detection, keep transactions short, and only then size the pool; more connections rarely fix a slow query and can overload the database.`],
    [4, `Design a strategy for transaction management across two services and a message broker without distributed transactions.`, `Use the transactional outbox: write the business change and an outbox row in one local transaction, then a relay (poller or CDC such as Debezium) publishes to the broker with at-least-once delivery. Consumers must be idempotent, using a processed-message table or natural keys. For multi-step business flows add a saga with compensating actions. Trade-off: eventual consistency and operational complexity versus avoiding 2PC's availability and coupling costs.`],
    [4, `Your team wants to stop using field injection and adopt hexagonal architecture in a large Spring codebase. How do you approach it?`, `Enforce constructor injection with a lint or ArchUnit rule, then structure code into domain (no Spring imports), application services, and adapters (web, persistence, messaging) behind ports. Use ArchUnit tests to guard dependency direction and Spring Modulith or Maven modules to enforce boundaries. Migrate incrementally by strangling one bounded context at a time. The trade-off is more mapping code and indirection, justified where the domain is complex or long-lived.`],
    [4, `How do you design caching with Spring Cache in a clustered deployment and avoid stale or stampede problems?`, `Use a shared cache such as Redis for consistency across nodes, with explicit TTLs and key design that includes tenant and version. Use @Cacheable(sync=true) to reduce stampedes on a single node, and consider request coalescing or probabilistic early refresh for hot keys. Evict on writes with @CacheEvict, accepting a small race window, and cache immutable DTOs rather than entities. Local caches such as Caffeine reduce latency but need invalidation via pub/sub or short TTLs.`],
    [4, `How would you build a robust event-driven flow with Spring (Kafka or RabbitMQ), including retries and poison messages?`, `Make consumers idempotent and commit offsets or acks only after processing. Use bounded retries with backoff, then route failures to a dead-letter topic or queue with the error context, and provide tooling to replay them. Separate retryable errors (timeouts) from non-retryable ones (deserialisation, validation) so poison messages do not block a partition. Monitor consumer lag and DLQ depth; ordering guarantees are per key or partition, so choose keys deliberately.`],
    [4, `How do you approach zero-downtime schema changes with Spring, Flyway or Liquibase and rolling deployments?`, `Use the expand and contract pattern: first ship backward-compatible additive changes (new nullable column, new table), deploy code that writes both or reads either, backfill in batches, then remove old structures in a later release. Never rename or drop in the same release the code stops using it, because old and new versions run at the same time during a rolling deploy. Run migrations as a separate step or with a lock so only one instance migrates, and test them against production-sized data for lock duration.`],
    [4, `Design the concurrency approach for a Spring service where two users may update the same aggregate at once.`, `Prefer optimistic locking with a JPA @Version column, returning 409 Conflict on OptimisticLockException and letting the client re-read and retry, since it scales and avoids holding locks. Use pessimistic locking (SELECT FOR UPDATE) only for genuinely hot rows with high conflict, with short transactions and timeouts to limit deadlocks. For cross-node invariants without a row to lock, use unique constraints or a distributed lock as a last resort. Always make retry loops bounded and idempotent.`],
    [4, `What is your approach to observability for a fleet of Spring services?`, `Use Micrometer for metrics, exported to Prometheus or an OTLP backend, and Micrometer Tracing or OpenTelemetry for distributed traces, with trace IDs injected into structured logs via MDC. Define SLIs and SLOs (latency percentiles, error rate, saturation) and alert on burn rate rather than raw thresholds. Watch cardinality of tags, because unbounded values such as user IDs can overwhelm the metrics backend. Add health groups for liveness and readiness so the platform restarts and routes traffic correctly.`],
    [4, `You must upgrade a large system from Spring Framework 5 / Boot 2.x to Boot 3. How do you plan it?`, `Move first to the latest 2.7 release and clear deprecation warnings, then use the Java 17 baseline and the javax to jakarta namespace migration (OpenRewrite recipes help a lot). Audit third-party libraries for Jakarta and Boot 3 support, since these are the usual blockers, and check changed defaults like trailing-slash matching and Micrometer or observation APIs. Upgrade in small, releasable steps behind a good test suite, and canary the rollout. Trade-off: a long-lived migration branch is riskier than incremental changes on trunk.`]
  ],
  notes: [
    { title: `Proxy Pitfalls`, points: [`Advice only runs via the proxy`, `this.method() skips the proxy`, `final and private not advised`, `Fix: split into another bean`, `Prefer TransactionTemplate for control`], code: `@Service\nclass Billing {\n  private final Audit audit; // separate bean\n  @Transactional\n  void pay() { audit.log(); } // REQUIRES_NEW works\n}`, diagram: { type: `flow`, items: [`Caller`, `Proxy`, `Advice`, `Target bean`] } },
    { title: `Hooks in Startup`, points: [`BFPP edits bean definitions`, `BPP wraps bean instances`, `Declare BFPP beans static`, `@Conditional gates registration`, `SmartLifecycle for start and stop order`], diagram: { type: `flow`, items: [`Definitions loaded`, `BFPP runs`, `Beans created`, `BPP wraps`] } },
    { title: `Propagation Cheatsheet`, points: [`REQUIRED: join or create`, `REQUIRES_NEW: suspend, new tx`, `NESTED: savepoint in same tx`, `Inner failure marks outer rollback-only`, `Swallowed exception still rolls back`], code: `@Transactional(propagation = REQUIRES_NEW)\nvoid writeAudit(Event e) { repo.save(e); }` },
    { title: `Outbox Pattern`, points: [`One local tx: data plus outbox row`, `Relay publishes to broker`, `At-least-once delivery`, `Consumers must be idempotent`, `Avoids dual-write inconsistency`], diagram: { type: `flow`, items: [`Business write`, `Outbox table`, `Relay / CDC`, `Broker`] } }
  ],
  cards: [
    [3, `Default bean proxy type in Boot?`, `CGLIB (proxyTargetClass=true), so classes are subclassed; final classes cannot be proxied.`],
    [3, `Why does self-invocation skip @Transactional?`, `The call goes to this, not the proxy, so no advice is applied.`],
    [4, `UnexpectedRollbackException cause?`, `An inner participating method marked the shared transaction rollback-only, then outer tried to commit.`],
    [3, `What does spring.jpa.open-in-view do?`, `Keeps the EntityManager open for the whole request; enabled by default and can hold connections too long.`],
    [2, `@Primary vs @Qualifier?`, `@Primary sets the default candidate; @Qualifier at the injection point selects explicitly and wins.`],
    [3, `BFPP vs BPP?`, `BFPP modifies bean definitions before instantiation; BPP wraps bean instances after creation.`],
    [4, `@TransactionalEventListener default phase?`, `AFTER_COMMIT; listener writes need REQUIRES_NEW as the original transaction is finished.`],
    [4, `Optimistic locking in JPA?`, `@Version column; conflicting update throws OptimisticLockException, commonly mapped to HTTP 409.`],
    [3, `proxyBeanMethods=false effect?`, `Disables CGLIB proxying of the @Configuration class: faster startup but inter-bean calls create new objects.`],
    [4, `Expand and contract migration?`, `Add compatible schema first, migrate code and data, drop old structures in a later release.`]
  ]
};

export const springboot = {
  questions: [
    [1, `What is the difference between application.properties and application.yml?`, `Both hold external configuration and bind to the same Environment; YAML is hierarchical and supports multiple documents and lists more readably, while properties is flat and simple. If both exist, properties files take precedence over YAML. YAML is whitespace sensitive and cannot be loaded with @PropertySource.`],
    [1, `What is the purpose of the embedded server and how can you change it?`, `Boot bundles a servlet container (Tomcat by default) inside the jar so the app runs with java -jar and no external server. To switch, exclude spring-boot-starter-tomcat and add spring-boot-starter-jetty or undertow. Port and other server settings are set with server.* properties.`],
    [2, `How does @ConfigurationProperties differ from @Value, and how do you validate it?`, `@ConfigurationProperties binds a prefix to a typed object with relaxed binding (kebab, camel, env style), nesting, and Duration or DataSize conversion. Add @Validated and Jakarta constraints so startup fails fast on bad config. It is better for grouped settings, while @Value suits a single value.`],
    [2, `What are liveness and readiness probes in Boot and how do they differ?`, `Liveness says the process is healthy or should be restarted; readiness says whether it can accept traffic. Enable them under management.endpoint.health.probes.enabled, and they are exposed as health groups such as /actuator/health/liveness. Do not put downstream dependency checks in liveness, or an outage of the database will cause restart storms.`],
    [2, `How do you write a custom auto-configuration or starter?`, `Create a @AutoConfiguration class with @Bean methods guarded by @ConditionalOnClass, @ConditionalOnMissingBean and @ConditionalOnProperty, and register it in META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports (Boot 2.7+). Provide properties via @ConfigurationProperties. Convention is a separate autoconfigure module plus a thin starter module.`],
    [2, `How do you handle validation errors and return a consistent error body?`, `Use Bean Validation annotations with @Valid on request bodies, then handle MethodArgumentNotValidException and ConstraintViolationException in a @RestControllerAdvice. Return RFC 7807 ProblemDetail (built in since Spring 6 / Boot 3) with field errors. Keep response shape stable, and never leak stack traces.`],
    [3, `How does Boot decide which auto-configurations to apply, and how do you debug it?`, `Candidates come from the AutoConfiguration.imports files on the classpath; each class is filtered by conditions such as @ConditionalOnClass and @ConditionalOnMissingBean. Your own beans usually back off the defaults. Debug with --debug or the conditions actuator endpoint, which lists positive and negative matches, and exclude with spring.autoconfigure.exclude.`],
    [3, `A Boot service passes health checks but requests time out under load. How do you investigate?`, `Check metrics first: Tomcat thread busy count, Hikari pending connections, GC pauses and downstream latency. Take thread dumps to see whether threads block on the pool, locks or an HTTP client without timeouts. Typical fixes are explicit client timeouts, bulkheads and right-sized pools; raising thread counts alone just moves the queue.`],
    [3, `How do you build container images for Boot apps and what are the trade-offs?`, `Use layered jars with Cloud Native Buildpacks (mvn spring-boot:build-image) or a multi-stage Dockerfile using layertools so dependencies sit in a cached layer. Set container-aware JVM flags such as MaxRAMPercentage and run as non-root. Buildpacks give secure, reproducible defaults with less to maintain, while a hand-written Dockerfile gives more control.`],
    [3, `How do you implement graceful shutdown in Spring Boot?`, `Set server.shutdown=graceful and spring.lifecycle.timeout-per-shutdown-phase so in-flight requests finish before the context closes. The instance should first fail readiness or be deregistered so the load balancer stops sending traffic, and the orchestrator grace period must exceed the timeout. Also stop consumers and schedulers cleanly via lifecycle callbacks.`],
    [3, `How do you handle secrets and environment-specific config safely?`, `Keep secrets out of the repo and jar; inject via environment variables, mounted files (configtree:), or a secrets manager such as Vault or a cloud equivalent with Spring Cloud integrations. Use profiles for non-secret differences only. Ensure actuator env and configprops endpoints sanitise values and are not publicly exposed.`],
    [3, `What are the trade-offs of using virtual threads with Spring Boot 3.2+?`, `Setting spring.threads.virtual.enabled=true runs request handling on virtual threads, letting blocking code scale without large thread pools. It helps IO-bound workloads but not CPU-bound ones. Watch for pinning inside synchronized blocks with blocking calls (fixed in later JDKs), thread-local misuse, and connection pools, which still cap real concurrency.`],
    [3, `How do you test slices of a Boot app efficiently?`, `Use slice annotations such as @WebMvcTest, @DataJpaTest and @JsonTest, which load only relevant auto-configuration and are much faster than @SpringBootTest. Use @MockBean for collaborators and Testcontainers for real databases with @ServiceConnection (Boot 3.1+). Context caching makes tests fast, but varying @MockBean or properties breaks the cache and slows the suite.`],
    [4, `Design a configuration strategy for 50 Boot services across environments.`, `Keep a versioned config repository or a central store, with Spring Cloud Config or platform-native ConfigMaps and secrets, and shared defaults inherited via a BOM or a common starter. Enforce typed, validated @ConfigurationProperties so bad config fails at startup, and support dynamic refresh only for values that are truly safe to change. Trade-off: central config adds a runtime dependency, so have local fallbacks or bake defaults into the artifact.`],
    [4, `You need a platform-wide internal starter for logging, security and metrics. How do you design it?`, `Ship it as a starter plus autoconfigure module with conditional, overridable beans and opt-out properties, so teams are not forced into behaviour. Version it through the shared BOM, keep it backward compatible, and document properties with configuration metadata. The risk is a hidden framework that couples all services to one release cadence, so keep it thin and prefer composition over mandatory base classes.`],
    [4, `How would you approach reducing cold-start for Boot on Kubernetes or serverless: CDS, AOT, or GraalVM native?`, `Boot 3 offers AOT processing plus GraalVM native images for very fast start and low memory, at the cost of long builds, reflection and proxy hints, and reduced library compatibility. Class Data Sharing and AppCDS give a smaller win with no code changes, while lazy initialisation cuts startup but moves failures to first use. Choose by measured need: native for scale-to-zero, plain JVM with tuning for long-running services where peak throughput matters.`],
    [4, `Explain how you would set up alerting and SLOs using Boot Actuator and Micrometer.`, `Expose metrics through Micrometer to a backend, using http.server.requests with percentile histograms for latency SLIs and status tags for error rate. Define SLOs per user journey and alert on error-budget burn over multiple windows rather than on CPU or single spikes. Add business metrics and keep tags low-cardinality. Secure actuator endpoints on a separate management port.`],
    [4, `How do you make a Boot service resilient to a flaky downstream dependency?`, `Set strict connect and read timeouts, then layer Resilience4j: retry with backoff and jitter only for idempotent calls, circuit breaker to stop hammering a failing service, bulkhead to isolate thread pools, and fallbacks where a degraded answer is acceptable. Retries multiply load during outages, so budget them and propagate deadlines. Expose breaker state as metrics to see it in incidents.`],
    [4, `A production incident: heap grows steadily and the pod is OOMKilled. Walk through your response.`, `Distinguish Java heap from total container memory: check MaxRAMPercentage, metaspace, direct buffers and thread stacks. Capture a heap dump with -XX:+HeapDumpOnOutOfMemoryError or jcmd, and analyse it for leaks such as unbounded caches, listener registrations or ThreadLocals. Use JFR and actuator metrics for trends. Short-term, restart and add limits; long-term, bound caches and add memory alerts.`],
    [4, `How would you migrate a service from Spring MVC to WebFlux, and should you?`, `Only when you have highly concurrent IO-bound workloads with fully non-blocking dependencies; otherwise MVC (now with virtual threads) is simpler and easier to debug. WebFlux requires reactive drivers, and a blocking call on an event-loop thread can stall the app. Migrate edge by edge, using WebClient first, and measure before committing. The team learning curve and harder stack traces are real costs.`],
    [3, `How do you secure and expose Actuator in production?`, `Expose only the required endpoints (management.endpoints.web.exposure.include), preferably on a separate management port bound to an internal network. Protect them with Spring Security roles, and sanitise sensitive values in env, configprops and heapdump, or disable those endpoints. Give the platform a narrow unauthenticated health path only for probes.`],
    [4, `How do you ensure backward compatibility of a Boot service's API and events during rolling deployments?`, `Both old and new versions run simultaneously, so make changes additive: new optional JSON fields, tolerant readers (ignore unknown properties), and versioned event schemas with a registry. Deprecate before removing and use consumer-driven contract tests such as Spring Cloud Contract or Pact in CI. Use feature flags to decouple deploy from release.`]
  ],
  notes: [
    { title: `Auto-Config Debugging`, points: [`Run with --debug`, `/actuator/conditions lists matches`, `Look for negative matches`, `Exclude via properties`, `Own bean makes default back off`], code: `@Bean\n@ConditionalOnMissingBean\nMyClient client() { return new MyClient(); }`, diagram: { type: `flow`, items: [`imports file`, `Conditions checked`, `Bean registered`, `Or backs off`] } },
    { title: `Probes and Shutdown`, points: [`Liveness: restart me?`, `Readiness: send traffic?`, `Keep dependencies out of liveness`, `server.shutdown=graceful`, `Grace period > phase timeout`], code: `server.shutdown=graceful\nspring.lifecycle.timeout-per-shutdown-phase=30s\nmanagement.endpoint.health.probes.enabled=true` },
    { title: `Test Slices`, points: [`@WebMvcTest: controllers only`, `@DataJpaTest: repos and DB`, `@SpringBootTest: full context`, `Testcontainers for real DB`, `Varying @MockBean breaks cache`], diagram: { type: `stack`, items: [`@SpringBootTest`, `@WebMvcTest`, `@DataJpaTest`, `Plain unit test`] } },
    { title: `Resilience Layers`, points: [`Timeouts first`, `Retry idempotent calls only`, `Circuit breaker stops storms`, `Bulkhead isolates pools`, `Fallback for degraded answer`], diagram: { type: `flow`, items: [`Timeout`, `Retry`, `Breaker`, `Fallback`] } }
  ],
  cards: [
    [2, `Where to register auto-config in Boot 3?`, `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`],
    [3, `See why a bean was or was not configured?`, `Run with --debug or hit /actuator/conditions.`],
    [3, `Enable graceful shutdown?`, `server.shutdown=graceful plus a lifecycle timeout.`],
    [3, `Virtual threads property?`, `spring.threads.virtual.enabled=true (Boot 3.2+).`],
    [4, `Why keep DB checks out of liveness?`, `A database outage would restart healthy pods and cause restart storms.`],
    [3, `Build an image without a Dockerfile?`, `mvn spring-boot:build-image (Cloud Native Buildpacks).`],
    [4, `Retry only when...?`, `The call is idempotent, with backoff, jitter and a retry budget.`],
    [2, `Standard error body in Boot 3?`, `RFC 7807 ProblemDetail.`],
    [3, `Why is @SpringBootTest slow?`, `It loads the full context; prefer slices like @WebMvcTest.`],
    [4, `Bound Actuator exposure how?`, `management.endpoints.web.exposure.include, separate management port, secured roles.`]
  ]
};

export const maven = {
  questions: [
    [1, `What is the difference between mvn install and mvn deploy?`, `install copies the built artifact to your local repository (~/.m2) so other local projects can use it. deploy uploads it to a remote repository such as Nexus or Artifactory, for sharing with the team or CI. deploy requires distributionManagement and credentials configured.`],
    [1, `What is the local repository and what does a SNAPSHOT version mean?`, `The local repository (~/.m2/repository) caches downloaded dependencies and installed artifacts. A SNAPSHOT version such as 1.0-SNAPSHOT signals work in progress and can be updated repeatedly, so Maven may re-check remote repos for newer builds. Releases are immutable.`],
    [2, `What is the difference between plugins and dependencies, and what are plugin goals bound to phases?`, `Dependencies are libraries your code uses at compile, test or runtime. Plugins are build tools that execute goals, and each goal can be bound to a lifecycle phase, for example compiler:compile to compile. Packaging type sets the default bindings, and you add more with executions in the plugin configuration.`],
    [2, `How does Maven resolve transitive dependencies and what is the nearest-wins rule?`, `Maven builds a dependency tree and, when the same artifact appears with different versions, picks the one nearest to the root; ties go to the first declared. That can silently choose an older version than a deeper library expects. Use dependency:tree and dependencyManagement or exclusions to control versions.`],
    [2, `What are exclusions and optional dependencies?`, `An exclusion removes a specific transitive dependency from the consuming project's tree, which is useful for banned or conflicting libraries. Marking a dependency optional means it is used by your module but is not propagated to projects that depend on you. Use optional for feature-specific libraries so consumers opt in.`],
    [2, `What does the maven-surefire vs maven-failsafe plugin split achieve?`, `Surefire runs unit tests in the test phase and fails the build immediately. Failsafe runs integration tests (usually *IT) in integration-test and verify phases so that post-integration-test cleanup, like stopping a container or server, still runs before the failure is reported. Run them with mvn verify.`],
    [3, `How do you use a BOM (bill of materials) with Maven, and why is it better than a parent POM for version alignment?`, `A BOM is a POM of packaging pom containing dependencyManagement, imported with scope import and type pom in your own dependencyManagement. It aligns versions without forcing inheritance, so a project can import several BOMs while still having its own parent. This matters because Maven has single inheritance.`],
    [3, `A build works locally but fails in CI with different dependency versions. How do you diagnose it?`, `Compare mvn dependency:tree output and the effective POM (help:effective-pom) between environments, and check for version ranges, SNAPSHOTs, different settings.xml or mirrors, and stale local repositories. Pin versions, avoid ranges, and use a clean repository in CI. Enforcer rules such as dependencyConvergence catch drift early.`],
    [3, `What does the Maven Enforcer plugin do and which rules do you use?`, `It fails the build when project rules are violated. Common rules: requireMavenVersion, requireJavaVersion, dependencyConvergence, banDuplicatePomDependencyVersions, bannedDependencies and requireReleaseDeps for releases. It moves policy from wiki pages into automated checks.`],
    [3, `Explain how to achieve reproducible builds with Maven.`, `Set project.build.outputTimestamp so archive entries get fixed timestamps, pin plugin versions, and avoid version ranges and SNAPSHOTs. Reproducibility also needs a consistent JDK and stable file ordering, which recent plugin versions provide. Verify with the artifact:compare goal or by building twice and comparing checksums.`],
    [3, `How do you manage plugin versions and what is pluginManagement?`, `pluginManagement in the parent declares versions and default configuration that child modules inherit only when they use the plugin, while plugins actually binds it to every child. Always pin plugin versions, since unpinned ones resolve to Maven's defaults and vary across Maven releases, harming reproducibility.`],
    [3, `How do you handle a private repository and credentials safely?`, `Configure repositories and mirrors in settings.xml, with server credentials referenced by id, never in the pom. Use encrypted passwords or environment variable interpolation and short-lived CI tokens. A repository manager mirroring central also gives caching and protection against upstream outages.`],
    [3, `What is the difference between mvn -pl, -am and -amd, and how do you use them in monorepos?`, `-pl selects specific modules, -am also builds the modules they depend on, and -amd builds modules that depend on them. In CI you can compute changed modules from git and build them with -pl and -amd to run only affected tests. Combine with -T for parallel builds.`],
    [4, `Design the Maven structure for a 40-module platform with several deployable services.`, `Use a root aggregator with a parent POM for plugin management and shared properties, and a separate BOM module to publish aligned versions for consumers. Separate API and contract modules from implementation to keep the dependency graph acyclic and thin. Enforce layering with the Enforcer or ArchUnit, and use CI that builds only affected modules. Trade-off: one big reactor gives atomic changes but slower builds, while separate repositories give autonomy but complicate versioning.`],
    [4, `How would you introduce an internal Maven repository manager and governance policy?`, `Deploy Nexus or Artifactory as the single proxy for Maven Central and internal artifacts, block direct internet access from CI, and enforce release repository immutability. Add checksum and vulnerability scanning (for example OWASP Dependency-Check or a repository firewall) and retention policies for snapshots. It improves supply-chain security and build availability at the cost of operating another critical service.`],
    [4, `What are the pros and cons of the maven-release-plugin versus CI-friendly versions?`, `The release plugin edits and commits poms, tags, and bumps versions, but it is slow, mutates the repo and fails awkwardly midway. CI-friendly versions use properties such as revision, changelist and sha1 in the pom, letting CI set the version with -Drevision and flatten-maven-plugin to publish resolved poms. The latter suits continuous delivery, while release plugin fits infrequent controlled releases.`],
    [4, `How do you defend against dependency supply-chain attacks in a Maven build?`, `Use a repository manager as the only source, verify checksums and signatures where available, pin exact versions and review updates via automated PRs. Generate an SBOM with CycloneDX, scan for known vulnerabilities in CI, and watch for dependency confusion by claiming internal group IDs on the proxy. Trade-off: strictness slows upgrades, so automate updates with tests.`],
    [4, `Your build takes 45 minutes. What is your systematic plan to reduce it?`, `Measure first with a build profiler or the -T timing and per-module durations, then attack the biggest costs. Parallelise modules (-T), split unit and integration tests, cache the local repository in CI, and build only changed modules. Consider the Maven build cache extension or move very large monorepos toward a tool with remote caching. Remove unnecessary plugins and avoid clean where safe.`],
    [4, `How do you migrate from Maven to Gradle, or should you?`, `Migrate only when you have a concrete pain such as build time or complex custom logic that Gradle's incremental build and remote cache solve. Costs include retraining, losing Maven's convention and uniformity, and rewriting plugin behaviour. Often improving Maven with parallelism, caching and the build cache extension is cheaper. If migrating, do it incrementally with a pilot module and keep publication coordinates stable.`],
    [3, `How do you handle multiple JDK targets and library compatibility in a Maven multi-module build?`, `Use maven.compiler.release rather than source and target, so the compiler checks against the correct API for that Java version. Toolchains let modules build with specific JDKs independent of the machine's default. For libraries used by different runtimes, consider multi-release jars or separate artifacts, and test on each supported JDK in a CI matrix.`],
    [4, `A transitive vulnerability is found in a library deep in your tree, and upstream has not patched. What do you do?`, `Confirm exploitability and whether the vulnerable code path is used, then override the version through dependencyManagement in your BOM or parent if a compatible fixed release exists. If not, exclude and replace or shade, and add regression tests. Track it in the SBOM with a review date and re-check when upstream updates. Always record the rationale so the override is not forgotten.`],
    [4, `How would you standardise build quality gates across teams using Maven?`, `Provide a corporate parent POM or a shared build profile that bundles plugin versions, Enforcer rules, formatting or Checkstyle, JaCoCo coverage checks, and dependency scanning. Keep it thin, versioned and upgradable, and let teams override with documented properties. Failing the build on gates in CI while allowing local overrides keeps developer speed while protecting mainline.`]
  ],
  notes: [
    { title: `Dependency Mediation`, points: [`Nearest to root wins`, `Tie: first declared wins`, `dependency:tree shows the result`, `Pin via dependencyManagement`, `Exclusions cut branches`], code: `mvn dependency:tree -Dverbose\nmvn help:effective-pom`, diagram: { type: `flow`, items: [`Your POM`, `Direct deps`, `Transitives`, `Nearest wins`] } },
    { title: `BOM Import`, points: [`Aligns versions without inheritance`, `Scope import, type pom`, `Import several BOMs`, `Child omits versions`, `Ideal for internal platforms`], code: `<dependencyManagement><dependencies>\n <dependency>\n  <groupId>com.acme</groupId>\n  <artifactId>acme-bom</artifactId>\n  <version>1.4.0</version>\n  <type>pom</type><scope>import</scope>\n </dependency>\n</dependencies></dependencyManagement>` },
    { title: `Reproducible Builds`, points: [`Set outputTimestamp`, `Pin plugin versions`, `No ranges or SNAPSHOTs`, `Same JDK in CI`, `Compare artifact checksums`], code: `<project.build.outputTimestamp>2024-01-01T00:00:00Z\n</project.build.outputTimestamp>` },
    { title: `Unit vs Integration Tests`, points: [`Surefire: unit, phase test`, `Failsafe: *IT, verify phase`, `Failsafe cleans up before failing`, `Run all with mvn verify`, `Split for faster feedback`], diagram: { type: `flow`, items: [`test (Surefire)`, `pre-integration`, `integration (Failsafe)`, `verify`] } }
  ],
  cards: [
    [1, `install vs deploy?`, `install: local ~/.m2. deploy: remote repository.`],
    [2, `Failsafe vs Surefire?`, `Surefire runs unit tests; Failsafe runs *IT in integration-test and verifies later.`],
    [3, `Why import a BOM?`, `Align versions without using your single parent slot.`],
    [3, `pluginManagement vs plugins?`, `pluginManagement only declares defaults; plugins actually binds them.`],
    [3, `Flag for reproducible archives?`, `project.build.outputTimestamp`],
    [3, `Use -amd for what?`, `Also build modules that depend on the selected ones.`],
    [4, `Why prefer maven.compiler.release?`, `It compiles against the correct JDK API set, not just the bytecode level.`],
    [3, `Enforcer rule for version clashes?`, `dependencyConvergence.`],
    [4, `Dependency confusion defence?`, `Proxy-only repository access and claiming internal group IDs.`],
    [4, `CI-friendly versions use?`, `revision, changelist and sha1 properties plus flatten-maven-plugin.`]
  ]
};

export const restapi = {
  questions: [
    [1, `What is the difference between path parameters and query parameters?`, `Path parameters identify a specific resource, as in /users/42. Query parameters filter, sort, paginate or otherwise modify a collection or view, as in /users?role=admin&page=2. Use path for identity and query for optional refinement.`],
    [1, `What is the difference between stateless and stateful, and why does REST prefer stateless?`, `Stateless means each request carries all information needed to process it, and the server keeps no client session between requests. This lets any server instance handle any request, so scaling and failover are simple. State, if needed, lives in the client, a token or a shared store.`],
    [2, `What are HATEOAS and the Richardson Maturity Model?`, `The model ranks APIs from level 0 (single endpoint), level 1 (resources), level 2 (HTTP verbs and status codes) to level 3 (hypermedia links). HATEOAS means responses include links to possible next actions so clients discover them. Most real-world APIs stop at level 2 because hypermedia adds complexity that few clients exploit.`],
    [2, `How does content negotiation work?`, `The client states what it accepts through the Accept header (and Accept-Language), and the server picks a representation and reports it in Content-Type, or replies 406 Not Acceptable. Request body format is declared by the client with Content-Type, and 415 signals an unsupported one. It can also carry versions through media types.`],
    [2, `Explain CORS and how a preflight request works.`, `Browsers block cross-origin requests unless the server permits them via Access-Control-Allow-* headers. For non-simple requests (custom headers, methods like PUT, JSON content type) the browser sends an OPTIONS preflight, and the server answers with allowed origins, methods and headers. It is a browser control, not server-side security, and wildcard origins must not be combined with credentials.`],
    [2, `How should you design error responses?`, `Use appropriate status codes with a consistent machine-readable body, ideally RFC 9457 (formerly 7807) problem details with type, title, status, detail and field errors. Include a correlation ID for support. Never expose stack traces or internal details, and do not return 200 with an error inside.`],
    [3, `Design an idempotency-key mechanism for a payment POST endpoint.`, `The client sends an Idempotency-Key header; the server stores the key with a hash of the request and the response, atomically claiming it with a unique constraint before doing work. A retry with the same key and body returns the stored response, while the same key with a different body returns 422 or 409. Keys need a TTL, and in-flight duplicates should get 409 or wait. The trade-off is extra storage and care with partial failures.`],
    [3, `Offset vs cursor (keyset) pagination: when do you choose each?`, `Offset is simple and supports jumping to page N, but deep offsets are slow and results shift when rows are inserted or deleted. Cursor pagination uses an opaque token of the last seen sort key, giving stable, fast paging on large or changing datasets. It cannot jump to arbitrary pages, and it requires a deterministic unique ordering such as (created_at, id).`],
    [3, `How do ETags and conditional requests support caching and concurrency control?`, `An ETag identifies a representation version. Clients send If-None-Match on GET to receive 304 Not Modified, saving bandwidth. For updates, If-Match makes PUT or PATCH fail with 412 Precondition Failed if the resource changed, which prevents lost updates. Strong versus weak ETags depend on byte-level equality.`],
    [3, `How do you version an API, and what are the trade-offs among URL, header and media-type versioning?`, `URL versions (/v2/) are visible, cacheable and easy to route, but proliferate. Header or media-type versioning keeps URLs stable but is harder to test and cache. Whichever you pick, prefer additive changes, treat removal as a breaking change with a deprecation window, and announce with Sunset and Deprecation headers.`],
    [3, `Explain common rate limiting algorithms and how you would return limits to clients.`, `Fixed window is simple but permits bursts at boundaries; sliding window is smoother; token bucket allows controlled bursts at a steady rate; leaky bucket smooths output. Enforce in a gateway with a shared store such as Redis for multi-node correctness. Respond with 429 and Retry-After, and expose remaining quota in RateLimit headers.`],
    [3, `How do you design a long-running operation in REST (for example, report generation)?`, `Accept the request with 202 Accepted and a Location header pointing to a status resource. The client polls it, or receives a webhook, and the status resource reports progress and eventually links to the result (or 303 See Other). Make the operation idempotent and expose cancellation. Polling is simple; webhooks reduce load but need retries and signature verification.`],
    [3, `What are common REST API security vulnerabilities you check for?`, `Broken object-level authorisation (accessing another user's ID), broken authentication, excessive data exposure, mass assignment via binding request bodies straight to entities, missing rate limits, and injection. Enforce authorisation per object on the server, whitelist writable fields with DTOs, and validate inputs. The OWASP API Security Top 10 is a good checklist.`],
    [4, `Design an API for bulk operations with partial failures.`, `Options: a batch endpoint accepting an array and returning 207 Multi-Status or 200 with a per-item result array containing status and error. Alternatively make it asynchronous with 202 and a job resource. Define whether the batch is atomic or best-effort, cap batch size, and give each item an idempotency key or client ID so retries are safe. Best-effort with clear per-item results is usually more practical at scale.`],
    [4, `How would you design webhooks for a public API platform?`, `Sign each payload (HMAC with a per-subscriber secret and timestamp) so receivers can verify authenticity and reject replays. Deliver at-least-once with exponential backoff, a retry window and a dead-letter view, and include event IDs so consumers can deduplicate. Allow subscribers to replay events and monitor failing endpoints, disabling those that fail persistently. Keep payloads small or thin with a fetch-by-ID pattern.`],
    [4, `How do you evolve a widely used API while supporting thousands of integrations?`, `Adopt additive-only evolution, with tolerant-reader guidance for clients, and version at coarse granularity for genuine breaking changes. Use contract tests and API linting (for example Spectral rules in CI) plus an OpenAPI diff check to block accidental breaks. Track usage per client so deprecations are targeted, communicate timelines with Sunset headers, and provide migration tooling. The cost is long-lived support of old versions, so set explicit end-of-life policy.`],
    [4, `Explain how you would design multi-tenant API rate limiting and fairness.`, `Limit per tenant and per API key, with tiers for plans, plus a global protection layer to shield backends. Use token buckets in a distributed store, and separate quotas for expensive endpoints. To avoid noisy neighbours, apply concurrency limits and load shedding under saturation, returning 429 or 503 with Retry-After. Trade-off: centralised counters add latency and a dependency, so sometimes approximate local limits are acceptable.`],
    [4, `Where does an API gateway help and where does it hurt?`, `It centralises cross-cutting concerns such as authentication, rate limiting, TLS, routing, request logging and response caching, and offers a single entry point. It hurts when business logic creeps in, when it becomes a bottleneck or single point of failure, or when every team must queue changes through it. Keep it thin, deploy it redundantly, and keep authorisation decisions about business objects in services.`],
    [4, `How do you achieve consistency and avoid lost updates in a REST API with concurrent clients?`, `Use optimistic concurrency: return a version or ETag on GET, require If-Match on updates, and answer 412 (or 409 with a version field) on a mismatch so the client re-reads and merges. Pair it with a version column in the database. For commands such as transfers, model them as explicit resources or operations with idempotency keys instead of blind PUTs of computed balances.`],
    [3, `How do you handle backward-compatible changes to enums and response fields?`, `Treat adding enum values as potentially breaking, since strict clients may crash, so document that clients must tolerate unknown values and provide a default. Add response fields freely but never change types or meanings. For requests, introduce new optional fields with defaults, and never make an optional field required in the same version. Contract tests with sample old clients catch violations.`],
    [4, `Design the observability approach for an API platform.`, `Propagate a correlation or W3C traceparent ID through every request and log it in structured logs. Measure RED metrics (rate, errors, duration) per route template, not per raw URL to keep cardinality low, and track latency percentiles instead of averages. Define SLOs per endpoint tier and alert on burn rate. Add synthetic checks for critical journeys and dashboards by client to spot who is affected.`],
    [4, `A client reports duplicate orders after a timeout. What went wrong and how do you fix it design-wise?`, `The client timed out, retried a non-idempotent POST, and the server had already processed the first request. Fix by requiring an Idempotency-Key with server-side deduplication, backed by a unique constraint, and returning the original result on replay. Also make the client retry with the same key and backoff, and consider giving the order a client-generated ID so PUT can create it idempotently.`]
  ],
  notes: [
    { title: `Idempotency Key Flow`, points: [`Client sends Idempotency-Key`, `Server claims key atomically`, `Store request hash and response`, `Replay returns stored response`, `Same key, new body: reject`], code: `POST /payments\nIdempotency-Key: 7f3a-91c2\n\n-> 201 Created (first)\n-> 201 same body (retry)`, diagram: { type: `flow`, items: [`Request + key`, `Claim key`, `Do work`, `Store response`] } },
    { title: `Paging Options`, points: [`Offset: simple, drifts, slow deep`, `Cursor: stable, fast`, `Needs unique sort key`, `Opaque cursor tokens`, `Cap page size`], code: `GET /orders?limit=20&after=eyJpZCI6NDJ9\n{ "data": [...], "next": "eyJpZCI6NjJ9" }` },
    { title: `Conditional Requests`, points: [`ETag identifies a version`, `If-None-Match gives 304`, `If-Match gives 412 on conflict`, `Prevents lost updates`, `Cheap caching win`], diagram: { type: `flow`, items: [`GET + ETag`, `Client edits`, `PUT If-Match`, `200 or 412`] } },
    { title: `Rate Limit Cheatsheet`, points: [`Token bucket allows bursts`, `Sliding window is smoother`, `Shared store for many nodes`, `429 plus Retry-After`, `Limit per key and tenant`], diagram: { type: `stack`, items: [`Global protection`, `Per tenant quota`, `Per key limit`, `Per endpoint cost`] } }
  ],
  cards: [
    [2, `Status for unsupported request body type?`, `415 Unsupported Media Type.`],
    [2, `Status when Accept cannot be met?`, `406 Not Acceptable.`],
    [3, `Which header prevents lost updates?`, `If-Match with an ETag; mismatch returns 412.`],
    [3, `Cursor pagination requirement?`, `A deterministic unique sort key such as (created_at, id).`],
    [3, `Status for async job accepted?`, `202 Accepted with a Location header to a status resource.`],
    [4, `Which header tells clients when to retry?`, `Retry-After, sent with 429 or 503.`],
    [4, `Secure webhooks how?`, `HMAC signature plus timestamp, and dedupe by event ID.`],
    [3, `BOLA in one line?`, `Accessing another user's object by changing the ID; check authorisation per object.`],
    [4, `Announce API retirement with?`, `Deprecation and Sunset headers plus a published timeline.`],
    [3, `Adding an enum value is...?`, `Potentially breaking; clients must tolerate unknown values.`]
  ]
};
