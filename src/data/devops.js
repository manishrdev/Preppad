export const jenkins = {
  questions: [
    [1, 'What is Jenkins and what is it used for?', 'An open-source automation server used to build, test and deploy software. It runs jobs/pipelines triggered by commits, schedules or webhooks and has a large plugin ecosystem.'],
    [1, 'Freestyle job vs Pipeline job?', 'Freestyle is configured through the UI and is limited. Pipeline is defined as code (Jenkinsfile) - versioned, reviewable, supports stages, parallelism, and complex logic. Pipelines are the recommended approach.'],
    [2, 'Declarative vs Scripted pipeline?', 'Declarative has a structured, opinionated syntax (pipeline { agent, stages, post }) with validation and is easier to read. Scripted is Groovy-based and more flexible but more error-prone. Use declarative and drop into script blocks when needed.'],
    [2, 'Explain Jenkins master/controller and agents.', 'The controller schedules jobs and serves the UI; agents (nodes) execute builds. Run builds on agents (static, or dynamic Docker/Kubernetes agents) with labels; keep the controller executor count at 0 for security and stability.'],
    [2, 'How do you handle credentials securely in Jenkins?', 'Store secrets in the Jenkins Credentials store (or integrate Vault/AWS Secrets Manager) and inject with withCredentials or credentials() binding; secrets are masked in logs. Never hardcode secrets in the Jenkinsfile or repo.'],
    [2, 'How do you trigger builds automatically?', 'Webhooks from GitHub/GitLab/Bitbucket (preferred), SCM polling (fallback), cron schedules, upstream job triggers, and multibranch pipelines that auto-create jobs per branch and PR.'],
    [2, 'What are shared libraries?', 'Reusable Groovy pipeline code stored in a Git repo and loaded with @Library, providing common steps and standardized pipeline templates across many projects.'],
    [3, 'How do you make Jenkins pipelines fast and reliable?', 'Parallel stages, caching dependencies, ephemeral container agents, workspace cleanup, timeouts and retries, fail-fast, splitting tests, avoiding heavy plugins, and monitoring queue times and build duration trends.'],
    [3, 'How would you implement blue/green or canary deployment via Jenkins?', 'Pipeline builds and pushes an image, deploys to the idle environment (blue/green) or a small percentage (canary) using Kubernetes/ECS/CodeDeploy, runs smoke tests and metrics checks, then shifts traffic and keeps automatic rollback on failed health checks.'],
    [4, 'How do you scale and govern Jenkins for a large organization?', 'Configuration as code (JCasC), plugin inventory control, controller HA/backup strategy or sharded controllers, Kubernetes dynamic agents, folder-level RBAC, shared library governance, audit logs, and possibly a migration path to managed CI (GitHub Actions/CodePipeline) where it reduces toil.'],
  ],
  notes: [
    { title: 'Declarative Jenkinsfile', diagram: { type: 'flow', items: ['Checkout', 'Build', 'Test', 'Package', 'Deploy'] }, points: ['agent = where it runs', 'stages > stage > steps', 'post { always / failure }'], code: 'pipeline {\n  agent any\n  stages {\n    stage("Build") { steps { sh "mvn -B package" } }\n    stage("Test")  { steps { sh "mvn test" } }\n  }\n  post { failure { echo "Failed" } }\n}' },
    { title: 'Jenkins Architecture', diagram: { type: 'flow', items: ['Git webhook', 'Controller', 'Agent executes', 'Artifacts / Deploy'] }, points: ['Controller orchestrates only', 'Agents run the work', 'Credentials store for secrets'] },
    { title: 'Pipeline Best Practices', points: ['Jenkinsfile in the repo', 'Ephemeral agents', 'Timeouts + retries', 'Shared libraries for reuse', 'Never echo secrets'] },
  ],
};

export const cicd = {
  questions: [
    [1, 'What is CI/CD? Difference between Continuous Delivery and Deployment?', 'CI: merge often and verify every change automatically (build + tests). Continuous Delivery: every passing build is releasable, with a manual approval to production. Continuous Deployment: every passing change is deployed to production automatically.'],
    [1, 'What are the typical stages of a CI/CD pipeline?', 'Source/commit -> build -> unit tests -> static analysis and security scans -> package artifact/image -> deploy to staging -> integration/E2E tests -> approval -> production deploy -> monitoring/rollback.'],
    [2, 'What is a build artifact and why should it be built once?', 'The versioned, immutable output of a build (jar, container image). Build once and promote the same artifact through environments to guarantee what you tested is what you ship; configuration differs, code does not.'],
    [2, 'Deployment strategies: rolling, blue/green, canary?', 'Rolling replaces instances gradually. Blue/green switches traffic between two full environments - instant rollback, double cost. Canary sends a small share of traffic to the new version and expands based on metrics. Feature flags decouple deploy from release.'],
    [2, 'How do you handle database migrations in a pipeline?', 'Version-controlled migrations (Flyway/Liquibase) run automatically, made backward compatible (expand/contract pattern) so old and new app versions can both run during rollout; test on production-like data and avoid destructive changes in the same release.'],
    [2, 'Trunk-based development vs GitFlow?', 'Trunk-based: short-lived branches merged to main daily with feature flags - suits CI/CD and fast feedback. GitFlow: long-lived develop/release branches - more ceremony and merge pain. Most modern teams prefer trunk-based.'],
    [3, 'How do you shift security left in a pipeline (DevSecOps)?', 'SAST (SonarQube/CodeQL), dependency and SBOM scans (OWASP Dependency-Check, Snyk), secret scanning, container image scanning (Trivy), IaC scanning, DAST against staging, signed images, and policy gates that fail builds on critical findings.'],
    [3, 'What is GitOps?', 'Git is the source of truth for desired infrastructure/app state; an operator (Argo CD, Flux) continuously reconciles the cluster to match. Deploy = merge to Git; rollback = revert. Gives auditability and drift detection.'],
    [3, 'How do you measure pipeline and delivery performance?', 'DORA metrics: deployment frequency, lead time for changes, change failure rate, and time to restore service. Also track pipeline duration, flaky test rate and queue time to find bottlenecks.'],
    [4, 'Design a CI/CD platform for 100+ services across environments.', 'Standardized pipeline templates/shared libraries, ephemeral runners, artifact repository, environment promotion with approvals and policy-as-code, GitOps for deployment, centralized secrets management, observability-driven automatic rollback, self-service onboarding, and cost/governance dashboards.'],
  ],
  notes: [
    { title: 'CI/CD Pipeline Flow', diagram: { type: 'flow', items: ['Commit', 'Build', 'Test', 'Scan', 'Deploy', 'Monitor'] }, points: ['CI = build + test on every commit', 'Delivery = releasable + manual gate', 'Deployment = fully automatic'] },
    { title: 'Deployment Strategies', points: ['Rolling: replace gradually', 'Blue/Green: swap traffic, fast rollback', 'Canary: small % first, watch metrics', 'Feature flags: deploy != release'], diagram: { type: 'flow', items: ['v1 100%', 'v2 5%', 'v2 50%', 'v2 100%'] } },
    { title: 'DORA Metrics', points: ['Deployment frequency', 'Lead time for changes', 'Change failure rate', 'Time to restore'] },
  ],
};

export const aws = {
  questions: [
    [1, 'Explain EC2, S3 and RDS in one line each.', 'EC2: resizable virtual servers. S3: durable object storage with buckets/keys, versioning and lifecycle rules. RDS: managed relational databases (MySQL, PostgreSQL, Oracle, SQL Server, Aurora) with backups, patching and Multi-AZ.'],
    [1, 'What are Regions and Availability Zones?', 'A Region is a geographic area; an Availability Zone is one or more isolated data centers within it. Deploy across multiple AZs for high availability and across Regions for disaster recovery.'],
    [1, 'What is IAM? Explain users, roles and policies.', 'Identity and Access Management controls who can do what. Users have long-term credentials; roles provide temporary credentials assumed by services or users (preferred); policies are JSON documents that allow/deny actions on resources. Follow least privilege.'],
    [2, 'Security groups vs NACLs?', 'Security groups are stateful, attached to ENIs/instances, allow rules only. NACLs are stateless, apply at the subnet level, and support allow and deny rules with ordered evaluation.'],
    [2, 'Explain a VPC with public and private subnets.', 'A VPC is an isolated virtual network. Public subnets route to an Internet Gateway; private subnets have no direct inbound internet and reach out via a NAT gateway. Place load balancers in public and app/DB tiers in private subnets.'],
    [2, 'Lambda: how does it work, and what are cold starts and limits?', 'Serverless functions triggered by events (API Gateway, S3, SQS). You pay per invocation and duration. Cold start = init latency of a new execution environment (mitigate with provisioned concurrency, smaller packages, SnapStart for Java). Limits: 15-minute timeout, memory up to 10 GB, concurrency quotas.'],
    [2, 'SQS vs SNS vs EventBridge vs Kinesis?', 'SQS: queue, consumers pull (decoupling, buffering). SNS: pub/sub fan-out push. EventBridge: event bus with rich routing rules and SaaS integrations. Kinesis: ordered streaming with replay for high-volume data.'],
    [2, 'Elastic Load Balancer types and Auto Scaling?', 'ALB (layer 7, HTTP routing), NLB (layer 4, high performance/static IP), GWLB (appliances). Auto Scaling groups add/remove instances by policies (target tracking on CPU/requests) across AZs with health checks.'],
    [2, 'DynamoDB vs RDS - when to choose which?', 'DynamoDB: serverless key-value/document store with single-digit ms latency and virtually unlimited scale, requires access-pattern-driven modelling. RDS/Aurora: relational, joins, transactions, ad-hoc queries. Choose by access patterns and consistency needs.'],
    [3, 'How do you design a highly available, fault tolerant web application on AWS?', 'Route 53 -> CloudFront -> ALB across multiple AZs -> Auto Scaling app tier (containers/EC2) -> Multi-AZ Aurora/RDS with read replicas, ElastiCache, S3 for static/objects, health checks, infrastructure as code, and backups/cross-region replication for DR.'],
    [3, 'ECS vs EKS vs Fargate vs Lambda?', 'ECS: AWS-native container orchestration, simpler. EKS: managed Kubernetes for portability and ecosystem. Fargate: serverless compute engine for ECS/EKS (no nodes to manage). Lambda: event-driven functions. Choose by team skills, workload duration and control needs.'],
    [3, 'How do you secure data and secrets in AWS?', 'Encrypt at rest (KMS, S3 SSE, EBS/RDS encryption) and in transit (TLS), keep secrets in Secrets Manager or Parameter Store, use IAM roles not access keys, VPC endpoints, S3 Block Public Access, CloudTrail/GuardDuty/Config for auditing and detection.'],
    [4, 'Describe disaster recovery strategies and their RTO/RPO trade-offs.', 'Backup and restore (cheapest, hours), pilot light (core minimal infra always on), warm standby (scaled-down full copy), multi-site active-active (near-zero RTO/RPO, highest cost). Choose from business requirements, and test failovers regularly.'],
    [4, 'How do you control cost and govern a multi-account AWS estate?', 'AWS Organizations with SCPs, landing zone (Control Tower), account-per-workload/environment, tagging standards, Budgets and Cost Explorer alerts, rightsizing, Savings Plans/Reserved capacity, lifecycle policies, and centralized logging/security accounts.'],
  ],
  notes: [
    { title: 'Three-Tier on AWS', diagram: { type: 'flow', items: ['Route 53', 'CloudFront', 'ALB', 'ECS / EC2', 'Aurora'] }, points: ['Multi-AZ everywhere', 'App tier in private subnets', 'S3 for static assets'] },
    { title: 'IAM Essentials', points: ['Prefer roles over users', 'Least privilege policies', 'Explicit Deny always wins', 'MFA on root and humans'], code: '{\n "Effect": "Allow",\n "Action": ["s3:GetObject"],\n "Resource": "arn:aws:s3:::my-bucket/*"\n}' },
    { title: 'Messaging Services', points: ['SQS = queue (pull)', 'SNS = fan-out (push)', 'EventBridge = routing bus', 'Kinesis = ordered streams'], diagram: { type: 'flow', items: ['Producer', 'SNS topic', 'SQS queues', 'Consumers'] } },
  ],
};

export const architecture = {
  questions: [
    [1, 'What is a monolith vs microservices?', 'A monolith deploys as one unit: simple to build and debug but scales and releases as a whole. Microservices split by business capability into independently deployable services: team autonomy and targeted scaling, at the cost of distributed-system complexity.'],
    [1, 'What is a layered (n-tier) architecture?', 'Organizing code into layers such as presentation, business, data access and database, where each layer depends only on the one beneath. It separates concerns and eases testing but can become rigid if layers leak.'],
    [2, 'Explain the CAP theorem and eventual consistency.', 'During a network partition a distributed system must choose Consistency or Availability (Partition tolerance is unavoidable). Eventual consistency lets replicas diverge briefly and converge later - trading strict consistency for availability and latency.'],
    [2, 'What is an API Gateway and what does it do?', 'A single entry point handling routing, authentication, rate limiting, request/response transformation, caching and aggregation for backend services. Avoid putting business logic in it.'],
    [2, 'How does caching work and what are the invalidation strategies?', 'Store hot data closer to the consumer (browser, CDN, Redis, in-process). Strategies: cache-aside, read-through, write-through, write-behind; TTLs and explicit invalidation on writes. Watch stampedes, stale data and memory limits.'],
    [3, 'Explain the Saga pattern and how it differs from 2PC.', 'A saga is a sequence of local transactions with compensating actions for rollback, coordinated by choreography (events) or orchestration (central coordinator). It avoids blocking distributed locks of two-phase commit, at the cost of eventual consistency and compensation design.'],
    [3, 'Event-driven architecture, CQRS and event sourcing - when do they fit?', 'Event-driven decouples producers/consumers via a broker. CQRS separates write and read models to optimize each. Event sourcing stores state as an append-only event log (audit, replay). They add complexity - use where auditability, scale asymmetry or integration justify it.'],
    [3, 'How do you make services resilient?', 'Timeouts, retries with backoff and jitter, circuit breakers, bulkheads, fallbacks, idempotent operations, rate limiting/load shedding, health checks, graceful degradation, and chaos testing.'],
    [3, 'Explain observability: logs, metrics, traces.', 'Logs (structured events), metrics (aggregated numbers - RED/USE), and distributed traces (request path across services with correlation IDs). OpenTelemetry standardizes collection; SLOs and alerting sit on top.'],
    [4, 'Walk through designing a URL shortener or notification system at scale.', 'Clarify requirements and scale (QPS, storage, latency), define APIs and data model, pick key generation (base62 of unique IDs), datastore (key-value/NoSQL), cache hot keys, CDN/edge redirects, replicate/shard, handle analytics asynchronously via queue, and discuss trade-offs, failure modes and monitoring.'],
    [4, 'How do you make and communicate architectural decisions?', 'Capture context, options and trade-offs in Architecture Decision Records, evaluate against quality attributes (scalability, cost, security, operability), prototype risky parts, involve stakeholders, define fitness functions to guard decisions, and revisit as constraints change.'],
    [4, 'How do you estimate and plan a large delivery with uncertainty?', 'Break work into a WBS, estimate with ranges (three-point/PERT) or relative sizing, identify dependencies and risks, add buffers explicitly, deliver in thin vertical slices to validate assumptions early, track velocity/burn-up, and communicate confidence levels and re-forecast regularly.'],
  ],
  notes: [
    { title: 'Monolith vs Microservices', points: ['Monolith: simple, one deploy', 'Microservices: independent scale + teams', 'Modular monolith is a great start', 'Distributed monolith = worst of both'], diagram: { type: 'flow', items: ['Client', 'API Gateway', 'Services', 'Own databases'] } },
    { title: 'Resilience Patterns', points: ['Timeout everything', 'Retry with backoff + jitter', 'Circuit breaker stops cascades', 'Bulkhead isolates failures', 'Idempotency makes retries safe'], diagram: { type: 'flow', items: ['Call', 'Timeout', 'Retry', 'Circuit open', 'Fallback'] } },
    { title: 'Saga Example', diagram: { type: 'flow', items: ['Create order', 'Reserve stock', 'Charge card', 'Ship'] }, points: ['Each step has a compensating action', 'Failure -> run compensations backwards', 'Choreography = events; Orchestration = coordinator'] },
  ],
};
