# Optional. Leave empty to run in local-only mode (progress saved in this browser).
# Create a free Firebase project -> Add Web app -> copy these values.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
